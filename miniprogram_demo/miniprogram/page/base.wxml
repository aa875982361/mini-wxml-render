
  
  
<wxs module="utils">
  module.exports = {
    getTagName: function(item, cid){
      if(!item.tagName && typeof item.content === "undefined"){
        return "tmpl_"+cid+"_"+"null"
      }
      var tag = item.tagName || "text-node";
      var result = "tmpl_"+cid+"_"+tag;
      return result
    },
    getTemplateByCid: function(cid){
      return "container_" + cid
    },
    getValue: function(value, defaultValue, isObj){
      if(typeof value === "undefined"){
        return isObj ? {} : defaultValue
      }
      return value
    },
    isFocus: function(item, prefix) {
      var s = item.focus !== undefined ? 'focus' : 'blur'
      return prefix + item.tagName + '_' + s
    },
  }
</wxs>

  
<template name="container_0"><block wx:for="{{vdoms}}" wx:key="key"><template is="{{utils.getTagName(item, cid)}}" data="{{item: item, cid}}"></template></block></template>

<template name="tmpl_0_view">
  <view wx:key="{{item.key}}" data-data="{{item.dataSet}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_0_pure-view">
  <view wx:key="{{item.key}}" style="{{item.style}}" class="{{item.class}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_0_static-view">
  <view wx:key="{{item.key}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}"  style="{{item.style}}" class="{{item.class}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>


<template name="tmpl_0_text">
  <text wx:key="{{item.key}}" data-data="{{item.dataSet}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}"><template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" /></text>
</template>

<template name="tmpl_0_template">
  <template is="{{item.is}}" data="{{...item.data}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </template>
</template>

<template name="tmpl_0_button">
  <button wx:key="{{item.key}}" data-data="{{item.dataSet}}" size="{{utils.getValue(item.size,'default')}}" type="{{item.type}}" plain="{{utils.getValue(item.plain,false)}}" disabled="{{item.disabled}}" loading="{{utils.getValue(item.loading,false)}}" form-type="{{item.formType}}" open-type="{{item.openType}}" hover-class="{{utils.getValue(item.hoverClass,'button-hover')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,20)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,70)}}" name="{{item.name}}" lang="{{utils.getValue(item.lang,en)}}" session-from="{{item.sessionFrom}}" send-message-title="{{item.sendMessageTitle}}" send-message-path="{{item.sendMessagePath}}" send-message-img="{{item.sendMessageImg}}" app-parameter="{{item.appParameter}}" show-message-card="{{utils.getValue(item.showMessageCard,false)}}" business-id="{{item.businessId}}" bindgetuserinfo="eventHandler" bindcontact="eventHandler" bindgetphonenumber="eventHandler" binderror="eventHandler" bindopensetting="eventHandler" bindlaunchapp="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </button>
</template>

<template name="tmpl_0_input">
  <template is="{{utils.isFocus(item, 'tmpl_0_')}}" data="{{item:item}}" />
</template>

<template name="tmpl_0_input_focus">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" focus="{{utils.getValue(item.focus,false)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_0_input_blur">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_0_text-node"> {{item.content}} </template>

<template name="tmpl_0_navbar">
  <navbar id="{{item.uid}}" title="{{item.title}}" noPlaceholder="{{item.noPlaceholder}}" css="{{item.css}}"></navbar>
</template>

<template name="tmpl_0_keyboard">
  <keyboard id="{{item.uid}}" limit="{{item.limit}}" price="{{item.price}}" bind:change="eventHandler" bind:confirm="eventHandler"></keyboard>
</template>

<template name="tmpl_0_scroll-view">
  <scroll-view wx:key="{{item.key}}" data-data="{{item.dataSet}}" scroll-x="{{utils.getValue(item.scrollX,false)}}" scroll-y="{{utils.getValue(item.scrollY,false)}}" upper-threshold="{{utils.getValue(item.upperThreshold,50)}}" lower-threshold="{{utils.getValue(item.lowerThreshold,50)}}" scroll-top="{{item.scrollTop}}" scroll-left="{{item.scrollLeft}}" scroll-into-view="{{item.scrollIntoView}}" scroll-with-animation="{{utils.getValue(item.scrollWithAnimation,false)}}" enable-back-to-top="{{utils.getValue(item.enableBackToTop,false)}}" bindscrolltoupper="eventHandler" bindscrolltolower="eventHandler" bindscroll="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" enable-flex="{{utils.getValue(item.enableFlex,false)}}" scroll-anchoring="{{utils.getValue(item.scrollAnchoring,false)}}" refresher-enabled="{{utils.getValue(item.refresherEnabled,false)}}" refresher-threshold="{{utils.getValue(item.refresherThreshold,45)}}" refresher-default-style="{{utils.getValue(item.refresherDefaultStyle,'black')}}" refresher-background="{{utils.getValue(item.refresherBackground,'#FFF')}}" refresher-triggered="{{utils.getValue(item.refresherTriggered,false)}}" enhanced="{{utils.getValue(item.enhanced,false)}}" bounces="{{utils.getValue(item.bounces,true)}}" show-scrollbar="{{utils.getValue(item.showScrollbar,true)}}" paging-enabled="{{utils.getValue(item.pagingEnabled,false)}}" fast-deceleration="{{utils.getValue(item.fastDeceleration,false)}}" binddragstart="eventHandler" binddragging="eventHandler" binddragend="eventHandler" bindrefresherpulling="eventHandler" bindrefresherrefresh="eventHandler" bindrefresherrestore="eventHandler" bindrefresherabort="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </scroll-view>
</template>

<template name="tmpl_0_block">
  <block wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </block>
</template>

<template name="tmpl_0_null">
</template>

<template name="tmpl_0_image">
  <image wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" src="{{item.src}}" mode="{{utils.getValue(item.mode,'scaleToFill')}}" lazy-load="{{utils.getValue(item.lazyLoad,false)}}" binderror="eventHandler" bindload="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" webp="{{utils.getValue(item.webp,false)}}" show-menu-by-longpress="{{utils.getValue(item.showMenuByLongpress,false)}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </image>
</template>

<template name="tmpl_0_icon">
  <icon wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" size="{{utils.getValue(item.size, 23)}}" type="{{item.type}}" color="{{utils.getValue(item.color, '')}}" bind:tap="eventHandler"/>
</template>

<template name="tmpl_0_i">
  <i 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    style="{{item.style}}" class="{{item.class}}" 
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchcancel="eventHandler"
    bindtouchend="eventHandler"
    bindtap="eventHandler"
    bindlongpress="eventHandler"
    bindlongtap="eventHandler"
    bindtransitionend="eventHandler"
    bindanimationstart="eventHandler"
    bindanimationiteration="eventHandler"
    bindanimationend="eventHandler"
    bindtouchforcechange="eventHandler"
  />
</template>

<template name="tmpl_0_progress">
  <progress wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" percent="{{utils.getValue(item.percent, 0)}}" color="{{utils.getValue(item.color, '#09BB07')}}" activeColor="{{utils.getValue(item.activeColor, '#09BB07')}}" backgroundColor="{{utils.getValue(item.backgroundColor, '#EBEBEB')}}" active="{{utils.getValue(item.active, false)}}" active-mode="{{utils.getValue(item.activeMode, backwards)}}"  show-info="{{utils.getValue(item.showInfo, false)}}" stroke-width="{{utils.getValue(item.strokeWidth, 6)}}" border-radius="{{util.getValue(item.borderRadius, 0)}}" font-size="{{utils.getValue(item.fontSize, 16)}}" duration="{{utils.getValue(item.duration, 30)}}" bindactiveend="eventHandler"/>
</template>

<template name="tmpl_0_rich-text">
  <rich-text wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" nodes="{{utils.getValue(item.nodes, [])}}" space="{{util.getVaue(item.space, 'ensp')}}" ></rich-text>
</template>

<template name="tmpl_0_swiper">
  <swiper
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"  
    indicator-dots="{{utils.getValue(item.indicatorDots, false)}}"
    indicator-color="{{utils.getValue(item.indicatorcolor, 'rgba(0, 0, 0, .3)')}}"
    indicator-active-color="{{utils.getValue(item.indicatorActiveColor, '#000000')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}" 
    interval="{{utils.getValue(item.interval, 5000)}}" 
    duration="{{utils.getValue(item.duration, 500)}}"
    circular="{{utils.getValue(item.circular, false)}}" 
    vertical="{{utils.getValue(item.vertical, false)}}" 
    previous-margin="{{utils.getValue(item.previousMargin, '0px')}}" 
    next-margin="{{utils.getValue(item.nextMargin, '0px')}}" 
    snap-to-edge="{{utils.getValue(item.snapToEdge, false)}}" 
    display-multiple-items="{{utils.getValue(item.displayMultipleItems, 1)}}" 
    easing-function="{{utils.getValue(item.easingFunction, 'default')}}" 
    bindchange="eventHandler"
    bindtransition="eventHandler"
    bindanimationfinish="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper>
</template>

<template name="tmpl_0_swiper-item">
  <swiper-item wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  item-id="{{utils.getValue(item.itemId, '')}}" skip-hidden-item-layout="{{utils.getValue(item.skipHiddenItemLayout, '')}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper-item>
</template>

<template name="tmpl_0_switch">
  <switch wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  checked="{{utils.getValue(item.checked, false)}}" disabled="{{utils.getValue(item.disabled, false)}}" type="{{utils.getValue(item.type, 'switch')}}" color="{{utils.getValue(item.color, '#04BE02')}}" bindchange="eventHandler"/>
</template>

<template name="tmpl_0_movable-area">
  <movable-area 
    style="{{item.style}}" class="{{item.class}}" 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    scale-area="{{utils.getValue(item.scaleArea, false)}}" 
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-area>
</template>

<template name="tmpl_0_movable-view">
  <movable-view 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}" 
    direction="{{utils.getValue(item.direction, 'none')}}"
    inertia="{{utils.getValue(item.inertia, false)}}"
    out-of-bounds="{{utils.getValue(item.outOfBounds, false)}}"
    x="{{utils.getValue(item.x, '0')}}"
    y="{{utils.getValue(item.y, '0')}}"
    damping="{{utils.getValue(item.damping, 20)}}"
    friction="{{utils.getValue(item.friction, 2)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    scale="{{utils.getValue(item.scale, false)}}"
    scale-min="{{utils.getValue(item.scaleMin, 0.5)}}"
    scale-max="{{utils.getValue(item.scaleMax, 10)}}"
    scale-value="{{utils.getValue(item.scaleValue, 1)}}"
    animation="{{utils.getValue(item.animation, false)}}"
    width="{{utils.getValue(item.width, '10px')}}"
    height="{{utils.getValue(item.height, '10px')}}"
    bindchange="eventHandler"
    bindscale="eventHandler"
    bindhtouchmove="eventHandler"
    bindvtouchmove="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-view>
</template>

<template name="tmpl_0_map">
  <map
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    longitude="{{item.longitude}}"
    latitude="{{item.latitude}}"scale
    scale="{{utils.getValue(item.scale, 16)}}"
    min-scale="{{utils.getValue(item.minScale, 3)}}"
    max-scale="{{utils.getValue(item.maxScale, 20)}}"
    markers="{{utils.getValue(item.markers, [])}}"
    covers="{{utils.getValue(item.covers, [])}}"
    polyline="{{utils.getValue(item.polyline, [])}}"
    circles="{{utils.getValue(item.circles, [])}}"
    controls="{{utils.getValue(item.controls, [])}}"
    include-points="{{utils.getValue(item.includePoints, [])}}"
    show-location="{{utils.getValue(item.showLocation, false)}}"
    polygons="{{utils.getValue(item.polygons, [])}}"
    subkey="{{utils.getValue(item.subkey, '')}}"
    setting="{{utils.getValue(item.setting, '', true)}}"
    layer-style="{{utils.getValue(item.layerStyle, 1)}}"
    rotate="0utils.getValue(item.rotate, 0)}}"
    enable-3D="{{utils.getValue(item.enable3D, false)}}"
    show-compass="{{utils.getValue(item.showCompass, false)}}"
    show-scale="{{utils.getValue(item.showScale, false)}}"
    enable-overlooking="{{utils.getValue(item.enableOverlooking, false)}}"
    enable-zoom="{{utils.getValue(item.enableZoom, true)}}"
    enable-scroll="{{utils.getValue(item.scale, true)}}"
    enable-rotate="{{utils.getValue(item.enableRotate, false)}}"
    enable-satellite="{{utils.getValue(item.enableSatellite, false)}}"
    enable-traffic="{{utils.getValue(item.enableTraffic, false)}}"
    enable-poi="{{utils.getValue(item.enablePoi, true)}}"
    enable-building="{{utils.getValue(item.enableBuilding, false)}}"
    bindtap="eventHandler"
    bindmarkertap="eventHandler"
    bindlabeltap="eventHandler"
    bindcontroltap="eventHandler"
    bindcallouttap="eventHandler"
    bindregionchange="eventHandler"
    bindpoitap="eventHandler"
    bindanchorpointtap="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </map>
</template>

<template name="tmpl_0_cover-view">
    <cover-view
      wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
      style="{{item.style}}" class="{{item.class}}"
      hover-class="{{utils.getValue(item.hoverClass,'none')}}"
      hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}"
      hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}"
      hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}"
      animation="{{item.animation}}"
      bindtouchstart="eventHandler"
      bindtouchmove="eventHandler"
      bindtouchend="eventHandler"
      bindtouchcancel="eventHandler"
      bindlongpress="eventHandler"
      bindanimationstart="eventHandler"
      bindanimationiteration="eventHandler"
      bindanimationend="eventHandler"
      bindtransitionend="eventHandler"
      bindtap="eventHandler"
     >
      <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
    </cover-view>
</template>

<template name="tmpl_0_label">
  <label
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    for="{{utils.getValue(item.for, '')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </label>
</template>

<template name="tmpl_0_checkbox">
  <checkbox
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox>
</template>

<template name="tmpl_0_checkbox-group">
  <checkbox-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox-group>
</template>

<template name="tmpl_0_form">
  <form
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    report-submit="{{utils.getValue(item.reportSubmit, false)}}"
    report-submit-timeout="{{utils.getValue(item.reportSubmitTimeout, 0)}}"
    bindsubmit="eventHandler"
    bindreset="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </form>
</template>

<template name="tmpl_0_radio">
  <radio
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio>
</template>

<template name="tmpl_0_radio-group">
  <radio-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio-group>
</template>

<template name="tmpl_0_picker">
  <picker
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    header-text="{{utils.getValue(item.header-text, '')}}"
    mode="{{utils.getValue(item.mode, 'selector')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    range="{{utils.getValue(item.range, [])}}"
    range-key="{{utils.getValue(item.rangeKey, '')}}"
    value="{{utils.getValue(item.value, 0)}}"
    start="{{utils.getValue(item.start, '')}}"
    end="{{utils.getValue(item.end, '')}}"
    fields="{{utils.getValue(item.fields, 'day')}}"
    custom-item="{{utils.getValue(item.customItem, '')}}"
    bindchange="eventHandler"
    bindcancel="eventHandler"
    bindcolumnchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker>
</template>

<template name="tmpl_0_picker-view">
  <picker-view
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, [])}}"
    indicator-style="{{utils.getValue(item.indicatorStyle, '')}}"
    indicator-class="{{utils.getValue(item.indicatorClass, '')}}"
    mask-style="{{utils.getValue(item.maskStyle, '')}}"
    mask-class="{{utils.getValue(item.maskClass, '')}}"
    bindchange="eventHandler"
    bindpickstart="eventHandler"
    bindpickend="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view>
</template>

<template name="tmpl_0_picker-view-column">
  <picker-view-column
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view-column>
</template>

<template name="tmpl_0_slider">
  <slider
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    min="{{utils.getValue(item.min, 0)}}"
    max="{{utils.getValue(item.max, 100)}}"
    step="{{utils.getValue(item.step, 1)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    value="{{utils.getValue(item.value, 0)}}"
    color="{{utils.getValue(item.color, '#e9e9e9')}}"
    selected-color="{{utils.getValue(item.selectedColor, '#1aad19')}}"
    activeColor="{{utils.getValue(item.activeColor, '#1aad19')}}"
    backgroundColor="{{utils.getValue(item.backgroundColor, '#e9e9e9')}}"
    block-size="{{utils.getValue(item.blockSize, 28)}}"
    block-color="{{utils.getValue(item.blockColor, '#ffffff')}}"
    show-value="{{utils.getValue(item.showValue, false)}}"
    bindchanging="eventHandler"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </slider>
</template>

<template name="tmpl_0_textarea">
  <textarea
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    placeholder-style="{{utils.getValue(item.placeholderStyle, '')}}"
    placeholder-class="{{utils.getValue(item.placeholderClass, 'textarea-placeholder')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    maxlength="{{utils.getValue(item.maxlength, 140)}}"
    auto-focus="{{utils.getValue(item.autoFocus, false)}}"
    focus="{{utils.getValue(item.focus, false)}}"
    auto-height="{{utils.getValue(item.autoHeight, false)}}"
    fixed="{{utils.getValue(item.fixed, false)}}"
    cursor-spacing="{{utils.getValue(item.cursorSpacing, 0)}}"
    cursor="{{utils.getValue(item.cursor, -1)}}"
    show-confirm-bar="{{utils.getValue(item.showConfirmBar, true)}}"
    selection-start="{{utils.getValue(item.selectionStart, -1)}}"
    selection-end="{{utils.getValue(item.selectionEnd, -1)}}"
    adjust-position="{{utils.getValue(item.adjustPosition, true)}}"
    hold-keyboard="{{utils.getValue(item.holdKeyboard, false)}}"
    disable-default-padding="{{utils.getValue(item.disableDefaultPadding, false)}}"
    confirm-type="{{utils.getValue(item.confirmType, 'return')}}"
    confirm-hold="{{utils.getValue(item.confirmHold, false)}}"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindlinechange="eventHandler"
    bindinput="eventHandler"
    bindconfirm="eventHandler"
    bindkeyboardheightchange="eventHandler"
  >
  
  </textarea>
</template>

<template name="tmpl_0_editor">
  <editor
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    read-only="{{utils.getValue(item.readOnly, false)}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    show-img-size="{{utils.getValue(item.showImgSize, false)}}"
    show-img-toolbar="{{utils.getValue(item.showImgToolbar, false)}}"
    show-img-resize="{{utils.getValue(item.showImgResize, false)}}"
    bindready="eventHandler"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindinput="eventHandler"
    bindstatuschange="eventHandler"
  >
  
  </editor>
</template>

<template name="tmpl_0_navigator">
  <navigator
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    target="{{utils.getValue(item.target, 'self')}}"
    url="{{utils.getValue(item.url, '')}}"
    open-type="{{utils.getValue(item.openType, 'navigate')}}"
    delta="{{utils.getValue(item.delta, 1)}}"
    app-id="{{utils.getValue(item.appId, '')}}"
    path="{{utils.getValue(item.path, '')}}"
    extra-data="{{utils.getValue(item.extraData, '', true)}}"
    version="{{utils.getValue(item.version, 'release')}}"
    redirect="{{utils.getValue(item.redirect, false)}}"
    short-link="{{utils.getValue(item.shortLink, '')}}"
    hover-class="{{utils.getValue(item.hoverClass, 'navigator-hover')}}"
    hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation, false)}}"
    hover-start-time="{{utils.getValue(item.hoverStartTime, 50)}}"
    hover-stay-time="{{utils.getValue(item.hoverStayTime, 600)}}"
    bindsuccess="eventHandler"
    bindfail="eventHandler"
    bindcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </navigator>
</template>

<template name="tmpl_0_video">
  <video
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{item.src}}"
    duration="{{utils.getValue(item.duration, 0)}}"
    controls="{{utils.getValue(item.controls, true)}}"
    danmu-list="{{utils.getValue(item.danmuList, [])}}"
    danmu-btn="{{utils.getValue(item.danmuBtn, false)}}"
    enable-danmu="{{utils.getValue(item.enableDanmu, false)}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    loop="{{utils.getValue(item.loop, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    initial-time="{{utils.getValue(item.initialTime, 0)}}"
    page-gesture="{{utils.getValue(item.pageGesture, false)}}"
    direction="{{utils.getValue(item.direction, 0)}}"
    show-progress="{{utils.getValue(item.showProgress, true)}}"
    show-fullscreen-btn="{{utils.getValue(item.showFullscreenBtn, true)}}"
    show-play-btn="{{utils.getValue(item.showPlayBtn, true)}}"
    show-center-play-btn="{{utils.getValue(item.showCenterPlayBtn, true)}}"
    enable-progress-gesture="{{utils.getValue(item.enableProgressGesture, true)}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    poster="{{utils.getValue(item.poster, '')}}"
    show-mute-btn="{{utils.getValue(item.showMuteBtn, false)}}"
    title="{{utils.getValue(item.title, '')}}"
    play-btn-position="{{utils.getValue(item.playBtnPosition, 'bottom')}}"
    enable-play-gesture="{{utils.getValue(item.enablePlayGesture, false)}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    vslide-gesture="{{utils.getValue(item.vslideGesture, false)}}"
    vslide-gesture-in-fullscreen="{{utils.getValue(item.vslideGestureInFullscreen, true)}}"
    ad-unit-id="{{item.adUnitId}}"
    poster-for-crawler="{{item.posterForCrawler}}"
    show-casting-button="{{utils.getValue(item.showCastingButton, false)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    picture-in-picture-show-progress="{{utils.getValue(item.pictureInPictureShowProgress, false)}}"
    enable-auto-rotation="{{utils.getValue(item.enableAutoRotation, false)}}"
    show-screen-lock-button="{{utils.getValue(item.showScreenLockButton, false)}}"
    show-snapshot-button="{{utils.getValue(item.showSnapshotButton, false)}}"
    show-background-playback-button="{{utils.getValue(item.showBackgroundPlaybackButton, false)}}"
    background-poster="{{utils.getValue(item.backgroundPoster, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    is-drm="{{utils.getValue(item.isDrm, false)}}"
    provision-url="{{utils.getValue(item.provisionUrl, '')}}"
    certificate-url="{{utils.getValue(item.certificateUrl, '')}}"
    license-url="{{utils.getValue(item.licenseUrl, '')}}"
    bindplay="eventHandler"
    bindpause="eventHandler"
    bindended="eventHandler"
    bindtimeupdate="eventHandler"
    bindfullscreenchange="eventHandler"
    bindwaiting="eventHandler"
    binderror="eventHandler"
    bindprogress="eventHandler"
    bindloadedmetadata="eventHandler"
    bindcontrolstoggle="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
    bindseekcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </video>
</template>

<template name="tmpl_0_camera">
  <camera
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    mode="{{utils.getValue(item.mode, 'normal')}}"
    resolution="{{utils.getValue(item.resolution, 'medium')}}"
    device-position="{{utils.getValue(item.devicePosition, 'back')}}"
    flash="{{utils.getValue(item.flash, 'auto')}}"
    frame-size="{{utils.getValue(item.frameSize, 'medium')}}"
    bindstop="eventHandler"
    binderror="eventHandler"
    bindinitdone="eventHandler"
    bindscancode="eventHandler"
  >
    
  </camera>
</template>

<template name="tmpl_0_live-player">
  <live-player
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{utils.getValue(item.src, '')}}"
    mode="{{utils.getValue(item.mode, 'live')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    min-cache="{{utils.getValue(item.minCache, 1)}}"
    max-cache="{{utils.getValue(item.maxCache, 3)}}"
    sound-mode="{{utils.getValue(item.soundMode, 'speaker')}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    bindstatechange="eventHandler"
    bindfullscreenchange="eventHandler"
    bindnetstatus="eventHandler"
    bindaudiovolumenotify="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-player>
</template>

<template name="tmpl_0_live-pusher">
  <live-pusher
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    url="{{utils.getValue(item.url, '')}}"
    mode="{{utils.getValue(item.mode, 'RTC')}}"
    autopush="{{utils.getValue(item.autopush, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    enable-camera="{{utils.getValue(item.enableCamera, true)}}"
    auto-focus="{{utils.getValue(item.autoFocus, true)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    beauty="{{utils.getValue(item.beauty, 0)}}"
    whiteness="{{utils.getValue(item.whiteness, 0)}}"
    aspect="{{utils.getValue(item.aspect, '9:16')}}"
    min-bitrate="{{utils.getValue(item.minBitrate, 200)}}"
    max-bitrate="{{utils.getValue(item.maxBitrate, 1000)}}"
    audio-quality="{{utils.getValue(item.audioQuality, 'high')}}"
    waiting-image="{{utils.getValue(item.waitingImage, '')}}"
    waiting-image-hash="{{utils.getValue(item.waitingImageHash, '')}}"
    zoom="{{utils.getValue(item.zoom, false)}}"
    device-position="{{utils.getValue(item.devicePosition, 'front')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    mirror="{{utils.getValue(item.mirror, false)}}"
    remote-mirror="{{utils.getValue(item.remoteMirror, false)}}"
    local-mirror="{{utils.getValue(item.localMirror, 'auto')}}"
    audio-reverb-type="{{utils.getValue(item.audioReverbType, 0)}}"
    enable-mic="{{utils.getValue(item.enableMic, true)}}"
    enable-agc="{{utils.getValue(item.enableAgc, false)}}"
    enable-ans="{{utils.getValue(item.enableAns, false)}}"
    audio-volume-type="{{utils.getValue(item.audioVolumeType, 'auto')}}"
    video-width="{{utils.getValue(item.videoWidth, 360)}}"
    video-height="{{utils.getValue(item.videoHeight, 640)}}"
    beauty-style="{{utils.getValue(item.beautyStyle, 'smooth')}}"
    filter="{{utils.getValue(item.filter, 'standard')}}"
    bindstatechange="eventHandler"
    bindnetstatus="eventHandler"
    binderror="eventHandler"
    bindbgmstart="eventHandler"
    bindbgmprogress="eventHandler"
    bindbgmcomplete="eventHandler"
    bindaudiovolumenotify="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-pusher>
</template>

<template name="tmpl_0_canvas">
  <canvas
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    type="{{utils.getValue(item.type, '')}}"
    canvas-id="{{utils.getValue(item.canvasId, '')}}"
    disable-scroll="{{utils.getValue(item.disableScroll, false)}}"
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchend="eventHandler"
    bindtouchcancel="eventHandler"
    bindlongtap="eventHandler"
    binderror="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </canvas>
</template>

<template name="tmpl_0_keyboard-accessory">
  <keyboard-accessory
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </keyboard-accessory>
</template>

      

<template name="container_1"><block wx:for="{{vdoms}}" wx:key="key"><template is="{{utils.getTagName(item, cid)}}" data="{{item: item, cid}}"></template></block></template>

<template name="tmpl_1_view">
  <view wx:key="{{item.key}}" data-data="{{item.dataSet}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_1_pure-view">
  <view wx:key="{{item.key}}" style="{{item.style}}" class="{{item.class}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_1_static-view">
  <view wx:key="{{item.key}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}"  style="{{item.style}}" class="{{item.class}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>


<template name="tmpl_1_text">
  <text wx:key="{{item.key}}" data-data="{{item.dataSet}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}"><template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" /></text>
</template>

<template name="tmpl_1_template">
  <template is="{{item.is}}" data="{{...item.data}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </template>
</template>

<template name="tmpl_1_button">
  <button wx:key="{{item.key}}" data-data="{{item.dataSet}}" size="{{utils.getValue(item.size,'default')}}" type="{{item.type}}" plain="{{utils.getValue(item.plain,false)}}" disabled="{{item.disabled}}" loading="{{utils.getValue(item.loading,false)}}" form-type="{{item.formType}}" open-type="{{item.openType}}" hover-class="{{utils.getValue(item.hoverClass,'button-hover')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,20)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,70)}}" name="{{item.name}}" lang="{{utils.getValue(item.lang,en)}}" session-from="{{item.sessionFrom}}" send-message-title="{{item.sendMessageTitle}}" send-message-path="{{item.sendMessagePath}}" send-message-img="{{item.sendMessageImg}}" app-parameter="{{item.appParameter}}" show-message-card="{{utils.getValue(item.showMessageCard,false)}}" business-id="{{item.businessId}}" bindgetuserinfo="eventHandler" bindcontact="eventHandler" bindgetphonenumber="eventHandler" binderror="eventHandler" bindopensetting="eventHandler" bindlaunchapp="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </button>
</template>

<template name="tmpl_1_input">
  <template is="{{utils.isFocus(item, 'tmpl_1_')}}" data="{{item:item}}" />
</template>

<template name="tmpl_1_input_focus">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" focus="{{utils.getValue(item.focus,false)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_1_input_blur">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_1_text-node"> {{item.content}} </template>

<template name="tmpl_1_navbar">
  <navbar id="{{item.uid}}" title="{{item.title}}" noPlaceholder="{{item.noPlaceholder}}" css="{{item.css}}"></navbar>
</template>

<template name="tmpl_1_keyboard">
  <keyboard id="{{item.uid}}" limit="{{item.limit}}" price="{{item.price}}" bind:change="eventHandler" bind:confirm="eventHandler"></keyboard>
</template>

<template name="tmpl_1_scroll-view">
  <scroll-view wx:key="{{item.key}}" data-data="{{item.dataSet}}" scroll-x="{{utils.getValue(item.scrollX,false)}}" scroll-y="{{utils.getValue(item.scrollY,false)}}" upper-threshold="{{utils.getValue(item.upperThreshold,50)}}" lower-threshold="{{utils.getValue(item.lowerThreshold,50)}}" scroll-top="{{item.scrollTop}}" scroll-left="{{item.scrollLeft}}" scroll-into-view="{{item.scrollIntoView}}" scroll-with-animation="{{utils.getValue(item.scrollWithAnimation,false)}}" enable-back-to-top="{{utils.getValue(item.enableBackToTop,false)}}" bindscrolltoupper="eventHandler" bindscrolltolower="eventHandler" bindscroll="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" enable-flex="{{utils.getValue(item.enableFlex,false)}}" scroll-anchoring="{{utils.getValue(item.scrollAnchoring,false)}}" refresher-enabled="{{utils.getValue(item.refresherEnabled,false)}}" refresher-threshold="{{utils.getValue(item.refresherThreshold,45)}}" refresher-default-style="{{utils.getValue(item.refresherDefaultStyle,'black')}}" refresher-background="{{utils.getValue(item.refresherBackground,'#FFF')}}" refresher-triggered="{{utils.getValue(item.refresherTriggered,false)}}" enhanced="{{utils.getValue(item.enhanced,false)}}" bounces="{{utils.getValue(item.bounces,true)}}" show-scrollbar="{{utils.getValue(item.showScrollbar,true)}}" paging-enabled="{{utils.getValue(item.pagingEnabled,false)}}" fast-deceleration="{{utils.getValue(item.fastDeceleration,false)}}" binddragstart="eventHandler" binddragging="eventHandler" binddragend="eventHandler" bindrefresherpulling="eventHandler" bindrefresherrefresh="eventHandler" bindrefresherrestore="eventHandler" bindrefresherabort="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </scroll-view>
</template>

<template name="tmpl_1_block">
  <block wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </block>
</template>

<template name="tmpl_1_null">
</template>

<template name="tmpl_1_image">
  <image wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" src="{{item.src}}" mode="{{utils.getValue(item.mode,'scaleToFill')}}" lazy-load="{{utils.getValue(item.lazyLoad,false)}}" binderror="eventHandler" bindload="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" webp="{{utils.getValue(item.webp,false)}}" show-menu-by-longpress="{{utils.getValue(item.showMenuByLongpress,false)}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </image>
</template>

<template name="tmpl_1_icon">
  <icon wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" size="{{utils.getValue(item.size, 23)}}" type="{{item.type}}" color="{{utils.getValue(item.color, '')}}" bind:tap="eventHandler"/>
</template>

<template name="tmpl_1_i">
  <i 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    style="{{item.style}}" class="{{item.class}}" 
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchcancel="eventHandler"
    bindtouchend="eventHandler"
    bindtap="eventHandler"
    bindlongpress="eventHandler"
    bindlongtap="eventHandler"
    bindtransitionend="eventHandler"
    bindanimationstart="eventHandler"
    bindanimationiteration="eventHandler"
    bindanimationend="eventHandler"
    bindtouchforcechange="eventHandler"
  />
</template>

<template name="tmpl_1_progress">
  <progress wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" percent="{{utils.getValue(item.percent, 0)}}" color="{{utils.getValue(item.color, '#09BB07')}}" activeColor="{{utils.getValue(item.activeColor, '#09BB07')}}" backgroundColor="{{utils.getValue(item.backgroundColor, '#EBEBEB')}}" active="{{utils.getValue(item.active, false)}}" active-mode="{{utils.getValue(item.activeMode, backwards)}}"  show-info="{{utils.getValue(item.showInfo, false)}}" stroke-width="{{utils.getValue(item.strokeWidth, 6)}}" border-radius="{{util.getValue(item.borderRadius, 0)}}" font-size="{{utils.getValue(item.fontSize, 16)}}" duration="{{utils.getValue(item.duration, 30)}}" bindactiveend="eventHandler"/>
</template>

<template name="tmpl_1_rich-text">
  <rich-text wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" nodes="{{utils.getValue(item.nodes, [])}}" space="{{util.getVaue(item.space, 'ensp')}}" ></rich-text>
</template>

<template name="tmpl_1_swiper">
  <swiper
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"  
    indicator-dots="{{utils.getValue(item.indicatorDots, false)}}"
    indicator-color="{{utils.getValue(item.indicatorcolor, 'rgba(0, 0, 0, .3)')}}"
    indicator-active-color="{{utils.getValue(item.indicatorActiveColor, '#000000')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}" 
    interval="{{utils.getValue(item.interval, 5000)}}" 
    duration="{{utils.getValue(item.duration, 500)}}"
    circular="{{utils.getValue(item.circular, false)}}" 
    vertical="{{utils.getValue(item.vertical, false)}}" 
    previous-margin="{{utils.getValue(item.previousMargin, '0px')}}" 
    next-margin="{{utils.getValue(item.nextMargin, '0px')}}" 
    snap-to-edge="{{utils.getValue(item.snapToEdge, false)}}" 
    display-multiple-items="{{utils.getValue(item.displayMultipleItems, 1)}}" 
    easing-function="{{utils.getValue(item.easingFunction, 'default')}}" 
    bindchange="eventHandler"
    bindtransition="eventHandler"
    bindanimationfinish="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper>
</template>

<template name="tmpl_1_swiper-item">
  <swiper-item wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  item-id="{{utils.getValue(item.itemId, '')}}" skip-hidden-item-layout="{{utils.getValue(item.skipHiddenItemLayout, '')}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper-item>
</template>

<template name="tmpl_1_switch">
  <switch wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  checked="{{utils.getValue(item.checked, false)}}" disabled="{{utils.getValue(item.disabled, false)}}" type="{{utils.getValue(item.type, 'switch')}}" color="{{utils.getValue(item.color, '#04BE02')}}" bindchange="eventHandler"/>
</template>

<template name="tmpl_1_movable-area">
  <movable-area 
    style="{{item.style}}" class="{{item.class}}" 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    scale-area="{{utils.getValue(item.scaleArea, false)}}" 
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-area>
</template>

<template name="tmpl_1_movable-view">
  <movable-view 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}" 
    direction="{{utils.getValue(item.direction, 'none')}}"
    inertia="{{utils.getValue(item.inertia, false)}}"
    out-of-bounds="{{utils.getValue(item.outOfBounds, false)}}"
    x="{{utils.getValue(item.x, '0')}}"
    y="{{utils.getValue(item.y, '0')}}"
    damping="{{utils.getValue(item.damping, 20)}}"
    friction="{{utils.getValue(item.friction, 2)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    scale="{{utils.getValue(item.scale, false)}}"
    scale-min="{{utils.getValue(item.scaleMin, 0.5)}}"
    scale-max="{{utils.getValue(item.scaleMax, 10)}}"
    scale-value="{{utils.getValue(item.scaleValue, 1)}}"
    animation="{{utils.getValue(item.animation, false)}}"
    width="{{utils.getValue(item.width, '10px')}}"
    height="{{utils.getValue(item.height, '10px')}}"
    bindchange="eventHandler"
    bindscale="eventHandler"
    bindhtouchmove="eventHandler"
    bindvtouchmove="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-view>
</template>

<template name="tmpl_1_map">
  <map
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    longitude="{{item.longitude}}"
    latitude="{{item.latitude}}"scale
    scale="{{utils.getValue(item.scale, 16)}}"
    min-scale="{{utils.getValue(item.minScale, 3)}}"
    max-scale="{{utils.getValue(item.maxScale, 20)}}"
    markers="{{utils.getValue(item.markers, [])}}"
    covers="{{utils.getValue(item.covers, [])}}"
    polyline="{{utils.getValue(item.polyline, [])}}"
    circles="{{utils.getValue(item.circles, [])}}"
    controls="{{utils.getValue(item.controls, [])}}"
    include-points="{{utils.getValue(item.includePoints, [])}}"
    show-location="{{utils.getValue(item.showLocation, false)}}"
    polygons="{{utils.getValue(item.polygons, [])}}"
    subkey="{{utils.getValue(item.subkey, '')}}"
    setting="{{utils.getValue(item.setting, '', true)}}"
    layer-style="{{utils.getValue(item.layerStyle, 1)}}"
    rotate="0utils.getValue(item.rotate, 0)}}"
    enable-3D="{{utils.getValue(item.enable3D, false)}}"
    show-compass="{{utils.getValue(item.showCompass, false)}}"
    show-scale="{{utils.getValue(item.showScale, false)}}"
    enable-overlooking="{{utils.getValue(item.enableOverlooking, false)}}"
    enable-zoom="{{utils.getValue(item.enableZoom, true)}}"
    enable-scroll="{{utils.getValue(item.scale, true)}}"
    enable-rotate="{{utils.getValue(item.enableRotate, false)}}"
    enable-satellite="{{utils.getValue(item.enableSatellite, false)}}"
    enable-traffic="{{utils.getValue(item.enableTraffic, false)}}"
    enable-poi="{{utils.getValue(item.enablePoi, true)}}"
    enable-building="{{utils.getValue(item.enableBuilding, false)}}"
    bindtap="eventHandler"
    bindmarkertap="eventHandler"
    bindlabeltap="eventHandler"
    bindcontroltap="eventHandler"
    bindcallouttap="eventHandler"
    bindregionchange="eventHandler"
    bindpoitap="eventHandler"
    bindanchorpointtap="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </map>
</template>

<template name="tmpl_1_cover-view">
    <cover-view
      wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
      style="{{item.style}}" class="{{item.class}}"
      hover-class="{{utils.getValue(item.hoverClass,'none')}}"
      hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}"
      hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}"
      hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}"
      animation="{{item.animation}}"
      bindtouchstart="eventHandler"
      bindtouchmove="eventHandler"
      bindtouchend="eventHandler"
      bindtouchcancel="eventHandler"
      bindlongpress="eventHandler"
      bindanimationstart="eventHandler"
      bindanimationiteration="eventHandler"
      bindanimationend="eventHandler"
      bindtransitionend="eventHandler"
      bindtap="eventHandler"
     >
      <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
    </cover-view>
</template>

<template name="tmpl_1_label">
  <label
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    for="{{utils.getValue(item.for, '')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </label>
</template>

<template name="tmpl_1_checkbox">
  <checkbox
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox>
</template>

<template name="tmpl_1_checkbox-group">
  <checkbox-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox-group>
</template>

<template name="tmpl_1_form">
  <form
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    report-submit="{{utils.getValue(item.reportSubmit, false)}}"
    report-submit-timeout="{{utils.getValue(item.reportSubmitTimeout, 0)}}"
    bindsubmit="eventHandler"
    bindreset="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </form>
</template>

<template name="tmpl_1_radio">
  <radio
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio>
</template>

<template name="tmpl_1_radio-group">
  <radio-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio-group>
</template>

<template name="tmpl_1_picker">
  <picker
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    header-text="{{utils.getValue(item.header-text, '')}}"
    mode="{{utils.getValue(item.mode, 'selector')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    range="{{utils.getValue(item.range, [])}}"
    range-key="{{utils.getValue(item.rangeKey, '')}}"
    value="{{utils.getValue(item.value, 0)}}"
    start="{{utils.getValue(item.start, '')}}"
    end="{{utils.getValue(item.end, '')}}"
    fields="{{utils.getValue(item.fields, 'day')}}"
    custom-item="{{utils.getValue(item.customItem, '')}}"
    bindchange="eventHandler"
    bindcancel="eventHandler"
    bindcolumnchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker>
</template>

<template name="tmpl_1_picker-view">
  <picker-view
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, [])}}"
    indicator-style="{{utils.getValue(item.indicatorStyle, '')}}"
    indicator-class="{{utils.getValue(item.indicatorClass, '')}}"
    mask-style="{{utils.getValue(item.maskStyle, '')}}"
    mask-class="{{utils.getValue(item.maskClass, '')}}"
    bindchange="eventHandler"
    bindpickstart="eventHandler"
    bindpickend="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view>
</template>

<template name="tmpl_1_picker-view-column">
  <picker-view-column
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view-column>
</template>

<template name="tmpl_1_slider">
  <slider
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    min="{{utils.getValue(item.min, 0)}}"
    max="{{utils.getValue(item.max, 100)}}"
    step="{{utils.getValue(item.step, 1)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    value="{{utils.getValue(item.value, 0)}}"
    color="{{utils.getValue(item.color, '#e9e9e9')}}"
    selected-color="{{utils.getValue(item.selectedColor, '#1aad19')}}"
    activeColor="{{utils.getValue(item.activeColor, '#1aad19')}}"
    backgroundColor="{{utils.getValue(item.backgroundColor, '#e9e9e9')}}"
    block-size="{{utils.getValue(item.blockSize, 28)}}"
    block-color="{{utils.getValue(item.blockColor, '#ffffff')}}"
    show-value="{{utils.getValue(item.showValue, false)}}"
    bindchanging="eventHandler"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </slider>
</template>

<template name="tmpl_1_textarea">
  <textarea
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    placeholder-style="{{utils.getValue(item.placeholderStyle, '')}}"
    placeholder-class="{{utils.getValue(item.placeholderClass, 'textarea-placeholder')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    maxlength="{{utils.getValue(item.maxlength, 140)}}"
    auto-focus="{{utils.getValue(item.autoFocus, false)}}"
    focus="{{utils.getValue(item.focus, false)}}"
    auto-height="{{utils.getValue(item.autoHeight, false)}}"
    fixed="{{utils.getValue(item.fixed, false)}}"
    cursor-spacing="{{utils.getValue(item.cursorSpacing, 0)}}"
    cursor="{{utils.getValue(item.cursor, -1)}}"
    show-confirm-bar="{{utils.getValue(item.showConfirmBar, true)}}"
    selection-start="{{utils.getValue(item.selectionStart, -1)}}"
    selection-end="{{utils.getValue(item.selectionEnd, -1)}}"
    adjust-position="{{utils.getValue(item.adjustPosition, true)}}"
    hold-keyboard="{{utils.getValue(item.holdKeyboard, false)}}"
    disable-default-padding="{{utils.getValue(item.disableDefaultPadding, false)}}"
    confirm-type="{{utils.getValue(item.confirmType, 'return')}}"
    confirm-hold="{{utils.getValue(item.confirmHold, false)}}"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindlinechange="eventHandler"
    bindinput="eventHandler"
    bindconfirm="eventHandler"
    bindkeyboardheightchange="eventHandler"
  >
  
  </textarea>
</template>

<template name="tmpl_1_editor">
  <editor
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    read-only="{{utils.getValue(item.readOnly, false)}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    show-img-size="{{utils.getValue(item.showImgSize, false)}}"
    show-img-toolbar="{{utils.getValue(item.showImgToolbar, false)}}"
    show-img-resize="{{utils.getValue(item.showImgResize, false)}}"
    bindready="eventHandler"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindinput="eventHandler"
    bindstatuschange="eventHandler"
  >
  
  </editor>
</template>

<template name="tmpl_1_navigator">
  <navigator
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    target="{{utils.getValue(item.target, 'self')}}"
    url="{{utils.getValue(item.url, '')}}"
    open-type="{{utils.getValue(item.openType, 'navigate')}}"
    delta="{{utils.getValue(item.delta, 1)}}"
    app-id="{{utils.getValue(item.appId, '')}}"
    path="{{utils.getValue(item.path, '')}}"
    extra-data="{{utils.getValue(item.extraData, '', true)}}"
    version="{{utils.getValue(item.version, 'release')}}"
    redirect="{{utils.getValue(item.redirect, false)}}"
    short-link="{{utils.getValue(item.shortLink, '')}}"
    hover-class="{{utils.getValue(item.hoverClass, 'navigator-hover')}}"
    hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation, false)}}"
    hover-start-time="{{utils.getValue(item.hoverStartTime, 50)}}"
    hover-stay-time="{{utils.getValue(item.hoverStayTime, 600)}}"
    bindsuccess="eventHandler"
    bindfail="eventHandler"
    bindcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </navigator>
</template>

<template name="tmpl_1_video">
  <video
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{item.src}}"
    duration="{{utils.getValue(item.duration, 0)}}"
    controls="{{utils.getValue(item.controls, true)}}"
    danmu-list="{{utils.getValue(item.danmuList, [])}}"
    danmu-btn="{{utils.getValue(item.danmuBtn, false)}}"
    enable-danmu="{{utils.getValue(item.enableDanmu, false)}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    loop="{{utils.getValue(item.loop, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    initial-time="{{utils.getValue(item.initialTime, 0)}}"
    page-gesture="{{utils.getValue(item.pageGesture, false)}}"
    direction="{{utils.getValue(item.direction, 0)}}"
    show-progress="{{utils.getValue(item.showProgress, true)}}"
    show-fullscreen-btn="{{utils.getValue(item.showFullscreenBtn, true)}}"
    show-play-btn="{{utils.getValue(item.showPlayBtn, true)}}"
    show-center-play-btn="{{utils.getValue(item.showCenterPlayBtn, true)}}"
    enable-progress-gesture="{{utils.getValue(item.enableProgressGesture, true)}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    poster="{{utils.getValue(item.poster, '')}}"
    show-mute-btn="{{utils.getValue(item.showMuteBtn, false)}}"
    title="{{utils.getValue(item.title, '')}}"
    play-btn-position="{{utils.getValue(item.playBtnPosition, 'bottom')}}"
    enable-play-gesture="{{utils.getValue(item.enablePlayGesture, false)}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    vslide-gesture="{{utils.getValue(item.vslideGesture, false)}}"
    vslide-gesture-in-fullscreen="{{utils.getValue(item.vslideGestureInFullscreen, true)}}"
    ad-unit-id="{{item.adUnitId}}"
    poster-for-crawler="{{item.posterForCrawler}}"
    show-casting-button="{{utils.getValue(item.showCastingButton, false)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    picture-in-picture-show-progress="{{utils.getValue(item.pictureInPictureShowProgress, false)}}"
    enable-auto-rotation="{{utils.getValue(item.enableAutoRotation, false)}}"
    show-screen-lock-button="{{utils.getValue(item.showScreenLockButton, false)}}"
    show-snapshot-button="{{utils.getValue(item.showSnapshotButton, false)}}"
    show-background-playback-button="{{utils.getValue(item.showBackgroundPlaybackButton, false)}}"
    background-poster="{{utils.getValue(item.backgroundPoster, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    is-drm="{{utils.getValue(item.isDrm, false)}}"
    provision-url="{{utils.getValue(item.provisionUrl, '')}}"
    certificate-url="{{utils.getValue(item.certificateUrl, '')}}"
    license-url="{{utils.getValue(item.licenseUrl, '')}}"
    bindplay="eventHandler"
    bindpause="eventHandler"
    bindended="eventHandler"
    bindtimeupdate="eventHandler"
    bindfullscreenchange="eventHandler"
    bindwaiting="eventHandler"
    binderror="eventHandler"
    bindprogress="eventHandler"
    bindloadedmetadata="eventHandler"
    bindcontrolstoggle="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
    bindseekcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </video>
</template>

<template name="tmpl_1_camera">
  <camera
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    mode="{{utils.getValue(item.mode, 'normal')}}"
    resolution="{{utils.getValue(item.resolution, 'medium')}}"
    device-position="{{utils.getValue(item.devicePosition, 'back')}}"
    flash="{{utils.getValue(item.flash, 'auto')}}"
    frame-size="{{utils.getValue(item.frameSize, 'medium')}}"
    bindstop="eventHandler"
    binderror="eventHandler"
    bindinitdone="eventHandler"
    bindscancode="eventHandler"
  >
    
  </camera>
</template>

<template name="tmpl_1_live-player">
  <live-player
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{utils.getValue(item.src, '')}}"
    mode="{{utils.getValue(item.mode, 'live')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    min-cache="{{utils.getValue(item.minCache, 1)}}"
    max-cache="{{utils.getValue(item.maxCache, 3)}}"
    sound-mode="{{utils.getValue(item.soundMode, 'speaker')}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    bindstatechange="eventHandler"
    bindfullscreenchange="eventHandler"
    bindnetstatus="eventHandler"
    bindaudiovolumenotify="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-player>
</template>

<template name="tmpl_1_live-pusher">
  <live-pusher
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    url="{{utils.getValue(item.url, '')}}"
    mode="{{utils.getValue(item.mode, 'RTC')}}"
    autopush="{{utils.getValue(item.autopush, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    enable-camera="{{utils.getValue(item.enableCamera, true)}}"
    auto-focus="{{utils.getValue(item.autoFocus, true)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    beauty="{{utils.getValue(item.beauty, 0)}}"
    whiteness="{{utils.getValue(item.whiteness, 0)}}"
    aspect="{{utils.getValue(item.aspect, '9:16')}}"
    min-bitrate="{{utils.getValue(item.minBitrate, 200)}}"
    max-bitrate="{{utils.getValue(item.maxBitrate, 1000)}}"
    audio-quality="{{utils.getValue(item.audioQuality, 'high')}}"
    waiting-image="{{utils.getValue(item.waitingImage, '')}}"
    waiting-image-hash="{{utils.getValue(item.waitingImageHash, '')}}"
    zoom="{{utils.getValue(item.zoom, false)}}"
    device-position="{{utils.getValue(item.devicePosition, 'front')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    mirror="{{utils.getValue(item.mirror, false)}}"
    remote-mirror="{{utils.getValue(item.remoteMirror, false)}}"
    local-mirror="{{utils.getValue(item.localMirror, 'auto')}}"
    audio-reverb-type="{{utils.getValue(item.audioReverbType, 0)}}"
    enable-mic="{{utils.getValue(item.enableMic, true)}}"
    enable-agc="{{utils.getValue(item.enableAgc, false)}}"
    enable-ans="{{utils.getValue(item.enableAns, false)}}"
    audio-volume-type="{{utils.getValue(item.audioVolumeType, 'auto')}}"
    video-width="{{utils.getValue(item.videoWidth, 360)}}"
    video-height="{{utils.getValue(item.videoHeight, 640)}}"
    beauty-style="{{utils.getValue(item.beautyStyle, 'smooth')}}"
    filter="{{utils.getValue(item.filter, 'standard')}}"
    bindstatechange="eventHandler"
    bindnetstatus="eventHandler"
    binderror="eventHandler"
    bindbgmstart="eventHandler"
    bindbgmprogress="eventHandler"
    bindbgmcomplete="eventHandler"
    bindaudiovolumenotify="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-pusher>
</template>

<template name="tmpl_1_canvas">
  <canvas
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    type="{{utils.getValue(item.type, '')}}"
    canvas-id="{{utils.getValue(item.canvasId, '')}}"
    disable-scroll="{{utils.getValue(item.disableScroll, false)}}"
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchend="eventHandler"
    bindtouchcancel="eventHandler"
    bindlongtap="eventHandler"
    binderror="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </canvas>
</template>

<template name="tmpl_1_keyboard-accessory">
  <keyboard-accessory
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </keyboard-accessory>
</template>

      

<template name="container_2"><block wx:for="{{vdoms}}" wx:key="key"><template is="{{utils.getTagName(item, cid)}}" data="{{item: item, cid}}"></template></block></template>

<template name="tmpl_2_view">
  <view wx:key="{{item.key}}" data-data="{{item.dataSet}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_2_pure-view">
  <view wx:key="{{item.key}}" style="{{item.style}}" class="{{item.class}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_2_static-view">
  <view wx:key="{{item.key}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}"  style="{{item.style}}" class="{{item.class}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>


<template name="tmpl_2_text">
  <text wx:key="{{item.key}}" data-data="{{item.dataSet}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}"><template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" /></text>
</template>

<template name="tmpl_2_template">
  <template is="{{item.is}}" data="{{...item.data}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </template>
</template>

<template name="tmpl_2_button">
  <button wx:key="{{item.key}}" data-data="{{item.dataSet}}" size="{{utils.getValue(item.size,'default')}}" type="{{item.type}}" plain="{{utils.getValue(item.plain,false)}}" disabled="{{item.disabled}}" loading="{{utils.getValue(item.loading,false)}}" form-type="{{item.formType}}" open-type="{{item.openType}}" hover-class="{{utils.getValue(item.hoverClass,'button-hover')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,20)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,70)}}" name="{{item.name}}" lang="{{utils.getValue(item.lang,en)}}" session-from="{{item.sessionFrom}}" send-message-title="{{item.sendMessageTitle}}" send-message-path="{{item.sendMessagePath}}" send-message-img="{{item.sendMessageImg}}" app-parameter="{{item.appParameter}}" show-message-card="{{utils.getValue(item.showMessageCard,false)}}" business-id="{{item.businessId}}" bindgetuserinfo="eventHandler" bindcontact="eventHandler" bindgetphonenumber="eventHandler" binderror="eventHandler" bindopensetting="eventHandler" bindlaunchapp="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </button>
</template>

<template name="tmpl_2_input">
  <template is="{{utils.isFocus(item, 'tmpl_2_')}}" data="{{item:item}}" />
</template>

<template name="tmpl_2_input_focus">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" focus="{{utils.getValue(item.focus,false)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_2_input_blur">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_2_text-node"> {{item.content}} </template>

<template name="tmpl_2_navbar">
  <navbar id="{{item.uid}}" title="{{item.title}}" noPlaceholder="{{item.noPlaceholder}}" css="{{item.css}}"></navbar>
</template>

<template name="tmpl_2_keyboard">
  <keyboard id="{{item.uid}}" limit="{{item.limit}}" price="{{item.price}}" bind:change="eventHandler" bind:confirm="eventHandler"></keyboard>
</template>

<template name="tmpl_2_scroll-view">
  <scroll-view wx:key="{{item.key}}" data-data="{{item.dataSet}}" scroll-x="{{utils.getValue(item.scrollX,false)}}" scroll-y="{{utils.getValue(item.scrollY,false)}}" upper-threshold="{{utils.getValue(item.upperThreshold,50)}}" lower-threshold="{{utils.getValue(item.lowerThreshold,50)}}" scroll-top="{{item.scrollTop}}" scroll-left="{{item.scrollLeft}}" scroll-into-view="{{item.scrollIntoView}}" scroll-with-animation="{{utils.getValue(item.scrollWithAnimation,false)}}" enable-back-to-top="{{utils.getValue(item.enableBackToTop,false)}}" bindscrolltoupper="eventHandler" bindscrolltolower="eventHandler" bindscroll="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" enable-flex="{{utils.getValue(item.enableFlex,false)}}" scroll-anchoring="{{utils.getValue(item.scrollAnchoring,false)}}" refresher-enabled="{{utils.getValue(item.refresherEnabled,false)}}" refresher-threshold="{{utils.getValue(item.refresherThreshold,45)}}" refresher-default-style="{{utils.getValue(item.refresherDefaultStyle,'black')}}" refresher-background="{{utils.getValue(item.refresherBackground,'#FFF')}}" refresher-triggered="{{utils.getValue(item.refresherTriggered,false)}}" enhanced="{{utils.getValue(item.enhanced,false)}}" bounces="{{utils.getValue(item.bounces,true)}}" show-scrollbar="{{utils.getValue(item.showScrollbar,true)}}" paging-enabled="{{utils.getValue(item.pagingEnabled,false)}}" fast-deceleration="{{utils.getValue(item.fastDeceleration,false)}}" binddragstart="eventHandler" binddragging="eventHandler" binddragend="eventHandler" bindrefresherpulling="eventHandler" bindrefresherrefresh="eventHandler" bindrefresherrestore="eventHandler" bindrefresherabort="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </scroll-view>
</template>

<template name="tmpl_2_block">
  <block wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </block>
</template>

<template name="tmpl_2_null">
</template>

<template name="tmpl_2_image">
  <image wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" src="{{item.src}}" mode="{{utils.getValue(item.mode,'scaleToFill')}}" lazy-load="{{utils.getValue(item.lazyLoad,false)}}" binderror="eventHandler" bindload="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" webp="{{utils.getValue(item.webp,false)}}" show-menu-by-longpress="{{utils.getValue(item.showMenuByLongpress,false)}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </image>
</template>

<template name="tmpl_2_icon">
  <icon wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" size="{{utils.getValue(item.size, 23)}}" type="{{item.type}}" color="{{utils.getValue(item.color, '')}}" bind:tap="eventHandler"/>
</template>

<template name="tmpl_2_i">
  <i 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    style="{{item.style}}" class="{{item.class}}" 
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchcancel="eventHandler"
    bindtouchend="eventHandler"
    bindtap="eventHandler"
    bindlongpress="eventHandler"
    bindlongtap="eventHandler"
    bindtransitionend="eventHandler"
    bindanimationstart="eventHandler"
    bindanimationiteration="eventHandler"
    bindanimationend="eventHandler"
    bindtouchforcechange="eventHandler"
  />
</template>

<template name="tmpl_2_progress">
  <progress wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" percent="{{utils.getValue(item.percent, 0)}}" color="{{utils.getValue(item.color, '#09BB07')}}" activeColor="{{utils.getValue(item.activeColor, '#09BB07')}}" backgroundColor="{{utils.getValue(item.backgroundColor, '#EBEBEB')}}" active="{{utils.getValue(item.active, false)}}" active-mode="{{utils.getValue(item.activeMode, backwards)}}"  show-info="{{utils.getValue(item.showInfo, false)}}" stroke-width="{{utils.getValue(item.strokeWidth, 6)}}" border-radius="{{util.getValue(item.borderRadius, 0)}}" font-size="{{utils.getValue(item.fontSize, 16)}}" duration="{{utils.getValue(item.duration, 30)}}" bindactiveend="eventHandler"/>
</template>

<template name="tmpl_2_rich-text">
  <rich-text wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" nodes="{{utils.getValue(item.nodes, [])}}" space="{{util.getVaue(item.space, 'ensp')}}" ></rich-text>
</template>

<template name="tmpl_2_swiper">
  <swiper
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"  
    indicator-dots="{{utils.getValue(item.indicatorDots, false)}}"
    indicator-color="{{utils.getValue(item.indicatorcolor, 'rgba(0, 0, 0, .3)')}}"
    indicator-active-color="{{utils.getValue(item.indicatorActiveColor, '#000000')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}" 
    interval="{{utils.getValue(item.interval, 5000)}}" 
    duration="{{utils.getValue(item.duration, 500)}}"
    circular="{{utils.getValue(item.circular, false)}}" 
    vertical="{{utils.getValue(item.vertical, false)}}" 
    previous-margin="{{utils.getValue(item.previousMargin, '0px')}}" 
    next-margin="{{utils.getValue(item.nextMargin, '0px')}}" 
    snap-to-edge="{{utils.getValue(item.snapToEdge, false)}}" 
    display-multiple-items="{{utils.getValue(item.displayMultipleItems, 1)}}" 
    easing-function="{{utils.getValue(item.easingFunction, 'default')}}" 
    bindchange="eventHandler"
    bindtransition="eventHandler"
    bindanimationfinish="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper>
</template>

<template name="tmpl_2_swiper-item">
  <swiper-item wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  item-id="{{utils.getValue(item.itemId, '')}}" skip-hidden-item-layout="{{utils.getValue(item.skipHiddenItemLayout, '')}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper-item>
</template>

<template name="tmpl_2_switch">
  <switch wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  checked="{{utils.getValue(item.checked, false)}}" disabled="{{utils.getValue(item.disabled, false)}}" type="{{utils.getValue(item.type, 'switch')}}" color="{{utils.getValue(item.color, '#04BE02')}}" bindchange="eventHandler"/>
</template>

<template name="tmpl_2_movable-area">
  <movable-area 
    style="{{item.style}}" class="{{item.class}}" 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    scale-area="{{utils.getValue(item.scaleArea, false)}}" 
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-area>
</template>

<template name="tmpl_2_movable-view">
  <movable-view 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}" 
    direction="{{utils.getValue(item.direction, 'none')}}"
    inertia="{{utils.getValue(item.inertia, false)}}"
    out-of-bounds="{{utils.getValue(item.outOfBounds, false)}}"
    x="{{utils.getValue(item.x, '0')}}"
    y="{{utils.getValue(item.y, '0')}}"
    damping="{{utils.getValue(item.damping, 20)}}"
    friction="{{utils.getValue(item.friction, 2)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    scale="{{utils.getValue(item.scale, false)}}"
    scale-min="{{utils.getValue(item.scaleMin, 0.5)}}"
    scale-max="{{utils.getValue(item.scaleMax, 10)}}"
    scale-value="{{utils.getValue(item.scaleValue, 1)}}"
    animation="{{utils.getValue(item.animation, false)}}"
    width="{{utils.getValue(item.width, '10px')}}"
    height="{{utils.getValue(item.height, '10px')}}"
    bindchange="eventHandler"
    bindscale="eventHandler"
    bindhtouchmove="eventHandler"
    bindvtouchmove="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-view>
</template>

<template name="tmpl_2_map">
  <map
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    longitude="{{item.longitude}}"
    latitude="{{item.latitude}}"scale
    scale="{{utils.getValue(item.scale, 16)}}"
    min-scale="{{utils.getValue(item.minScale, 3)}}"
    max-scale="{{utils.getValue(item.maxScale, 20)}}"
    markers="{{utils.getValue(item.markers, [])}}"
    covers="{{utils.getValue(item.covers, [])}}"
    polyline="{{utils.getValue(item.polyline, [])}}"
    circles="{{utils.getValue(item.circles, [])}}"
    controls="{{utils.getValue(item.controls, [])}}"
    include-points="{{utils.getValue(item.includePoints, [])}}"
    show-location="{{utils.getValue(item.showLocation, false)}}"
    polygons="{{utils.getValue(item.polygons, [])}}"
    subkey="{{utils.getValue(item.subkey, '')}}"
    setting="{{utils.getValue(item.setting, '', true)}}"
    layer-style="{{utils.getValue(item.layerStyle, 1)}}"
    rotate="0utils.getValue(item.rotate, 0)}}"
    enable-3D="{{utils.getValue(item.enable3D, false)}}"
    show-compass="{{utils.getValue(item.showCompass, false)}}"
    show-scale="{{utils.getValue(item.showScale, false)}}"
    enable-overlooking="{{utils.getValue(item.enableOverlooking, false)}}"
    enable-zoom="{{utils.getValue(item.enableZoom, true)}}"
    enable-scroll="{{utils.getValue(item.scale, true)}}"
    enable-rotate="{{utils.getValue(item.enableRotate, false)}}"
    enable-satellite="{{utils.getValue(item.enableSatellite, false)}}"
    enable-traffic="{{utils.getValue(item.enableTraffic, false)}}"
    enable-poi="{{utils.getValue(item.enablePoi, true)}}"
    enable-building="{{utils.getValue(item.enableBuilding, false)}}"
    bindtap="eventHandler"
    bindmarkertap="eventHandler"
    bindlabeltap="eventHandler"
    bindcontroltap="eventHandler"
    bindcallouttap="eventHandler"
    bindregionchange="eventHandler"
    bindpoitap="eventHandler"
    bindanchorpointtap="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </map>
</template>

<template name="tmpl_2_cover-view">
    <cover-view
      wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
      style="{{item.style}}" class="{{item.class}}"
      hover-class="{{utils.getValue(item.hoverClass,'none')}}"
      hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}"
      hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}"
      hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}"
      animation="{{item.animation}}"
      bindtouchstart="eventHandler"
      bindtouchmove="eventHandler"
      bindtouchend="eventHandler"
      bindtouchcancel="eventHandler"
      bindlongpress="eventHandler"
      bindanimationstart="eventHandler"
      bindanimationiteration="eventHandler"
      bindanimationend="eventHandler"
      bindtransitionend="eventHandler"
      bindtap="eventHandler"
     >
      <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
    </cover-view>
</template>

<template name="tmpl_2_label">
  <label
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    for="{{utils.getValue(item.for, '')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </label>
</template>

<template name="tmpl_2_checkbox">
  <checkbox
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox>
</template>

<template name="tmpl_2_checkbox-group">
  <checkbox-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox-group>
</template>

<template name="tmpl_2_form">
  <form
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    report-submit="{{utils.getValue(item.reportSubmit, false)}}"
    report-submit-timeout="{{utils.getValue(item.reportSubmitTimeout, 0)}}"
    bindsubmit="eventHandler"
    bindreset="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </form>
</template>

<template name="tmpl_2_radio">
  <radio
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio>
</template>

<template name="tmpl_2_radio-group">
  <radio-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio-group>
</template>

<template name="tmpl_2_picker">
  <picker
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    header-text="{{utils.getValue(item.header-text, '')}}"
    mode="{{utils.getValue(item.mode, 'selector')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    range="{{utils.getValue(item.range, [])}}"
    range-key="{{utils.getValue(item.rangeKey, '')}}"
    value="{{utils.getValue(item.value, 0)}}"
    start="{{utils.getValue(item.start, '')}}"
    end="{{utils.getValue(item.end, '')}}"
    fields="{{utils.getValue(item.fields, 'day')}}"
    custom-item="{{utils.getValue(item.customItem, '')}}"
    bindchange="eventHandler"
    bindcancel="eventHandler"
    bindcolumnchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker>
</template>

<template name="tmpl_2_picker-view">
  <picker-view
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, [])}}"
    indicator-style="{{utils.getValue(item.indicatorStyle, '')}}"
    indicator-class="{{utils.getValue(item.indicatorClass, '')}}"
    mask-style="{{utils.getValue(item.maskStyle, '')}}"
    mask-class="{{utils.getValue(item.maskClass, '')}}"
    bindchange="eventHandler"
    bindpickstart="eventHandler"
    bindpickend="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view>
</template>

<template name="tmpl_2_picker-view-column">
  <picker-view-column
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view-column>
</template>

<template name="tmpl_2_slider">
  <slider
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    min="{{utils.getValue(item.min, 0)}}"
    max="{{utils.getValue(item.max, 100)}}"
    step="{{utils.getValue(item.step, 1)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    value="{{utils.getValue(item.value, 0)}}"
    color="{{utils.getValue(item.color, '#e9e9e9')}}"
    selected-color="{{utils.getValue(item.selectedColor, '#1aad19')}}"
    activeColor="{{utils.getValue(item.activeColor, '#1aad19')}}"
    backgroundColor="{{utils.getValue(item.backgroundColor, '#e9e9e9')}}"
    block-size="{{utils.getValue(item.blockSize, 28)}}"
    block-color="{{utils.getValue(item.blockColor, '#ffffff')}}"
    show-value="{{utils.getValue(item.showValue, false)}}"
    bindchanging="eventHandler"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </slider>
</template>

<template name="tmpl_2_textarea">
  <textarea
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    placeholder-style="{{utils.getValue(item.placeholderStyle, '')}}"
    placeholder-class="{{utils.getValue(item.placeholderClass, 'textarea-placeholder')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    maxlength="{{utils.getValue(item.maxlength, 140)}}"
    auto-focus="{{utils.getValue(item.autoFocus, false)}}"
    focus="{{utils.getValue(item.focus, false)}}"
    auto-height="{{utils.getValue(item.autoHeight, false)}}"
    fixed="{{utils.getValue(item.fixed, false)}}"
    cursor-spacing="{{utils.getValue(item.cursorSpacing, 0)}}"
    cursor="{{utils.getValue(item.cursor, -1)}}"
    show-confirm-bar="{{utils.getValue(item.showConfirmBar, true)}}"
    selection-start="{{utils.getValue(item.selectionStart, -1)}}"
    selection-end="{{utils.getValue(item.selectionEnd, -1)}}"
    adjust-position="{{utils.getValue(item.adjustPosition, true)}}"
    hold-keyboard="{{utils.getValue(item.holdKeyboard, false)}}"
    disable-default-padding="{{utils.getValue(item.disableDefaultPadding, false)}}"
    confirm-type="{{utils.getValue(item.confirmType, 'return')}}"
    confirm-hold="{{utils.getValue(item.confirmHold, false)}}"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindlinechange="eventHandler"
    bindinput="eventHandler"
    bindconfirm="eventHandler"
    bindkeyboardheightchange="eventHandler"
  >
  
  </textarea>
</template>

<template name="tmpl_2_editor">
  <editor
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    read-only="{{utils.getValue(item.readOnly, false)}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    show-img-size="{{utils.getValue(item.showImgSize, false)}}"
    show-img-toolbar="{{utils.getValue(item.showImgToolbar, false)}}"
    show-img-resize="{{utils.getValue(item.showImgResize, false)}}"
    bindready="eventHandler"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindinput="eventHandler"
    bindstatuschange="eventHandler"
  >
  
  </editor>
</template>

<template name="tmpl_2_navigator">
  <navigator
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    target="{{utils.getValue(item.target, 'self')}}"
    url="{{utils.getValue(item.url, '')}}"
    open-type="{{utils.getValue(item.openType, 'navigate')}}"
    delta="{{utils.getValue(item.delta, 1)}}"
    app-id="{{utils.getValue(item.appId, '')}}"
    path="{{utils.getValue(item.path, '')}}"
    extra-data="{{utils.getValue(item.extraData, '', true)}}"
    version="{{utils.getValue(item.version, 'release')}}"
    redirect="{{utils.getValue(item.redirect, false)}}"
    short-link="{{utils.getValue(item.shortLink, '')}}"
    hover-class="{{utils.getValue(item.hoverClass, 'navigator-hover')}}"
    hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation, false)}}"
    hover-start-time="{{utils.getValue(item.hoverStartTime, 50)}}"
    hover-stay-time="{{utils.getValue(item.hoverStayTime, 600)}}"
    bindsuccess="eventHandler"
    bindfail="eventHandler"
    bindcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </navigator>
</template>

<template name="tmpl_2_video">
  <video
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{item.src}}"
    duration="{{utils.getValue(item.duration, 0)}}"
    controls="{{utils.getValue(item.controls, true)}}"
    danmu-list="{{utils.getValue(item.danmuList, [])}}"
    danmu-btn="{{utils.getValue(item.danmuBtn, false)}}"
    enable-danmu="{{utils.getValue(item.enableDanmu, false)}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    loop="{{utils.getValue(item.loop, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    initial-time="{{utils.getValue(item.initialTime, 0)}}"
    page-gesture="{{utils.getValue(item.pageGesture, false)}}"
    direction="{{utils.getValue(item.direction, 0)}}"
    show-progress="{{utils.getValue(item.showProgress, true)}}"
    show-fullscreen-btn="{{utils.getValue(item.showFullscreenBtn, true)}}"
    show-play-btn="{{utils.getValue(item.showPlayBtn, true)}}"
    show-center-play-btn="{{utils.getValue(item.showCenterPlayBtn, true)}}"
    enable-progress-gesture="{{utils.getValue(item.enableProgressGesture, true)}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    poster="{{utils.getValue(item.poster, '')}}"
    show-mute-btn="{{utils.getValue(item.showMuteBtn, false)}}"
    title="{{utils.getValue(item.title, '')}}"
    play-btn-position="{{utils.getValue(item.playBtnPosition, 'bottom')}}"
    enable-play-gesture="{{utils.getValue(item.enablePlayGesture, false)}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    vslide-gesture="{{utils.getValue(item.vslideGesture, false)}}"
    vslide-gesture-in-fullscreen="{{utils.getValue(item.vslideGestureInFullscreen, true)}}"
    ad-unit-id="{{item.adUnitId}}"
    poster-for-crawler="{{item.posterForCrawler}}"
    show-casting-button="{{utils.getValue(item.showCastingButton, false)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    picture-in-picture-show-progress="{{utils.getValue(item.pictureInPictureShowProgress, false)}}"
    enable-auto-rotation="{{utils.getValue(item.enableAutoRotation, false)}}"
    show-screen-lock-button="{{utils.getValue(item.showScreenLockButton, false)}}"
    show-snapshot-button="{{utils.getValue(item.showSnapshotButton, false)}}"
    show-background-playback-button="{{utils.getValue(item.showBackgroundPlaybackButton, false)}}"
    background-poster="{{utils.getValue(item.backgroundPoster, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    is-drm="{{utils.getValue(item.isDrm, false)}}"
    provision-url="{{utils.getValue(item.provisionUrl, '')}}"
    certificate-url="{{utils.getValue(item.certificateUrl, '')}}"
    license-url="{{utils.getValue(item.licenseUrl, '')}}"
    bindplay="eventHandler"
    bindpause="eventHandler"
    bindended="eventHandler"
    bindtimeupdate="eventHandler"
    bindfullscreenchange="eventHandler"
    bindwaiting="eventHandler"
    binderror="eventHandler"
    bindprogress="eventHandler"
    bindloadedmetadata="eventHandler"
    bindcontrolstoggle="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
    bindseekcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </video>
</template>

<template name="tmpl_2_camera">
  <camera
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    mode="{{utils.getValue(item.mode, 'normal')}}"
    resolution="{{utils.getValue(item.resolution, 'medium')}}"
    device-position="{{utils.getValue(item.devicePosition, 'back')}}"
    flash="{{utils.getValue(item.flash, 'auto')}}"
    frame-size="{{utils.getValue(item.frameSize, 'medium')}}"
    bindstop="eventHandler"
    binderror="eventHandler"
    bindinitdone="eventHandler"
    bindscancode="eventHandler"
  >
    
  </camera>
</template>

<template name="tmpl_2_live-player">
  <live-player
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{utils.getValue(item.src, '')}}"
    mode="{{utils.getValue(item.mode, 'live')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    min-cache="{{utils.getValue(item.minCache, 1)}}"
    max-cache="{{utils.getValue(item.maxCache, 3)}}"
    sound-mode="{{utils.getValue(item.soundMode, 'speaker')}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    bindstatechange="eventHandler"
    bindfullscreenchange="eventHandler"
    bindnetstatus="eventHandler"
    bindaudiovolumenotify="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-player>
</template>

<template name="tmpl_2_live-pusher">
  <live-pusher
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    url="{{utils.getValue(item.url, '')}}"
    mode="{{utils.getValue(item.mode, 'RTC')}}"
    autopush="{{utils.getValue(item.autopush, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    enable-camera="{{utils.getValue(item.enableCamera, true)}}"
    auto-focus="{{utils.getValue(item.autoFocus, true)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    beauty="{{utils.getValue(item.beauty, 0)}}"
    whiteness="{{utils.getValue(item.whiteness, 0)}}"
    aspect="{{utils.getValue(item.aspect, '9:16')}}"
    min-bitrate="{{utils.getValue(item.minBitrate, 200)}}"
    max-bitrate="{{utils.getValue(item.maxBitrate, 1000)}}"
    audio-quality="{{utils.getValue(item.audioQuality, 'high')}}"
    waiting-image="{{utils.getValue(item.waitingImage, '')}}"
    waiting-image-hash="{{utils.getValue(item.waitingImageHash, '')}}"
    zoom="{{utils.getValue(item.zoom, false)}}"
    device-position="{{utils.getValue(item.devicePosition, 'front')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    mirror="{{utils.getValue(item.mirror, false)}}"
    remote-mirror="{{utils.getValue(item.remoteMirror, false)}}"
    local-mirror="{{utils.getValue(item.localMirror, 'auto')}}"
    audio-reverb-type="{{utils.getValue(item.audioReverbType, 0)}}"
    enable-mic="{{utils.getValue(item.enableMic, true)}}"
    enable-agc="{{utils.getValue(item.enableAgc, false)}}"
    enable-ans="{{utils.getValue(item.enableAns, false)}}"
    audio-volume-type="{{utils.getValue(item.audioVolumeType, 'auto')}}"
    video-width="{{utils.getValue(item.videoWidth, 360)}}"
    video-height="{{utils.getValue(item.videoHeight, 640)}}"
    beauty-style="{{utils.getValue(item.beautyStyle, 'smooth')}}"
    filter="{{utils.getValue(item.filter, 'standard')}}"
    bindstatechange="eventHandler"
    bindnetstatus="eventHandler"
    binderror="eventHandler"
    bindbgmstart="eventHandler"
    bindbgmprogress="eventHandler"
    bindbgmcomplete="eventHandler"
    bindaudiovolumenotify="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-pusher>
</template>

<template name="tmpl_2_canvas">
  <canvas
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    type="{{utils.getValue(item.type, '')}}"
    canvas-id="{{utils.getValue(item.canvasId, '')}}"
    disable-scroll="{{utils.getValue(item.disableScroll, false)}}"
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchend="eventHandler"
    bindtouchcancel="eventHandler"
    bindlongtap="eventHandler"
    binderror="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </canvas>
</template>

<template name="tmpl_2_keyboard-accessory">
  <keyboard-accessory
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </keyboard-accessory>
</template>

      

<template name="container_3"><block wx:for="{{vdoms}}" wx:key="key"><template is="{{utils.getTagName(item, cid)}}" data="{{item: item, cid}}"></template></block></template>

<template name="tmpl_3_view">
  <view wx:key="{{item.key}}" data-data="{{item.dataSet}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_3_pure-view">
  <view wx:key="{{item.key}}" style="{{item.style}}" class="{{item.class}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_3_static-view">
  <view wx:key="{{item.key}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}"  style="{{item.style}}" class="{{item.class}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>


<template name="tmpl_3_text">
  <text wx:key="{{item.key}}" data-data="{{item.dataSet}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}"><template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" /></text>
</template>

<template name="tmpl_3_template">
  <template is="{{item.is}}" data="{{...item.data}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </template>
</template>

<template name="tmpl_3_button">
  <button wx:key="{{item.key}}" data-data="{{item.dataSet}}" size="{{utils.getValue(item.size,'default')}}" type="{{item.type}}" plain="{{utils.getValue(item.plain,false)}}" disabled="{{item.disabled}}" loading="{{utils.getValue(item.loading,false)}}" form-type="{{item.formType}}" open-type="{{item.openType}}" hover-class="{{utils.getValue(item.hoverClass,'button-hover')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,20)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,70)}}" name="{{item.name}}" lang="{{utils.getValue(item.lang,en)}}" session-from="{{item.sessionFrom}}" send-message-title="{{item.sendMessageTitle}}" send-message-path="{{item.sendMessagePath}}" send-message-img="{{item.sendMessageImg}}" app-parameter="{{item.appParameter}}" show-message-card="{{utils.getValue(item.showMessageCard,false)}}" business-id="{{item.businessId}}" bindgetuserinfo="eventHandler" bindcontact="eventHandler" bindgetphonenumber="eventHandler" binderror="eventHandler" bindopensetting="eventHandler" bindlaunchapp="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </button>
</template>

<template name="tmpl_3_input">
  <template is="{{utils.isFocus(item, 'tmpl_3_')}}" data="{{item:item}}" />
</template>

<template name="tmpl_3_input_focus">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" focus="{{utils.getValue(item.focus,false)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_3_input_blur">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_3_text-node"> {{item.content}} </template>

<template name="tmpl_3_navbar">
  <navbar id="{{item.uid}}" title="{{item.title}}" noPlaceholder="{{item.noPlaceholder}}" css="{{item.css}}"></navbar>
</template>

<template name="tmpl_3_keyboard">
  <keyboard id="{{item.uid}}" limit="{{item.limit}}" price="{{item.price}}" bind:change="eventHandler" bind:confirm="eventHandler"></keyboard>
</template>

<template name="tmpl_3_scroll-view">
  <scroll-view wx:key="{{item.key}}" data-data="{{item.dataSet}}" scroll-x="{{utils.getValue(item.scrollX,false)}}" scroll-y="{{utils.getValue(item.scrollY,false)}}" upper-threshold="{{utils.getValue(item.upperThreshold,50)}}" lower-threshold="{{utils.getValue(item.lowerThreshold,50)}}" scroll-top="{{item.scrollTop}}" scroll-left="{{item.scrollLeft}}" scroll-into-view="{{item.scrollIntoView}}" scroll-with-animation="{{utils.getValue(item.scrollWithAnimation,false)}}" enable-back-to-top="{{utils.getValue(item.enableBackToTop,false)}}" bindscrolltoupper="eventHandler" bindscrolltolower="eventHandler" bindscroll="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" enable-flex="{{utils.getValue(item.enableFlex,false)}}" scroll-anchoring="{{utils.getValue(item.scrollAnchoring,false)}}" refresher-enabled="{{utils.getValue(item.refresherEnabled,false)}}" refresher-threshold="{{utils.getValue(item.refresherThreshold,45)}}" refresher-default-style="{{utils.getValue(item.refresherDefaultStyle,'black')}}" refresher-background="{{utils.getValue(item.refresherBackground,'#FFF')}}" refresher-triggered="{{utils.getValue(item.refresherTriggered,false)}}" enhanced="{{utils.getValue(item.enhanced,false)}}" bounces="{{utils.getValue(item.bounces,true)}}" show-scrollbar="{{utils.getValue(item.showScrollbar,true)}}" paging-enabled="{{utils.getValue(item.pagingEnabled,false)}}" fast-deceleration="{{utils.getValue(item.fastDeceleration,false)}}" binddragstart="eventHandler" binddragging="eventHandler" binddragend="eventHandler" bindrefresherpulling="eventHandler" bindrefresherrefresh="eventHandler" bindrefresherrestore="eventHandler" bindrefresherabort="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </scroll-view>
</template>

<template name="tmpl_3_block">
  <block wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </block>
</template>

<template name="tmpl_3_null">
</template>

<template name="tmpl_3_image">
  <image wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" src="{{item.src}}" mode="{{utils.getValue(item.mode,'scaleToFill')}}" lazy-load="{{utils.getValue(item.lazyLoad,false)}}" binderror="eventHandler" bindload="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" webp="{{utils.getValue(item.webp,false)}}" show-menu-by-longpress="{{utils.getValue(item.showMenuByLongpress,false)}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </image>
</template>

<template name="tmpl_3_icon">
  <icon wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" size="{{utils.getValue(item.size, 23)}}" type="{{item.type}}" color="{{utils.getValue(item.color, '')}}" bind:tap="eventHandler"/>
</template>

<template name="tmpl_3_i">
  <i 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    style="{{item.style}}" class="{{item.class}}" 
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchcancel="eventHandler"
    bindtouchend="eventHandler"
    bindtap="eventHandler"
    bindlongpress="eventHandler"
    bindlongtap="eventHandler"
    bindtransitionend="eventHandler"
    bindanimationstart="eventHandler"
    bindanimationiteration="eventHandler"
    bindanimationend="eventHandler"
    bindtouchforcechange="eventHandler"
  />
</template>

<template name="tmpl_3_progress">
  <progress wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" percent="{{utils.getValue(item.percent, 0)}}" color="{{utils.getValue(item.color, '#09BB07')}}" activeColor="{{utils.getValue(item.activeColor, '#09BB07')}}" backgroundColor="{{utils.getValue(item.backgroundColor, '#EBEBEB')}}" active="{{utils.getValue(item.active, false)}}" active-mode="{{utils.getValue(item.activeMode, backwards)}}"  show-info="{{utils.getValue(item.showInfo, false)}}" stroke-width="{{utils.getValue(item.strokeWidth, 6)}}" border-radius="{{util.getValue(item.borderRadius, 0)}}" font-size="{{utils.getValue(item.fontSize, 16)}}" duration="{{utils.getValue(item.duration, 30)}}" bindactiveend="eventHandler"/>
</template>

<template name="tmpl_3_rich-text">
  <rich-text wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" nodes="{{utils.getValue(item.nodes, [])}}" space="{{util.getVaue(item.space, 'ensp')}}" ></rich-text>
</template>

<template name="tmpl_3_swiper">
  <swiper
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"  
    indicator-dots="{{utils.getValue(item.indicatorDots, false)}}"
    indicator-color="{{utils.getValue(item.indicatorcolor, 'rgba(0, 0, 0, .3)')}}"
    indicator-active-color="{{utils.getValue(item.indicatorActiveColor, '#000000')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}" 
    interval="{{utils.getValue(item.interval, 5000)}}" 
    duration="{{utils.getValue(item.duration, 500)}}"
    circular="{{utils.getValue(item.circular, false)}}" 
    vertical="{{utils.getValue(item.vertical, false)}}" 
    previous-margin="{{utils.getValue(item.previousMargin, '0px')}}" 
    next-margin="{{utils.getValue(item.nextMargin, '0px')}}" 
    snap-to-edge="{{utils.getValue(item.snapToEdge, false)}}" 
    display-multiple-items="{{utils.getValue(item.displayMultipleItems, 1)}}" 
    easing-function="{{utils.getValue(item.easingFunction, 'default')}}" 
    bindchange="eventHandler"
    bindtransition="eventHandler"
    bindanimationfinish="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper>
</template>

<template name="tmpl_3_swiper-item">
  <swiper-item wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  item-id="{{utils.getValue(item.itemId, '')}}" skip-hidden-item-layout="{{utils.getValue(item.skipHiddenItemLayout, '')}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper-item>
</template>

<template name="tmpl_3_switch">
  <switch wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  checked="{{utils.getValue(item.checked, false)}}" disabled="{{utils.getValue(item.disabled, false)}}" type="{{utils.getValue(item.type, 'switch')}}" color="{{utils.getValue(item.color, '#04BE02')}}" bindchange="eventHandler"/>
</template>

<template name="tmpl_3_movable-area">
  <movable-area 
    style="{{item.style}}" class="{{item.class}}" 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    scale-area="{{utils.getValue(item.scaleArea, false)}}" 
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-area>
</template>

<template name="tmpl_3_movable-view">
  <movable-view 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}" 
    direction="{{utils.getValue(item.direction, 'none')}}"
    inertia="{{utils.getValue(item.inertia, false)}}"
    out-of-bounds="{{utils.getValue(item.outOfBounds, false)}}"
    x="{{utils.getValue(item.x, '0')}}"
    y="{{utils.getValue(item.y, '0')}}"
    damping="{{utils.getValue(item.damping, 20)}}"
    friction="{{utils.getValue(item.friction, 2)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    scale="{{utils.getValue(item.scale, false)}}"
    scale-min="{{utils.getValue(item.scaleMin, 0.5)}}"
    scale-max="{{utils.getValue(item.scaleMax, 10)}}"
    scale-value="{{utils.getValue(item.scaleValue, 1)}}"
    animation="{{utils.getValue(item.animation, false)}}"
    width="{{utils.getValue(item.width, '10px')}}"
    height="{{utils.getValue(item.height, '10px')}}"
    bindchange="eventHandler"
    bindscale="eventHandler"
    bindhtouchmove="eventHandler"
    bindvtouchmove="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-view>
</template>

<template name="tmpl_3_map">
  <map
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    longitude="{{item.longitude}}"
    latitude="{{item.latitude}}"scale
    scale="{{utils.getValue(item.scale, 16)}}"
    min-scale="{{utils.getValue(item.minScale, 3)}}"
    max-scale="{{utils.getValue(item.maxScale, 20)}}"
    markers="{{utils.getValue(item.markers, [])}}"
    covers="{{utils.getValue(item.covers, [])}}"
    polyline="{{utils.getValue(item.polyline, [])}}"
    circles="{{utils.getValue(item.circles, [])}}"
    controls="{{utils.getValue(item.controls, [])}}"
    include-points="{{utils.getValue(item.includePoints, [])}}"
    show-location="{{utils.getValue(item.showLocation, false)}}"
    polygons="{{utils.getValue(item.polygons, [])}}"
    subkey="{{utils.getValue(item.subkey, '')}}"
    setting="{{utils.getValue(item.setting, '', true)}}"
    layer-style="{{utils.getValue(item.layerStyle, 1)}}"
    rotate="0utils.getValue(item.rotate, 0)}}"
    enable-3D="{{utils.getValue(item.enable3D, false)}}"
    show-compass="{{utils.getValue(item.showCompass, false)}}"
    show-scale="{{utils.getValue(item.showScale, false)}}"
    enable-overlooking="{{utils.getValue(item.enableOverlooking, false)}}"
    enable-zoom="{{utils.getValue(item.enableZoom, true)}}"
    enable-scroll="{{utils.getValue(item.scale, true)}}"
    enable-rotate="{{utils.getValue(item.enableRotate, false)}}"
    enable-satellite="{{utils.getValue(item.enableSatellite, false)}}"
    enable-traffic="{{utils.getValue(item.enableTraffic, false)}}"
    enable-poi="{{utils.getValue(item.enablePoi, true)}}"
    enable-building="{{utils.getValue(item.enableBuilding, false)}}"
    bindtap="eventHandler"
    bindmarkertap="eventHandler"
    bindlabeltap="eventHandler"
    bindcontroltap="eventHandler"
    bindcallouttap="eventHandler"
    bindregionchange="eventHandler"
    bindpoitap="eventHandler"
    bindanchorpointtap="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </map>
</template>

<template name="tmpl_3_cover-view">
    <cover-view
      wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
      style="{{item.style}}" class="{{item.class}}"
      hover-class="{{utils.getValue(item.hoverClass,'none')}}"
      hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}"
      hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}"
      hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}"
      animation="{{item.animation}}"
      bindtouchstart="eventHandler"
      bindtouchmove="eventHandler"
      bindtouchend="eventHandler"
      bindtouchcancel="eventHandler"
      bindlongpress="eventHandler"
      bindanimationstart="eventHandler"
      bindanimationiteration="eventHandler"
      bindanimationend="eventHandler"
      bindtransitionend="eventHandler"
      bindtap="eventHandler"
     >
      <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
    </cover-view>
</template>

<template name="tmpl_3_label">
  <label
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    for="{{utils.getValue(item.for, '')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </label>
</template>

<template name="tmpl_3_checkbox">
  <checkbox
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox>
</template>

<template name="tmpl_3_checkbox-group">
  <checkbox-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox-group>
</template>

<template name="tmpl_3_form">
  <form
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    report-submit="{{utils.getValue(item.reportSubmit, false)}}"
    report-submit-timeout="{{utils.getValue(item.reportSubmitTimeout, 0)}}"
    bindsubmit="eventHandler"
    bindreset="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </form>
</template>

<template name="tmpl_3_radio">
  <radio
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio>
</template>

<template name="tmpl_3_radio-group">
  <radio-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio-group>
</template>

<template name="tmpl_3_picker">
  <picker
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    header-text="{{utils.getValue(item.header-text, '')}}"
    mode="{{utils.getValue(item.mode, 'selector')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    range="{{utils.getValue(item.range, [])}}"
    range-key="{{utils.getValue(item.rangeKey, '')}}"
    value="{{utils.getValue(item.value, 0)}}"
    start="{{utils.getValue(item.start, '')}}"
    end="{{utils.getValue(item.end, '')}}"
    fields="{{utils.getValue(item.fields, 'day')}}"
    custom-item="{{utils.getValue(item.customItem, '')}}"
    bindchange="eventHandler"
    bindcancel="eventHandler"
    bindcolumnchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker>
</template>

<template name="tmpl_3_picker-view">
  <picker-view
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, [])}}"
    indicator-style="{{utils.getValue(item.indicatorStyle, '')}}"
    indicator-class="{{utils.getValue(item.indicatorClass, '')}}"
    mask-style="{{utils.getValue(item.maskStyle, '')}}"
    mask-class="{{utils.getValue(item.maskClass, '')}}"
    bindchange="eventHandler"
    bindpickstart="eventHandler"
    bindpickend="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view>
</template>

<template name="tmpl_3_picker-view-column">
  <picker-view-column
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view-column>
</template>

<template name="tmpl_3_slider">
  <slider
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    min="{{utils.getValue(item.min, 0)}}"
    max="{{utils.getValue(item.max, 100)}}"
    step="{{utils.getValue(item.step, 1)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    value="{{utils.getValue(item.value, 0)}}"
    color="{{utils.getValue(item.color, '#e9e9e9')}}"
    selected-color="{{utils.getValue(item.selectedColor, '#1aad19')}}"
    activeColor="{{utils.getValue(item.activeColor, '#1aad19')}}"
    backgroundColor="{{utils.getValue(item.backgroundColor, '#e9e9e9')}}"
    block-size="{{utils.getValue(item.blockSize, 28)}}"
    block-color="{{utils.getValue(item.blockColor, '#ffffff')}}"
    show-value="{{utils.getValue(item.showValue, false)}}"
    bindchanging="eventHandler"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </slider>
</template>

<template name="tmpl_3_textarea">
  <textarea
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    placeholder-style="{{utils.getValue(item.placeholderStyle, '')}}"
    placeholder-class="{{utils.getValue(item.placeholderClass, 'textarea-placeholder')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    maxlength="{{utils.getValue(item.maxlength, 140)}}"
    auto-focus="{{utils.getValue(item.autoFocus, false)}}"
    focus="{{utils.getValue(item.focus, false)}}"
    auto-height="{{utils.getValue(item.autoHeight, false)}}"
    fixed="{{utils.getValue(item.fixed, false)}}"
    cursor-spacing="{{utils.getValue(item.cursorSpacing, 0)}}"
    cursor="{{utils.getValue(item.cursor, -1)}}"
    show-confirm-bar="{{utils.getValue(item.showConfirmBar, true)}}"
    selection-start="{{utils.getValue(item.selectionStart, -1)}}"
    selection-end="{{utils.getValue(item.selectionEnd, -1)}}"
    adjust-position="{{utils.getValue(item.adjustPosition, true)}}"
    hold-keyboard="{{utils.getValue(item.holdKeyboard, false)}}"
    disable-default-padding="{{utils.getValue(item.disableDefaultPadding, false)}}"
    confirm-type="{{utils.getValue(item.confirmType, 'return')}}"
    confirm-hold="{{utils.getValue(item.confirmHold, false)}}"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindlinechange="eventHandler"
    bindinput="eventHandler"
    bindconfirm="eventHandler"
    bindkeyboardheightchange="eventHandler"
  >
  
  </textarea>
</template>

<template name="tmpl_3_editor">
  <editor
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    read-only="{{utils.getValue(item.readOnly, false)}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    show-img-size="{{utils.getValue(item.showImgSize, false)}}"
    show-img-toolbar="{{utils.getValue(item.showImgToolbar, false)}}"
    show-img-resize="{{utils.getValue(item.showImgResize, false)}}"
    bindready="eventHandler"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindinput="eventHandler"
    bindstatuschange="eventHandler"
  >
  
  </editor>
</template>

<template name="tmpl_3_navigator">
  <navigator
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    target="{{utils.getValue(item.target, 'self')}}"
    url="{{utils.getValue(item.url, '')}}"
    open-type="{{utils.getValue(item.openType, 'navigate')}}"
    delta="{{utils.getValue(item.delta, 1)}}"
    app-id="{{utils.getValue(item.appId, '')}}"
    path="{{utils.getValue(item.path, '')}}"
    extra-data="{{utils.getValue(item.extraData, '', true)}}"
    version="{{utils.getValue(item.version, 'release')}}"
    redirect="{{utils.getValue(item.redirect, false)}}"
    short-link="{{utils.getValue(item.shortLink, '')}}"
    hover-class="{{utils.getValue(item.hoverClass, 'navigator-hover')}}"
    hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation, false)}}"
    hover-start-time="{{utils.getValue(item.hoverStartTime, 50)}}"
    hover-stay-time="{{utils.getValue(item.hoverStayTime, 600)}}"
    bindsuccess="eventHandler"
    bindfail="eventHandler"
    bindcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </navigator>
</template>

<template name="tmpl_3_video">
  <video
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{item.src}}"
    duration="{{utils.getValue(item.duration, 0)}}"
    controls="{{utils.getValue(item.controls, true)}}"
    danmu-list="{{utils.getValue(item.danmuList, [])}}"
    danmu-btn="{{utils.getValue(item.danmuBtn, false)}}"
    enable-danmu="{{utils.getValue(item.enableDanmu, false)}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    loop="{{utils.getValue(item.loop, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    initial-time="{{utils.getValue(item.initialTime, 0)}}"
    page-gesture="{{utils.getValue(item.pageGesture, false)}}"
    direction="{{utils.getValue(item.direction, 0)}}"
    show-progress="{{utils.getValue(item.showProgress, true)}}"
    show-fullscreen-btn="{{utils.getValue(item.showFullscreenBtn, true)}}"
    show-play-btn="{{utils.getValue(item.showPlayBtn, true)}}"
    show-center-play-btn="{{utils.getValue(item.showCenterPlayBtn, true)}}"
    enable-progress-gesture="{{utils.getValue(item.enableProgressGesture, true)}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    poster="{{utils.getValue(item.poster, '')}}"
    show-mute-btn="{{utils.getValue(item.showMuteBtn, false)}}"
    title="{{utils.getValue(item.title, '')}}"
    play-btn-position="{{utils.getValue(item.playBtnPosition, 'bottom')}}"
    enable-play-gesture="{{utils.getValue(item.enablePlayGesture, false)}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    vslide-gesture="{{utils.getValue(item.vslideGesture, false)}}"
    vslide-gesture-in-fullscreen="{{utils.getValue(item.vslideGestureInFullscreen, true)}}"
    ad-unit-id="{{item.adUnitId}}"
    poster-for-crawler="{{item.posterForCrawler}}"
    show-casting-button="{{utils.getValue(item.showCastingButton, false)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    picture-in-picture-show-progress="{{utils.getValue(item.pictureInPictureShowProgress, false)}}"
    enable-auto-rotation="{{utils.getValue(item.enableAutoRotation, false)}}"
    show-screen-lock-button="{{utils.getValue(item.showScreenLockButton, false)}}"
    show-snapshot-button="{{utils.getValue(item.showSnapshotButton, false)}}"
    show-background-playback-button="{{utils.getValue(item.showBackgroundPlaybackButton, false)}}"
    background-poster="{{utils.getValue(item.backgroundPoster, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    is-drm="{{utils.getValue(item.isDrm, false)}}"
    provision-url="{{utils.getValue(item.provisionUrl, '')}}"
    certificate-url="{{utils.getValue(item.certificateUrl, '')}}"
    license-url="{{utils.getValue(item.licenseUrl, '')}}"
    bindplay="eventHandler"
    bindpause="eventHandler"
    bindended="eventHandler"
    bindtimeupdate="eventHandler"
    bindfullscreenchange="eventHandler"
    bindwaiting="eventHandler"
    binderror="eventHandler"
    bindprogress="eventHandler"
    bindloadedmetadata="eventHandler"
    bindcontrolstoggle="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
    bindseekcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </video>
</template>

<template name="tmpl_3_camera">
  <camera
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    mode="{{utils.getValue(item.mode, 'normal')}}"
    resolution="{{utils.getValue(item.resolution, 'medium')}}"
    device-position="{{utils.getValue(item.devicePosition, 'back')}}"
    flash="{{utils.getValue(item.flash, 'auto')}}"
    frame-size="{{utils.getValue(item.frameSize, 'medium')}}"
    bindstop="eventHandler"
    binderror="eventHandler"
    bindinitdone="eventHandler"
    bindscancode="eventHandler"
  >
    
  </camera>
</template>

<template name="tmpl_3_live-player">
  <live-player
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{utils.getValue(item.src, '')}}"
    mode="{{utils.getValue(item.mode, 'live')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    min-cache="{{utils.getValue(item.minCache, 1)}}"
    max-cache="{{utils.getValue(item.maxCache, 3)}}"
    sound-mode="{{utils.getValue(item.soundMode, 'speaker')}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    bindstatechange="eventHandler"
    bindfullscreenchange="eventHandler"
    bindnetstatus="eventHandler"
    bindaudiovolumenotify="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-player>
</template>

<template name="tmpl_3_live-pusher">
  <live-pusher
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    url="{{utils.getValue(item.url, '')}}"
    mode="{{utils.getValue(item.mode, 'RTC')}}"
    autopush="{{utils.getValue(item.autopush, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    enable-camera="{{utils.getValue(item.enableCamera, true)}}"
    auto-focus="{{utils.getValue(item.autoFocus, true)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    beauty="{{utils.getValue(item.beauty, 0)}}"
    whiteness="{{utils.getValue(item.whiteness, 0)}}"
    aspect="{{utils.getValue(item.aspect, '9:16')}}"
    min-bitrate="{{utils.getValue(item.minBitrate, 200)}}"
    max-bitrate="{{utils.getValue(item.maxBitrate, 1000)}}"
    audio-quality="{{utils.getValue(item.audioQuality, 'high')}}"
    waiting-image="{{utils.getValue(item.waitingImage, '')}}"
    waiting-image-hash="{{utils.getValue(item.waitingImageHash, '')}}"
    zoom="{{utils.getValue(item.zoom, false)}}"
    device-position="{{utils.getValue(item.devicePosition, 'front')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    mirror="{{utils.getValue(item.mirror, false)}}"
    remote-mirror="{{utils.getValue(item.remoteMirror, false)}}"
    local-mirror="{{utils.getValue(item.localMirror, 'auto')}}"
    audio-reverb-type="{{utils.getValue(item.audioReverbType, 0)}}"
    enable-mic="{{utils.getValue(item.enableMic, true)}}"
    enable-agc="{{utils.getValue(item.enableAgc, false)}}"
    enable-ans="{{utils.getValue(item.enableAns, false)}}"
    audio-volume-type="{{utils.getValue(item.audioVolumeType, 'auto')}}"
    video-width="{{utils.getValue(item.videoWidth, 360)}}"
    video-height="{{utils.getValue(item.videoHeight, 640)}}"
    beauty-style="{{utils.getValue(item.beautyStyle, 'smooth')}}"
    filter="{{utils.getValue(item.filter, 'standard')}}"
    bindstatechange="eventHandler"
    bindnetstatus="eventHandler"
    binderror="eventHandler"
    bindbgmstart="eventHandler"
    bindbgmprogress="eventHandler"
    bindbgmcomplete="eventHandler"
    bindaudiovolumenotify="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-pusher>
</template>

<template name="tmpl_3_canvas">
  <canvas
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    type="{{utils.getValue(item.type, '')}}"
    canvas-id="{{utils.getValue(item.canvasId, '')}}"
    disable-scroll="{{utils.getValue(item.disableScroll, false)}}"
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchend="eventHandler"
    bindtouchcancel="eventHandler"
    bindlongtap="eventHandler"
    binderror="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </canvas>
</template>

<template name="tmpl_3_keyboard-accessory">
  <keyboard-accessory
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </keyboard-accessory>
</template>

      

<template name="container_4"><block wx:for="{{vdoms}}" wx:key="key"><template is="{{utils.getTagName(item, cid)}}" data="{{item: item, cid}}"></template></block></template>

<template name="tmpl_4_view">
  <view wx:key="{{item.key}}" data-data="{{item.dataSet}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_4_pure-view">
  <view wx:key="{{item.key}}" style="{{item.style}}" class="{{item.class}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_4_static-view">
  <view wx:key="{{item.key}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}"  style="{{item.style}}" class="{{item.class}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>


<template name="tmpl_4_text">
  <text wx:key="{{item.key}}" data-data="{{item.dataSet}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}"><template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" /></text>
</template>

<template name="tmpl_4_template">
  <template is="{{item.is}}" data="{{...item.data}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </template>
</template>

<template name="tmpl_4_button">
  <button wx:key="{{item.key}}" data-data="{{item.dataSet}}" size="{{utils.getValue(item.size,'default')}}" type="{{item.type}}" plain="{{utils.getValue(item.plain,false)}}" disabled="{{item.disabled}}" loading="{{utils.getValue(item.loading,false)}}" form-type="{{item.formType}}" open-type="{{item.openType}}" hover-class="{{utils.getValue(item.hoverClass,'button-hover')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,20)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,70)}}" name="{{item.name}}" lang="{{utils.getValue(item.lang,en)}}" session-from="{{item.sessionFrom}}" send-message-title="{{item.sendMessageTitle}}" send-message-path="{{item.sendMessagePath}}" send-message-img="{{item.sendMessageImg}}" app-parameter="{{item.appParameter}}" show-message-card="{{utils.getValue(item.showMessageCard,false)}}" business-id="{{item.businessId}}" bindgetuserinfo="eventHandler" bindcontact="eventHandler" bindgetphonenumber="eventHandler" binderror="eventHandler" bindopensetting="eventHandler" bindlaunchapp="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </button>
</template>

<template name="tmpl_4_input">
  <template is="{{utils.isFocus(item, 'tmpl_4_')}}" data="{{item:item}}" />
</template>

<template name="tmpl_4_input_focus">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" focus="{{utils.getValue(item.focus,false)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_4_input_blur">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_4_text-node"> {{item.content}} </template>

<template name="tmpl_4_navbar">
  <navbar id="{{item.uid}}" title="{{item.title}}" noPlaceholder="{{item.noPlaceholder}}" css="{{item.css}}"></navbar>
</template>

<template name="tmpl_4_keyboard">
  <keyboard id="{{item.uid}}" limit="{{item.limit}}" price="{{item.price}}" bind:change="eventHandler" bind:confirm="eventHandler"></keyboard>
</template>

<template name="tmpl_4_scroll-view">
  <scroll-view wx:key="{{item.key}}" data-data="{{item.dataSet}}" scroll-x="{{utils.getValue(item.scrollX,false)}}" scroll-y="{{utils.getValue(item.scrollY,false)}}" upper-threshold="{{utils.getValue(item.upperThreshold,50)}}" lower-threshold="{{utils.getValue(item.lowerThreshold,50)}}" scroll-top="{{item.scrollTop}}" scroll-left="{{item.scrollLeft}}" scroll-into-view="{{item.scrollIntoView}}" scroll-with-animation="{{utils.getValue(item.scrollWithAnimation,false)}}" enable-back-to-top="{{utils.getValue(item.enableBackToTop,false)}}" bindscrolltoupper="eventHandler" bindscrolltolower="eventHandler" bindscroll="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" enable-flex="{{utils.getValue(item.enableFlex,false)}}" scroll-anchoring="{{utils.getValue(item.scrollAnchoring,false)}}" refresher-enabled="{{utils.getValue(item.refresherEnabled,false)}}" refresher-threshold="{{utils.getValue(item.refresherThreshold,45)}}" refresher-default-style="{{utils.getValue(item.refresherDefaultStyle,'black')}}" refresher-background="{{utils.getValue(item.refresherBackground,'#FFF')}}" refresher-triggered="{{utils.getValue(item.refresherTriggered,false)}}" enhanced="{{utils.getValue(item.enhanced,false)}}" bounces="{{utils.getValue(item.bounces,true)}}" show-scrollbar="{{utils.getValue(item.showScrollbar,true)}}" paging-enabled="{{utils.getValue(item.pagingEnabled,false)}}" fast-deceleration="{{utils.getValue(item.fastDeceleration,false)}}" binddragstart="eventHandler" binddragging="eventHandler" binddragend="eventHandler" bindrefresherpulling="eventHandler" bindrefresherrefresh="eventHandler" bindrefresherrestore="eventHandler" bindrefresherabort="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </scroll-view>
</template>

<template name="tmpl_4_block">
  <block wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </block>
</template>

<template name="tmpl_4_null">
</template>

<template name="tmpl_4_image">
  <image wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" src="{{item.src}}" mode="{{utils.getValue(item.mode,'scaleToFill')}}" lazy-load="{{utils.getValue(item.lazyLoad,false)}}" binderror="eventHandler" bindload="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" webp="{{utils.getValue(item.webp,false)}}" show-menu-by-longpress="{{utils.getValue(item.showMenuByLongpress,false)}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </image>
</template>

<template name="tmpl_4_icon">
  <icon wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" size="{{utils.getValue(item.size, 23)}}" type="{{item.type}}" color="{{utils.getValue(item.color, '')}}" bind:tap="eventHandler"/>
</template>

<template name="tmpl_4_i">
  <i 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    style="{{item.style}}" class="{{item.class}}" 
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchcancel="eventHandler"
    bindtouchend="eventHandler"
    bindtap="eventHandler"
    bindlongpress="eventHandler"
    bindlongtap="eventHandler"
    bindtransitionend="eventHandler"
    bindanimationstart="eventHandler"
    bindanimationiteration="eventHandler"
    bindanimationend="eventHandler"
    bindtouchforcechange="eventHandler"
  />
</template>

<template name="tmpl_4_progress">
  <progress wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" percent="{{utils.getValue(item.percent, 0)}}" color="{{utils.getValue(item.color, '#09BB07')}}" activeColor="{{utils.getValue(item.activeColor, '#09BB07')}}" backgroundColor="{{utils.getValue(item.backgroundColor, '#EBEBEB')}}" active="{{utils.getValue(item.active, false)}}" active-mode="{{utils.getValue(item.activeMode, backwards)}}"  show-info="{{utils.getValue(item.showInfo, false)}}" stroke-width="{{utils.getValue(item.strokeWidth, 6)}}" border-radius="{{util.getValue(item.borderRadius, 0)}}" font-size="{{utils.getValue(item.fontSize, 16)}}" duration="{{utils.getValue(item.duration, 30)}}" bindactiveend="eventHandler"/>
</template>

<template name="tmpl_4_rich-text">
  <rich-text wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" nodes="{{utils.getValue(item.nodes, [])}}" space="{{util.getVaue(item.space, 'ensp')}}" ></rich-text>
</template>

<template name="tmpl_4_swiper">
  <swiper
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"  
    indicator-dots="{{utils.getValue(item.indicatorDots, false)}}"
    indicator-color="{{utils.getValue(item.indicatorcolor, 'rgba(0, 0, 0, .3)')}}"
    indicator-active-color="{{utils.getValue(item.indicatorActiveColor, '#000000')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}" 
    interval="{{utils.getValue(item.interval, 5000)}}" 
    duration="{{utils.getValue(item.duration, 500)}}"
    circular="{{utils.getValue(item.circular, false)}}" 
    vertical="{{utils.getValue(item.vertical, false)}}" 
    previous-margin="{{utils.getValue(item.previousMargin, '0px')}}" 
    next-margin="{{utils.getValue(item.nextMargin, '0px')}}" 
    snap-to-edge="{{utils.getValue(item.snapToEdge, false)}}" 
    display-multiple-items="{{utils.getValue(item.displayMultipleItems, 1)}}" 
    easing-function="{{utils.getValue(item.easingFunction, 'default')}}" 
    bindchange="eventHandler"
    bindtransition="eventHandler"
    bindanimationfinish="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper>
</template>

<template name="tmpl_4_swiper-item">
  <swiper-item wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  item-id="{{utils.getValue(item.itemId, '')}}" skip-hidden-item-layout="{{utils.getValue(item.skipHiddenItemLayout, '')}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper-item>
</template>

<template name="tmpl_4_switch">
  <switch wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  checked="{{utils.getValue(item.checked, false)}}" disabled="{{utils.getValue(item.disabled, false)}}" type="{{utils.getValue(item.type, 'switch')}}" color="{{utils.getValue(item.color, '#04BE02')}}" bindchange="eventHandler"/>
</template>

<template name="tmpl_4_movable-area">
  <movable-area 
    style="{{item.style}}" class="{{item.class}}" 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    scale-area="{{utils.getValue(item.scaleArea, false)}}" 
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-area>
</template>

<template name="tmpl_4_movable-view">
  <movable-view 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}" 
    direction="{{utils.getValue(item.direction, 'none')}}"
    inertia="{{utils.getValue(item.inertia, false)}}"
    out-of-bounds="{{utils.getValue(item.outOfBounds, false)}}"
    x="{{utils.getValue(item.x, '0')}}"
    y="{{utils.getValue(item.y, '0')}}"
    damping="{{utils.getValue(item.damping, 20)}}"
    friction="{{utils.getValue(item.friction, 2)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    scale="{{utils.getValue(item.scale, false)}}"
    scale-min="{{utils.getValue(item.scaleMin, 0.5)}}"
    scale-max="{{utils.getValue(item.scaleMax, 10)}}"
    scale-value="{{utils.getValue(item.scaleValue, 1)}}"
    animation="{{utils.getValue(item.animation, false)}}"
    width="{{utils.getValue(item.width, '10px')}}"
    height="{{utils.getValue(item.height, '10px')}}"
    bindchange="eventHandler"
    bindscale="eventHandler"
    bindhtouchmove="eventHandler"
    bindvtouchmove="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-view>
</template>

<template name="tmpl_4_map">
  <map
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    longitude="{{item.longitude}}"
    latitude="{{item.latitude}}"scale
    scale="{{utils.getValue(item.scale, 16)}}"
    min-scale="{{utils.getValue(item.minScale, 3)}}"
    max-scale="{{utils.getValue(item.maxScale, 20)}}"
    markers="{{utils.getValue(item.markers, [])}}"
    covers="{{utils.getValue(item.covers, [])}}"
    polyline="{{utils.getValue(item.polyline, [])}}"
    circles="{{utils.getValue(item.circles, [])}}"
    controls="{{utils.getValue(item.controls, [])}}"
    include-points="{{utils.getValue(item.includePoints, [])}}"
    show-location="{{utils.getValue(item.showLocation, false)}}"
    polygons="{{utils.getValue(item.polygons, [])}}"
    subkey="{{utils.getValue(item.subkey, '')}}"
    setting="{{utils.getValue(item.setting, '', true)}}"
    layer-style="{{utils.getValue(item.layerStyle, 1)}}"
    rotate="0utils.getValue(item.rotate, 0)}}"
    enable-3D="{{utils.getValue(item.enable3D, false)}}"
    show-compass="{{utils.getValue(item.showCompass, false)}}"
    show-scale="{{utils.getValue(item.showScale, false)}}"
    enable-overlooking="{{utils.getValue(item.enableOverlooking, false)}}"
    enable-zoom="{{utils.getValue(item.enableZoom, true)}}"
    enable-scroll="{{utils.getValue(item.scale, true)}}"
    enable-rotate="{{utils.getValue(item.enableRotate, false)}}"
    enable-satellite="{{utils.getValue(item.enableSatellite, false)}}"
    enable-traffic="{{utils.getValue(item.enableTraffic, false)}}"
    enable-poi="{{utils.getValue(item.enablePoi, true)}}"
    enable-building="{{utils.getValue(item.enableBuilding, false)}}"
    bindtap="eventHandler"
    bindmarkertap="eventHandler"
    bindlabeltap="eventHandler"
    bindcontroltap="eventHandler"
    bindcallouttap="eventHandler"
    bindregionchange="eventHandler"
    bindpoitap="eventHandler"
    bindanchorpointtap="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </map>
</template>

<template name="tmpl_4_cover-view">
    <cover-view
      wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
      style="{{item.style}}" class="{{item.class}}"
      hover-class="{{utils.getValue(item.hoverClass,'none')}}"
      hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}"
      hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}"
      hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}"
      animation="{{item.animation}}"
      bindtouchstart="eventHandler"
      bindtouchmove="eventHandler"
      bindtouchend="eventHandler"
      bindtouchcancel="eventHandler"
      bindlongpress="eventHandler"
      bindanimationstart="eventHandler"
      bindanimationiteration="eventHandler"
      bindanimationend="eventHandler"
      bindtransitionend="eventHandler"
      bindtap="eventHandler"
     >
      <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
    </cover-view>
</template>

<template name="tmpl_4_label">
  <label
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    for="{{utils.getValue(item.for, '')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </label>
</template>

<template name="tmpl_4_checkbox">
  <checkbox
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox>
</template>

<template name="tmpl_4_checkbox-group">
  <checkbox-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox-group>
</template>

<template name="tmpl_4_form">
  <form
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    report-submit="{{utils.getValue(item.reportSubmit, false)}}"
    report-submit-timeout="{{utils.getValue(item.reportSubmitTimeout, 0)}}"
    bindsubmit="eventHandler"
    bindreset="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </form>
</template>

<template name="tmpl_4_radio">
  <radio
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio>
</template>

<template name="tmpl_4_radio-group">
  <radio-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio-group>
</template>

<template name="tmpl_4_picker">
  <picker
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    header-text="{{utils.getValue(item.header-text, '')}}"
    mode="{{utils.getValue(item.mode, 'selector')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    range="{{utils.getValue(item.range, [])}}"
    range-key="{{utils.getValue(item.rangeKey, '')}}"
    value="{{utils.getValue(item.value, 0)}}"
    start="{{utils.getValue(item.start, '')}}"
    end="{{utils.getValue(item.end, '')}}"
    fields="{{utils.getValue(item.fields, 'day')}}"
    custom-item="{{utils.getValue(item.customItem, '')}}"
    bindchange="eventHandler"
    bindcancel="eventHandler"
    bindcolumnchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker>
</template>

<template name="tmpl_4_picker-view">
  <picker-view
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, [])}}"
    indicator-style="{{utils.getValue(item.indicatorStyle, '')}}"
    indicator-class="{{utils.getValue(item.indicatorClass, '')}}"
    mask-style="{{utils.getValue(item.maskStyle, '')}}"
    mask-class="{{utils.getValue(item.maskClass, '')}}"
    bindchange="eventHandler"
    bindpickstart="eventHandler"
    bindpickend="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view>
</template>

<template name="tmpl_4_picker-view-column">
  <picker-view-column
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view-column>
</template>

<template name="tmpl_4_slider">
  <slider
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    min="{{utils.getValue(item.min, 0)}}"
    max="{{utils.getValue(item.max, 100)}}"
    step="{{utils.getValue(item.step, 1)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    value="{{utils.getValue(item.value, 0)}}"
    color="{{utils.getValue(item.color, '#e9e9e9')}}"
    selected-color="{{utils.getValue(item.selectedColor, '#1aad19')}}"
    activeColor="{{utils.getValue(item.activeColor, '#1aad19')}}"
    backgroundColor="{{utils.getValue(item.backgroundColor, '#e9e9e9')}}"
    block-size="{{utils.getValue(item.blockSize, 28)}}"
    block-color="{{utils.getValue(item.blockColor, '#ffffff')}}"
    show-value="{{utils.getValue(item.showValue, false)}}"
    bindchanging="eventHandler"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </slider>
</template>

<template name="tmpl_4_textarea">
  <textarea
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    placeholder-style="{{utils.getValue(item.placeholderStyle, '')}}"
    placeholder-class="{{utils.getValue(item.placeholderClass, 'textarea-placeholder')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    maxlength="{{utils.getValue(item.maxlength, 140)}}"
    auto-focus="{{utils.getValue(item.autoFocus, false)}}"
    focus="{{utils.getValue(item.focus, false)}}"
    auto-height="{{utils.getValue(item.autoHeight, false)}}"
    fixed="{{utils.getValue(item.fixed, false)}}"
    cursor-spacing="{{utils.getValue(item.cursorSpacing, 0)}}"
    cursor="{{utils.getValue(item.cursor, -1)}}"
    show-confirm-bar="{{utils.getValue(item.showConfirmBar, true)}}"
    selection-start="{{utils.getValue(item.selectionStart, -1)}}"
    selection-end="{{utils.getValue(item.selectionEnd, -1)}}"
    adjust-position="{{utils.getValue(item.adjustPosition, true)}}"
    hold-keyboard="{{utils.getValue(item.holdKeyboard, false)}}"
    disable-default-padding="{{utils.getValue(item.disableDefaultPadding, false)}}"
    confirm-type="{{utils.getValue(item.confirmType, 'return')}}"
    confirm-hold="{{utils.getValue(item.confirmHold, false)}}"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindlinechange="eventHandler"
    bindinput="eventHandler"
    bindconfirm="eventHandler"
    bindkeyboardheightchange="eventHandler"
  >
  
  </textarea>
</template>

<template name="tmpl_4_editor">
  <editor
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    read-only="{{utils.getValue(item.readOnly, false)}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    show-img-size="{{utils.getValue(item.showImgSize, false)}}"
    show-img-toolbar="{{utils.getValue(item.showImgToolbar, false)}}"
    show-img-resize="{{utils.getValue(item.showImgResize, false)}}"
    bindready="eventHandler"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindinput="eventHandler"
    bindstatuschange="eventHandler"
  >
  
  </editor>
</template>

<template name="tmpl_4_navigator">
  <navigator
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    target="{{utils.getValue(item.target, 'self')}}"
    url="{{utils.getValue(item.url, '')}}"
    open-type="{{utils.getValue(item.openType, 'navigate')}}"
    delta="{{utils.getValue(item.delta, 1)}}"
    app-id="{{utils.getValue(item.appId, '')}}"
    path="{{utils.getValue(item.path, '')}}"
    extra-data="{{utils.getValue(item.extraData, '', true)}}"
    version="{{utils.getValue(item.version, 'release')}}"
    redirect="{{utils.getValue(item.redirect, false)}}"
    short-link="{{utils.getValue(item.shortLink, '')}}"
    hover-class="{{utils.getValue(item.hoverClass, 'navigator-hover')}}"
    hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation, false)}}"
    hover-start-time="{{utils.getValue(item.hoverStartTime, 50)}}"
    hover-stay-time="{{utils.getValue(item.hoverStayTime, 600)}}"
    bindsuccess="eventHandler"
    bindfail="eventHandler"
    bindcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </navigator>
</template>

<template name="tmpl_4_video">
  <video
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{item.src}}"
    duration="{{utils.getValue(item.duration, 0)}}"
    controls="{{utils.getValue(item.controls, true)}}"
    danmu-list="{{utils.getValue(item.danmuList, [])}}"
    danmu-btn="{{utils.getValue(item.danmuBtn, false)}}"
    enable-danmu="{{utils.getValue(item.enableDanmu, false)}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    loop="{{utils.getValue(item.loop, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    initial-time="{{utils.getValue(item.initialTime, 0)}}"
    page-gesture="{{utils.getValue(item.pageGesture, false)}}"
    direction="{{utils.getValue(item.direction, 0)}}"
    show-progress="{{utils.getValue(item.showProgress, true)}}"
    show-fullscreen-btn="{{utils.getValue(item.showFullscreenBtn, true)}}"
    show-play-btn="{{utils.getValue(item.showPlayBtn, true)}}"
    show-center-play-btn="{{utils.getValue(item.showCenterPlayBtn, true)}}"
    enable-progress-gesture="{{utils.getValue(item.enableProgressGesture, true)}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    poster="{{utils.getValue(item.poster, '')}}"
    show-mute-btn="{{utils.getValue(item.showMuteBtn, false)}}"
    title="{{utils.getValue(item.title, '')}}"
    play-btn-position="{{utils.getValue(item.playBtnPosition, 'bottom')}}"
    enable-play-gesture="{{utils.getValue(item.enablePlayGesture, false)}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    vslide-gesture="{{utils.getValue(item.vslideGesture, false)}}"
    vslide-gesture-in-fullscreen="{{utils.getValue(item.vslideGestureInFullscreen, true)}}"
    ad-unit-id="{{item.adUnitId}}"
    poster-for-crawler="{{item.posterForCrawler}}"
    show-casting-button="{{utils.getValue(item.showCastingButton, false)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    picture-in-picture-show-progress="{{utils.getValue(item.pictureInPictureShowProgress, false)}}"
    enable-auto-rotation="{{utils.getValue(item.enableAutoRotation, false)}}"
    show-screen-lock-button="{{utils.getValue(item.showScreenLockButton, false)}}"
    show-snapshot-button="{{utils.getValue(item.showSnapshotButton, false)}}"
    show-background-playback-button="{{utils.getValue(item.showBackgroundPlaybackButton, false)}}"
    background-poster="{{utils.getValue(item.backgroundPoster, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    is-drm="{{utils.getValue(item.isDrm, false)}}"
    provision-url="{{utils.getValue(item.provisionUrl, '')}}"
    certificate-url="{{utils.getValue(item.certificateUrl, '')}}"
    license-url="{{utils.getValue(item.licenseUrl, '')}}"
    bindplay="eventHandler"
    bindpause="eventHandler"
    bindended="eventHandler"
    bindtimeupdate="eventHandler"
    bindfullscreenchange="eventHandler"
    bindwaiting="eventHandler"
    binderror="eventHandler"
    bindprogress="eventHandler"
    bindloadedmetadata="eventHandler"
    bindcontrolstoggle="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
    bindseekcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </video>
</template>

<template name="tmpl_4_camera">
  <camera
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    mode="{{utils.getValue(item.mode, 'normal')}}"
    resolution="{{utils.getValue(item.resolution, 'medium')}}"
    device-position="{{utils.getValue(item.devicePosition, 'back')}}"
    flash="{{utils.getValue(item.flash, 'auto')}}"
    frame-size="{{utils.getValue(item.frameSize, 'medium')}}"
    bindstop="eventHandler"
    binderror="eventHandler"
    bindinitdone="eventHandler"
    bindscancode="eventHandler"
  >
    
  </camera>
</template>

<template name="tmpl_4_live-player">
  <live-player
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{utils.getValue(item.src, '')}}"
    mode="{{utils.getValue(item.mode, 'live')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    min-cache="{{utils.getValue(item.minCache, 1)}}"
    max-cache="{{utils.getValue(item.maxCache, 3)}}"
    sound-mode="{{utils.getValue(item.soundMode, 'speaker')}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    bindstatechange="eventHandler"
    bindfullscreenchange="eventHandler"
    bindnetstatus="eventHandler"
    bindaudiovolumenotify="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-player>
</template>

<template name="tmpl_4_live-pusher">
  <live-pusher
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    url="{{utils.getValue(item.url, '')}}"
    mode="{{utils.getValue(item.mode, 'RTC')}}"
    autopush="{{utils.getValue(item.autopush, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    enable-camera="{{utils.getValue(item.enableCamera, true)}}"
    auto-focus="{{utils.getValue(item.autoFocus, true)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    beauty="{{utils.getValue(item.beauty, 0)}}"
    whiteness="{{utils.getValue(item.whiteness, 0)}}"
    aspect="{{utils.getValue(item.aspect, '9:16')}}"
    min-bitrate="{{utils.getValue(item.minBitrate, 200)}}"
    max-bitrate="{{utils.getValue(item.maxBitrate, 1000)}}"
    audio-quality="{{utils.getValue(item.audioQuality, 'high')}}"
    waiting-image="{{utils.getValue(item.waitingImage, '')}}"
    waiting-image-hash="{{utils.getValue(item.waitingImageHash, '')}}"
    zoom="{{utils.getValue(item.zoom, false)}}"
    device-position="{{utils.getValue(item.devicePosition, 'front')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    mirror="{{utils.getValue(item.mirror, false)}}"
    remote-mirror="{{utils.getValue(item.remoteMirror, false)}}"
    local-mirror="{{utils.getValue(item.localMirror, 'auto')}}"
    audio-reverb-type="{{utils.getValue(item.audioReverbType, 0)}}"
    enable-mic="{{utils.getValue(item.enableMic, true)}}"
    enable-agc="{{utils.getValue(item.enableAgc, false)}}"
    enable-ans="{{utils.getValue(item.enableAns, false)}}"
    audio-volume-type="{{utils.getValue(item.audioVolumeType, 'auto')}}"
    video-width="{{utils.getValue(item.videoWidth, 360)}}"
    video-height="{{utils.getValue(item.videoHeight, 640)}}"
    beauty-style="{{utils.getValue(item.beautyStyle, 'smooth')}}"
    filter="{{utils.getValue(item.filter, 'standard')}}"
    bindstatechange="eventHandler"
    bindnetstatus="eventHandler"
    binderror="eventHandler"
    bindbgmstart="eventHandler"
    bindbgmprogress="eventHandler"
    bindbgmcomplete="eventHandler"
    bindaudiovolumenotify="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-pusher>
</template>

<template name="tmpl_4_canvas">
  <canvas
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    type="{{utils.getValue(item.type, '')}}"
    canvas-id="{{utils.getValue(item.canvasId, '')}}"
    disable-scroll="{{utils.getValue(item.disableScroll, false)}}"
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchend="eventHandler"
    bindtouchcancel="eventHandler"
    bindlongtap="eventHandler"
    binderror="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </canvas>
</template>

<template name="tmpl_4_keyboard-accessory">
  <keyboard-accessory
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </keyboard-accessory>
</template>

      

<template name="container_5"><block wx:for="{{vdoms}}" wx:key="key"><template is="{{utils.getTagName(item, cid)}}" data="{{item: item, cid}}"></template></block></template>

<template name="tmpl_5_view">
  <view wx:key="{{item.key}}" data-data="{{item.dataSet}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_5_pure-view">
  <view wx:key="{{item.key}}" style="{{item.style}}" class="{{item.class}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_5_static-view">
  <view wx:key="{{item.key}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}"  style="{{item.style}}" class="{{item.class}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>


<template name="tmpl_5_text">
  <text wx:key="{{item.key}}" data-data="{{item.dataSet}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}"><template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" /></text>
</template>

<template name="tmpl_5_template">
  <template is="{{item.is}}" data="{{...item.data}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </template>
</template>

<template name="tmpl_5_button">
  <button wx:key="{{item.key}}" data-data="{{item.dataSet}}" size="{{utils.getValue(item.size,'default')}}" type="{{item.type}}" plain="{{utils.getValue(item.plain,false)}}" disabled="{{item.disabled}}" loading="{{utils.getValue(item.loading,false)}}" form-type="{{item.formType}}" open-type="{{item.openType}}" hover-class="{{utils.getValue(item.hoverClass,'button-hover')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,20)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,70)}}" name="{{item.name}}" lang="{{utils.getValue(item.lang,en)}}" session-from="{{item.sessionFrom}}" send-message-title="{{item.sendMessageTitle}}" send-message-path="{{item.sendMessagePath}}" send-message-img="{{item.sendMessageImg}}" app-parameter="{{item.appParameter}}" show-message-card="{{utils.getValue(item.showMessageCard,false)}}" business-id="{{item.businessId}}" bindgetuserinfo="eventHandler" bindcontact="eventHandler" bindgetphonenumber="eventHandler" binderror="eventHandler" bindopensetting="eventHandler" bindlaunchapp="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </button>
</template>

<template name="tmpl_5_input">
  <template is="{{utils.isFocus(item, 'tmpl_5_')}}" data="{{item:item}}" />
</template>

<template name="tmpl_5_input_focus">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" focus="{{utils.getValue(item.focus,false)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_5_input_blur">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_5_text-node"> {{item.content}} </template>

<template name="tmpl_5_navbar">
  <navbar id="{{item.uid}}" title="{{item.title}}" noPlaceholder="{{item.noPlaceholder}}" css="{{item.css}}"></navbar>
</template>

<template name="tmpl_5_keyboard">
  <keyboard id="{{item.uid}}" limit="{{item.limit}}" price="{{item.price}}" bind:change="eventHandler" bind:confirm="eventHandler"></keyboard>
</template>

<template name="tmpl_5_scroll-view">
  <scroll-view wx:key="{{item.key}}" data-data="{{item.dataSet}}" scroll-x="{{utils.getValue(item.scrollX,false)}}" scroll-y="{{utils.getValue(item.scrollY,false)}}" upper-threshold="{{utils.getValue(item.upperThreshold,50)}}" lower-threshold="{{utils.getValue(item.lowerThreshold,50)}}" scroll-top="{{item.scrollTop}}" scroll-left="{{item.scrollLeft}}" scroll-into-view="{{item.scrollIntoView}}" scroll-with-animation="{{utils.getValue(item.scrollWithAnimation,false)}}" enable-back-to-top="{{utils.getValue(item.enableBackToTop,false)}}" bindscrolltoupper="eventHandler" bindscrolltolower="eventHandler" bindscroll="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" enable-flex="{{utils.getValue(item.enableFlex,false)}}" scroll-anchoring="{{utils.getValue(item.scrollAnchoring,false)}}" refresher-enabled="{{utils.getValue(item.refresherEnabled,false)}}" refresher-threshold="{{utils.getValue(item.refresherThreshold,45)}}" refresher-default-style="{{utils.getValue(item.refresherDefaultStyle,'black')}}" refresher-background="{{utils.getValue(item.refresherBackground,'#FFF')}}" refresher-triggered="{{utils.getValue(item.refresherTriggered,false)}}" enhanced="{{utils.getValue(item.enhanced,false)}}" bounces="{{utils.getValue(item.bounces,true)}}" show-scrollbar="{{utils.getValue(item.showScrollbar,true)}}" paging-enabled="{{utils.getValue(item.pagingEnabled,false)}}" fast-deceleration="{{utils.getValue(item.fastDeceleration,false)}}" binddragstart="eventHandler" binddragging="eventHandler" binddragend="eventHandler" bindrefresherpulling="eventHandler" bindrefresherrefresh="eventHandler" bindrefresherrestore="eventHandler" bindrefresherabort="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </scroll-view>
</template>

<template name="tmpl_5_block">
  <block wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </block>
</template>

<template name="tmpl_5_null">
</template>

<template name="tmpl_5_image">
  <image wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" src="{{item.src}}" mode="{{utils.getValue(item.mode,'scaleToFill')}}" lazy-load="{{utils.getValue(item.lazyLoad,false)}}" binderror="eventHandler" bindload="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" webp="{{utils.getValue(item.webp,false)}}" show-menu-by-longpress="{{utils.getValue(item.showMenuByLongpress,false)}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </image>
</template>

<template name="tmpl_5_icon">
  <icon wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" size="{{utils.getValue(item.size, 23)}}" type="{{item.type}}" color="{{utils.getValue(item.color, '')}}" bind:tap="eventHandler"/>
</template>

<template name="tmpl_5_i">
  <i 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    style="{{item.style}}" class="{{item.class}}" 
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchcancel="eventHandler"
    bindtouchend="eventHandler"
    bindtap="eventHandler"
    bindlongpress="eventHandler"
    bindlongtap="eventHandler"
    bindtransitionend="eventHandler"
    bindanimationstart="eventHandler"
    bindanimationiteration="eventHandler"
    bindanimationend="eventHandler"
    bindtouchforcechange="eventHandler"
  />
</template>

<template name="tmpl_5_progress">
  <progress wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" percent="{{utils.getValue(item.percent, 0)}}" color="{{utils.getValue(item.color, '#09BB07')}}" activeColor="{{utils.getValue(item.activeColor, '#09BB07')}}" backgroundColor="{{utils.getValue(item.backgroundColor, '#EBEBEB')}}" active="{{utils.getValue(item.active, false)}}" active-mode="{{utils.getValue(item.activeMode, backwards)}}"  show-info="{{utils.getValue(item.showInfo, false)}}" stroke-width="{{utils.getValue(item.strokeWidth, 6)}}" border-radius="{{util.getValue(item.borderRadius, 0)}}" font-size="{{utils.getValue(item.fontSize, 16)}}" duration="{{utils.getValue(item.duration, 30)}}" bindactiveend="eventHandler"/>
</template>

<template name="tmpl_5_rich-text">
  <rich-text wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" nodes="{{utils.getValue(item.nodes, [])}}" space="{{util.getVaue(item.space, 'ensp')}}" ></rich-text>
</template>

<template name="tmpl_5_swiper">
  <swiper
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"  
    indicator-dots="{{utils.getValue(item.indicatorDots, false)}}"
    indicator-color="{{utils.getValue(item.indicatorcolor, 'rgba(0, 0, 0, .3)')}}"
    indicator-active-color="{{utils.getValue(item.indicatorActiveColor, '#000000')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}" 
    interval="{{utils.getValue(item.interval, 5000)}}" 
    duration="{{utils.getValue(item.duration, 500)}}"
    circular="{{utils.getValue(item.circular, false)}}" 
    vertical="{{utils.getValue(item.vertical, false)}}" 
    previous-margin="{{utils.getValue(item.previousMargin, '0px')}}" 
    next-margin="{{utils.getValue(item.nextMargin, '0px')}}" 
    snap-to-edge="{{utils.getValue(item.snapToEdge, false)}}" 
    display-multiple-items="{{utils.getValue(item.displayMultipleItems, 1)}}" 
    easing-function="{{utils.getValue(item.easingFunction, 'default')}}" 
    bindchange="eventHandler"
    bindtransition="eventHandler"
    bindanimationfinish="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper>
</template>

<template name="tmpl_5_swiper-item">
  <swiper-item wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  item-id="{{utils.getValue(item.itemId, '')}}" skip-hidden-item-layout="{{utils.getValue(item.skipHiddenItemLayout, '')}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper-item>
</template>

<template name="tmpl_5_switch">
  <switch wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  checked="{{utils.getValue(item.checked, false)}}" disabled="{{utils.getValue(item.disabled, false)}}" type="{{utils.getValue(item.type, 'switch')}}" color="{{utils.getValue(item.color, '#04BE02')}}" bindchange="eventHandler"/>
</template>

<template name="tmpl_5_movable-area">
  <movable-area 
    style="{{item.style}}" class="{{item.class}}" 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    scale-area="{{utils.getValue(item.scaleArea, false)}}" 
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-area>
</template>

<template name="tmpl_5_movable-view">
  <movable-view 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}" 
    direction="{{utils.getValue(item.direction, 'none')}}"
    inertia="{{utils.getValue(item.inertia, false)}}"
    out-of-bounds="{{utils.getValue(item.outOfBounds, false)}}"
    x="{{utils.getValue(item.x, '0')}}"
    y="{{utils.getValue(item.y, '0')}}"
    damping="{{utils.getValue(item.damping, 20)}}"
    friction="{{utils.getValue(item.friction, 2)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    scale="{{utils.getValue(item.scale, false)}}"
    scale-min="{{utils.getValue(item.scaleMin, 0.5)}}"
    scale-max="{{utils.getValue(item.scaleMax, 10)}}"
    scale-value="{{utils.getValue(item.scaleValue, 1)}}"
    animation="{{utils.getValue(item.animation, false)}}"
    width="{{utils.getValue(item.width, '10px')}}"
    height="{{utils.getValue(item.height, '10px')}}"
    bindchange="eventHandler"
    bindscale="eventHandler"
    bindhtouchmove="eventHandler"
    bindvtouchmove="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-view>
</template>

<template name="tmpl_5_map">
  <map
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    longitude="{{item.longitude}}"
    latitude="{{item.latitude}}"scale
    scale="{{utils.getValue(item.scale, 16)}}"
    min-scale="{{utils.getValue(item.minScale, 3)}}"
    max-scale="{{utils.getValue(item.maxScale, 20)}}"
    markers="{{utils.getValue(item.markers, [])}}"
    covers="{{utils.getValue(item.covers, [])}}"
    polyline="{{utils.getValue(item.polyline, [])}}"
    circles="{{utils.getValue(item.circles, [])}}"
    controls="{{utils.getValue(item.controls, [])}}"
    include-points="{{utils.getValue(item.includePoints, [])}}"
    show-location="{{utils.getValue(item.showLocation, false)}}"
    polygons="{{utils.getValue(item.polygons, [])}}"
    subkey="{{utils.getValue(item.subkey, '')}}"
    setting="{{utils.getValue(item.setting, '', true)}}"
    layer-style="{{utils.getValue(item.layerStyle, 1)}}"
    rotate="0utils.getValue(item.rotate, 0)}}"
    enable-3D="{{utils.getValue(item.enable3D, false)}}"
    show-compass="{{utils.getValue(item.showCompass, false)}}"
    show-scale="{{utils.getValue(item.showScale, false)}}"
    enable-overlooking="{{utils.getValue(item.enableOverlooking, false)}}"
    enable-zoom="{{utils.getValue(item.enableZoom, true)}}"
    enable-scroll="{{utils.getValue(item.scale, true)}}"
    enable-rotate="{{utils.getValue(item.enableRotate, false)}}"
    enable-satellite="{{utils.getValue(item.enableSatellite, false)}}"
    enable-traffic="{{utils.getValue(item.enableTraffic, false)}}"
    enable-poi="{{utils.getValue(item.enablePoi, true)}}"
    enable-building="{{utils.getValue(item.enableBuilding, false)}}"
    bindtap="eventHandler"
    bindmarkertap="eventHandler"
    bindlabeltap="eventHandler"
    bindcontroltap="eventHandler"
    bindcallouttap="eventHandler"
    bindregionchange="eventHandler"
    bindpoitap="eventHandler"
    bindanchorpointtap="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </map>
</template>

<template name="tmpl_5_cover-view">
    <cover-view
      wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
      style="{{item.style}}" class="{{item.class}}"
      hover-class="{{utils.getValue(item.hoverClass,'none')}}"
      hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}"
      hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}"
      hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}"
      animation="{{item.animation}}"
      bindtouchstart="eventHandler"
      bindtouchmove="eventHandler"
      bindtouchend="eventHandler"
      bindtouchcancel="eventHandler"
      bindlongpress="eventHandler"
      bindanimationstart="eventHandler"
      bindanimationiteration="eventHandler"
      bindanimationend="eventHandler"
      bindtransitionend="eventHandler"
      bindtap="eventHandler"
     >
      <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
    </cover-view>
</template>

<template name="tmpl_5_label">
  <label
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    for="{{utils.getValue(item.for, '')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </label>
</template>

<template name="tmpl_5_checkbox">
  <checkbox
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox>
</template>

<template name="tmpl_5_checkbox-group">
  <checkbox-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox-group>
</template>

<template name="tmpl_5_form">
  <form
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    report-submit="{{utils.getValue(item.reportSubmit, false)}}"
    report-submit-timeout="{{utils.getValue(item.reportSubmitTimeout, 0)}}"
    bindsubmit="eventHandler"
    bindreset="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </form>
</template>

<template name="tmpl_5_radio">
  <radio
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio>
</template>

<template name="tmpl_5_radio-group">
  <radio-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio-group>
</template>

<template name="tmpl_5_picker">
  <picker
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    header-text="{{utils.getValue(item.header-text, '')}}"
    mode="{{utils.getValue(item.mode, 'selector')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    range="{{utils.getValue(item.range, [])}}"
    range-key="{{utils.getValue(item.rangeKey, '')}}"
    value="{{utils.getValue(item.value, 0)}}"
    start="{{utils.getValue(item.start, '')}}"
    end="{{utils.getValue(item.end, '')}}"
    fields="{{utils.getValue(item.fields, 'day')}}"
    custom-item="{{utils.getValue(item.customItem, '')}}"
    bindchange="eventHandler"
    bindcancel="eventHandler"
    bindcolumnchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker>
</template>

<template name="tmpl_5_picker-view">
  <picker-view
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, [])}}"
    indicator-style="{{utils.getValue(item.indicatorStyle, '')}}"
    indicator-class="{{utils.getValue(item.indicatorClass, '')}}"
    mask-style="{{utils.getValue(item.maskStyle, '')}}"
    mask-class="{{utils.getValue(item.maskClass, '')}}"
    bindchange="eventHandler"
    bindpickstart="eventHandler"
    bindpickend="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view>
</template>

<template name="tmpl_5_picker-view-column">
  <picker-view-column
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view-column>
</template>

<template name="tmpl_5_slider">
  <slider
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    min="{{utils.getValue(item.min, 0)}}"
    max="{{utils.getValue(item.max, 100)}}"
    step="{{utils.getValue(item.step, 1)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    value="{{utils.getValue(item.value, 0)}}"
    color="{{utils.getValue(item.color, '#e9e9e9')}}"
    selected-color="{{utils.getValue(item.selectedColor, '#1aad19')}}"
    activeColor="{{utils.getValue(item.activeColor, '#1aad19')}}"
    backgroundColor="{{utils.getValue(item.backgroundColor, '#e9e9e9')}}"
    block-size="{{utils.getValue(item.blockSize, 28)}}"
    block-color="{{utils.getValue(item.blockColor, '#ffffff')}}"
    show-value="{{utils.getValue(item.showValue, false)}}"
    bindchanging="eventHandler"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </slider>
</template>

<template name="tmpl_5_textarea">
  <textarea
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    placeholder-style="{{utils.getValue(item.placeholderStyle, '')}}"
    placeholder-class="{{utils.getValue(item.placeholderClass, 'textarea-placeholder')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    maxlength="{{utils.getValue(item.maxlength, 140)}}"
    auto-focus="{{utils.getValue(item.autoFocus, false)}}"
    focus="{{utils.getValue(item.focus, false)}}"
    auto-height="{{utils.getValue(item.autoHeight, false)}}"
    fixed="{{utils.getValue(item.fixed, false)}}"
    cursor-spacing="{{utils.getValue(item.cursorSpacing, 0)}}"
    cursor="{{utils.getValue(item.cursor, -1)}}"
    show-confirm-bar="{{utils.getValue(item.showConfirmBar, true)}}"
    selection-start="{{utils.getValue(item.selectionStart, -1)}}"
    selection-end="{{utils.getValue(item.selectionEnd, -1)}}"
    adjust-position="{{utils.getValue(item.adjustPosition, true)}}"
    hold-keyboard="{{utils.getValue(item.holdKeyboard, false)}}"
    disable-default-padding="{{utils.getValue(item.disableDefaultPadding, false)}}"
    confirm-type="{{utils.getValue(item.confirmType, 'return')}}"
    confirm-hold="{{utils.getValue(item.confirmHold, false)}}"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindlinechange="eventHandler"
    bindinput="eventHandler"
    bindconfirm="eventHandler"
    bindkeyboardheightchange="eventHandler"
  >
  
  </textarea>
</template>

<template name="tmpl_5_editor">
  <editor
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    read-only="{{utils.getValue(item.readOnly, false)}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    show-img-size="{{utils.getValue(item.showImgSize, false)}}"
    show-img-toolbar="{{utils.getValue(item.showImgToolbar, false)}}"
    show-img-resize="{{utils.getValue(item.showImgResize, false)}}"
    bindready="eventHandler"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindinput="eventHandler"
    bindstatuschange="eventHandler"
  >
  
  </editor>
</template>

<template name="tmpl_5_navigator">
  <navigator
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    target="{{utils.getValue(item.target, 'self')}}"
    url="{{utils.getValue(item.url, '')}}"
    open-type="{{utils.getValue(item.openType, 'navigate')}}"
    delta="{{utils.getValue(item.delta, 1)}}"
    app-id="{{utils.getValue(item.appId, '')}}"
    path="{{utils.getValue(item.path, '')}}"
    extra-data="{{utils.getValue(item.extraData, '', true)}}"
    version="{{utils.getValue(item.version, 'release')}}"
    redirect="{{utils.getValue(item.redirect, false)}}"
    short-link="{{utils.getValue(item.shortLink, '')}}"
    hover-class="{{utils.getValue(item.hoverClass, 'navigator-hover')}}"
    hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation, false)}}"
    hover-start-time="{{utils.getValue(item.hoverStartTime, 50)}}"
    hover-stay-time="{{utils.getValue(item.hoverStayTime, 600)}}"
    bindsuccess="eventHandler"
    bindfail="eventHandler"
    bindcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </navigator>
</template>

<template name="tmpl_5_video">
  <video
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{item.src}}"
    duration="{{utils.getValue(item.duration, 0)}}"
    controls="{{utils.getValue(item.controls, true)}}"
    danmu-list="{{utils.getValue(item.danmuList, [])}}"
    danmu-btn="{{utils.getValue(item.danmuBtn, false)}}"
    enable-danmu="{{utils.getValue(item.enableDanmu, false)}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    loop="{{utils.getValue(item.loop, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    initial-time="{{utils.getValue(item.initialTime, 0)}}"
    page-gesture="{{utils.getValue(item.pageGesture, false)}}"
    direction="{{utils.getValue(item.direction, 0)}}"
    show-progress="{{utils.getValue(item.showProgress, true)}}"
    show-fullscreen-btn="{{utils.getValue(item.showFullscreenBtn, true)}}"
    show-play-btn="{{utils.getValue(item.showPlayBtn, true)}}"
    show-center-play-btn="{{utils.getValue(item.showCenterPlayBtn, true)}}"
    enable-progress-gesture="{{utils.getValue(item.enableProgressGesture, true)}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    poster="{{utils.getValue(item.poster, '')}}"
    show-mute-btn="{{utils.getValue(item.showMuteBtn, false)}}"
    title="{{utils.getValue(item.title, '')}}"
    play-btn-position="{{utils.getValue(item.playBtnPosition, 'bottom')}}"
    enable-play-gesture="{{utils.getValue(item.enablePlayGesture, false)}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    vslide-gesture="{{utils.getValue(item.vslideGesture, false)}}"
    vslide-gesture-in-fullscreen="{{utils.getValue(item.vslideGestureInFullscreen, true)}}"
    ad-unit-id="{{item.adUnitId}}"
    poster-for-crawler="{{item.posterForCrawler}}"
    show-casting-button="{{utils.getValue(item.showCastingButton, false)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    picture-in-picture-show-progress="{{utils.getValue(item.pictureInPictureShowProgress, false)}}"
    enable-auto-rotation="{{utils.getValue(item.enableAutoRotation, false)}}"
    show-screen-lock-button="{{utils.getValue(item.showScreenLockButton, false)}}"
    show-snapshot-button="{{utils.getValue(item.showSnapshotButton, false)}}"
    show-background-playback-button="{{utils.getValue(item.showBackgroundPlaybackButton, false)}}"
    background-poster="{{utils.getValue(item.backgroundPoster, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    is-drm="{{utils.getValue(item.isDrm, false)}}"
    provision-url="{{utils.getValue(item.provisionUrl, '')}}"
    certificate-url="{{utils.getValue(item.certificateUrl, '')}}"
    license-url="{{utils.getValue(item.licenseUrl, '')}}"
    bindplay="eventHandler"
    bindpause="eventHandler"
    bindended="eventHandler"
    bindtimeupdate="eventHandler"
    bindfullscreenchange="eventHandler"
    bindwaiting="eventHandler"
    binderror="eventHandler"
    bindprogress="eventHandler"
    bindloadedmetadata="eventHandler"
    bindcontrolstoggle="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
    bindseekcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </video>
</template>

<template name="tmpl_5_camera">
  <camera
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    mode="{{utils.getValue(item.mode, 'normal')}}"
    resolution="{{utils.getValue(item.resolution, 'medium')}}"
    device-position="{{utils.getValue(item.devicePosition, 'back')}}"
    flash="{{utils.getValue(item.flash, 'auto')}}"
    frame-size="{{utils.getValue(item.frameSize, 'medium')}}"
    bindstop="eventHandler"
    binderror="eventHandler"
    bindinitdone="eventHandler"
    bindscancode="eventHandler"
  >
    
  </camera>
</template>

<template name="tmpl_5_live-player">
  <live-player
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{utils.getValue(item.src, '')}}"
    mode="{{utils.getValue(item.mode, 'live')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    min-cache="{{utils.getValue(item.minCache, 1)}}"
    max-cache="{{utils.getValue(item.maxCache, 3)}}"
    sound-mode="{{utils.getValue(item.soundMode, 'speaker')}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    bindstatechange="eventHandler"
    bindfullscreenchange="eventHandler"
    bindnetstatus="eventHandler"
    bindaudiovolumenotify="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-player>
</template>

<template name="tmpl_5_live-pusher">
  <live-pusher
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    url="{{utils.getValue(item.url, '')}}"
    mode="{{utils.getValue(item.mode, 'RTC')}}"
    autopush="{{utils.getValue(item.autopush, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    enable-camera="{{utils.getValue(item.enableCamera, true)}}"
    auto-focus="{{utils.getValue(item.autoFocus, true)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    beauty="{{utils.getValue(item.beauty, 0)}}"
    whiteness="{{utils.getValue(item.whiteness, 0)}}"
    aspect="{{utils.getValue(item.aspect, '9:16')}}"
    min-bitrate="{{utils.getValue(item.minBitrate, 200)}}"
    max-bitrate="{{utils.getValue(item.maxBitrate, 1000)}}"
    audio-quality="{{utils.getValue(item.audioQuality, 'high')}}"
    waiting-image="{{utils.getValue(item.waitingImage, '')}}"
    waiting-image-hash="{{utils.getValue(item.waitingImageHash, '')}}"
    zoom="{{utils.getValue(item.zoom, false)}}"
    device-position="{{utils.getValue(item.devicePosition, 'front')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    mirror="{{utils.getValue(item.mirror, false)}}"
    remote-mirror="{{utils.getValue(item.remoteMirror, false)}}"
    local-mirror="{{utils.getValue(item.localMirror, 'auto')}}"
    audio-reverb-type="{{utils.getValue(item.audioReverbType, 0)}}"
    enable-mic="{{utils.getValue(item.enableMic, true)}}"
    enable-agc="{{utils.getValue(item.enableAgc, false)}}"
    enable-ans="{{utils.getValue(item.enableAns, false)}}"
    audio-volume-type="{{utils.getValue(item.audioVolumeType, 'auto')}}"
    video-width="{{utils.getValue(item.videoWidth, 360)}}"
    video-height="{{utils.getValue(item.videoHeight, 640)}}"
    beauty-style="{{utils.getValue(item.beautyStyle, 'smooth')}}"
    filter="{{utils.getValue(item.filter, 'standard')}}"
    bindstatechange="eventHandler"
    bindnetstatus="eventHandler"
    binderror="eventHandler"
    bindbgmstart="eventHandler"
    bindbgmprogress="eventHandler"
    bindbgmcomplete="eventHandler"
    bindaudiovolumenotify="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-pusher>
</template>

<template name="tmpl_5_canvas">
  <canvas
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    type="{{utils.getValue(item.type, '')}}"
    canvas-id="{{utils.getValue(item.canvasId, '')}}"
    disable-scroll="{{utils.getValue(item.disableScroll, false)}}"
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchend="eventHandler"
    bindtouchcancel="eventHandler"
    bindlongtap="eventHandler"
    binderror="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </canvas>
</template>

<template name="tmpl_5_keyboard-accessory">
  <keyboard-accessory
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </keyboard-accessory>
</template>

      

<template name="container_6"><block wx:for="{{vdoms}}" wx:key="key"><template is="{{utils.getTagName(item, cid)}}" data="{{item: item, cid}}"></template></block></template>

<template name="tmpl_6_view">
  <view wx:key="{{item.key}}" data-data="{{item.dataSet}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_6_pure-view">
  <view wx:key="{{item.key}}" style="{{item.style}}" class="{{item.class}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_6_static-view">
  <view wx:key="{{item.key}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}"  style="{{item.style}}" class="{{item.class}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>


<template name="tmpl_6_text">
  <text wx:key="{{item.key}}" data-data="{{item.dataSet}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}"><template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" /></text>
</template>

<template name="tmpl_6_template">
  <template is="{{item.is}}" data="{{...item.data}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </template>
</template>

<template name="tmpl_6_button">
  <button wx:key="{{item.key}}" data-data="{{item.dataSet}}" size="{{utils.getValue(item.size,'default')}}" type="{{item.type}}" plain="{{utils.getValue(item.plain,false)}}" disabled="{{item.disabled}}" loading="{{utils.getValue(item.loading,false)}}" form-type="{{item.formType}}" open-type="{{item.openType}}" hover-class="{{utils.getValue(item.hoverClass,'button-hover')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,20)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,70)}}" name="{{item.name}}" lang="{{utils.getValue(item.lang,en)}}" session-from="{{item.sessionFrom}}" send-message-title="{{item.sendMessageTitle}}" send-message-path="{{item.sendMessagePath}}" send-message-img="{{item.sendMessageImg}}" app-parameter="{{item.appParameter}}" show-message-card="{{utils.getValue(item.showMessageCard,false)}}" business-id="{{item.businessId}}" bindgetuserinfo="eventHandler" bindcontact="eventHandler" bindgetphonenumber="eventHandler" binderror="eventHandler" bindopensetting="eventHandler" bindlaunchapp="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </button>
</template>

<template name="tmpl_6_input">
  <template is="{{utils.isFocus(item, 'tmpl_6_')}}" data="{{item:item}}" />
</template>

<template name="tmpl_6_input_focus">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" focus="{{utils.getValue(item.focus,false)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_6_input_blur">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_6_text-node"> {{item.content}} </template>

<template name="tmpl_6_navbar">
  <navbar id="{{item.uid}}" title="{{item.title}}" noPlaceholder="{{item.noPlaceholder}}" css="{{item.css}}"></navbar>
</template>

<template name="tmpl_6_keyboard">
  <keyboard id="{{item.uid}}" limit="{{item.limit}}" price="{{item.price}}" bind:change="eventHandler" bind:confirm="eventHandler"></keyboard>
</template>

<template name="tmpl_6_scroll-view">
  <scroll-view wx:key="{{item.key}}" data-data="{{item.dataSet}}" scroll-x="{{utils.getValue(item.scrollX,false)}}" scroll-y="{{utils.getValue(item.scrollY,false)}}" upper-threshold="{{utils.getValue(item.upperThreshold,50)}}" lower-threshold="{{utils.getValue(item.lowerThreshold,50)}}" scroll-top="{{item.scrollTop}}" scroll-left="{{item.scrollLeft}}" scroll-into-view="{{item.scrollIntoView}}" scroll-with-animation="{{utils.getValue(item.scrollWithAnimation,false)}}" enable-back-to-top="{{utils.getValue(item.enableBackToTop,false)}}" bindscrolltoupper="eventHandler" bindscrolltolower="eventHandler" bindscroll="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" enable-flex="{{utils.getValue(item.enableFlex,false)}}" scroll-anchoring="{{utils.getValue(item.scrollAnchoring,false)}}" refresher-enabled="{{utils.getValue(item.refresherEnabled,false)}}" refresher-threshold="{{utils.getValue(item.refresherThreshold,45)}}" refresher-default-style="{{utils.getValue(item.refresherDefaultStyle,'black')}}" refresher-background="{{utils.getValue(item.refresherBackground,'#FFF')}}" refresher-triggered="{{utils.getValue(item.refresherTriggered,false)}}" enhanced="{{utils.getValue(item.enhanced,false)}}" bounces="{{utils.getValue(item.bounces,true)}}" show-scrollbar="{{utils.getValue(item.showScrollbar,true)}}" paging-enabled="{{utils.getValue(item.pagingEnabled,false)}}" fast-deceleration="{{utils.getValue(item.fastDeceleration,false)}}" binddragstart="eventHandler" binddragging="eventHandler" binddragend="eventHandler" bindrefresherpulling="eventHandler" bindrefresherrefresh="eventHandler" bindrefresherrestore="eventHandler" bindrefresherabort="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </scroll-view>
</template>

<template name="tmpl_6_block">
  <block wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </block>
</template>

<template name="tmpl_6_null">
</template>

<template name="tmpl_6_image">
  <image wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" src="{{item.src}}" mode="{{utils.getValue(item.mode,'scaleToFill')}}" lazy-load="{{utils.getValue(item.lazyLoad,false)}}" binderror="eventHandler" bindload="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" webp="{{utils.getValue(item.webp,false)}}" show-menu-by-longpress="{{utils.getValue(item.showMenuByLongpress,false)}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </image>
</template>

<template name="tmpl_6_icon">
  <icon wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" size="{{utils.getValue(item.size, 23)}}" type="{{item.type}}" color="{{utils.getValue(item.color, '')}}" bind:tap="eventHandler"/>
</template>

<template name="tmpl_6_i">
  <i 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    style="{{item.style}}" class="{{item.class}}" 
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchcancel="eventHandler"
    bindtouchend="eventHandler"
    bindtap="eventHandler"
    bindlongpress="eventHandler"
    bindlongtap="eventHandler"
    bindtransitionend="eventHandler"
    bindanimationstart="eventHandler"
    bindanimationiteration="eventHandler"
    bindanimationend="eventHandler"
    bindtouchforcechange="eventHandler"
  />
</template>

<template name="tmpl_6_progress">
  <progress wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" percent="{{utils.getValue(item.percent, 0)}}" color="{{utils.getValue(item.color, '#09BB07')}}" activeColor="{{utils.getValue(item.activeColor, '#09BB07')}}" backgroundColor="{{utils.getValue(item.backgroundColor, '#EBEBEB')}}" active="{{utils.getValue(item.active, false)}}" active-mode="{{utils.getValue(item.activeMode, backwards)}}"  show-info="{{utils.getValue(item.showInfo, false)}}" stroke-width="{{utils.getValue(item.strokeWidth, 6)}}" border-radius="{{util.getValue(item.borderRadius, 0)}}" font-size="{{utils.getValue(item.fontSize, 16)}}" duration="{{utils.getValue(item.duration, 30)}}" bindactiveend="eventHandler"/>
</template>

<template name="tmpl_6_rich-text">
  <rich-text wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" nodes="{{utils.getValue(item.nodes, [])}}" space="{{util.getVaue(item.space, 'ensp')}}" ></rich-text>
</template>

<template name="tmpl_6_swiper">
  <swiper
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"  
    indicator-dots="{{utils.getValue(item.indicatorDots, false)}}"
    indicator-color="{{utils.getValue(item.indicatorcolor, 'rgba(0, 0, 0, .3)')}}"
    indicator-active-color="{{utils.getValue(item.indicatorActiveColor, '#000000')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}" 
    interval="{{utils.getValue(item.interval, 5000)}}" 
    duration="{{utils.getValue(item.duration, 500)}}"
    circular="{{utils.getValue(item.circular, false)}}" 
    vertical="{{utils.getValue(item.vertical, false)}}" 
    previous-margin="{{utils.getValue(item.previousMargin, '0px')}}" 
    next-margin="{{utils.getValue(item.nextMargin, '0px')}}" 
    snap-to-edge="{{utils.getValue(item.snapToEdge, false)}}" 
    display-multiple-items="{{utils.getValue(item.displayMultipleItems, 1)}}" 
    easing-function="{{utils.getValue(item.easingFunction, 'default')}}" 
    bindchange="eventHandler"
    bindtransition="eventHandler"
    bindanimationfinish="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper>
</template>

<template name="tmpl_6_swiper-item">
  <swiper-item wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  item-id="{{utils.getValue(item.itemId, '')}}" skip-hidden-item-layout="{{utils.getValue(item.skipHiddenItemLayout, '')}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper-item>
</template>

<template name="tmpl_6_switch">
  <switch wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  checked="{{utils.getValue(item.checked, false)}}" disabled="{{utils.getValue(item.disabled, false)}}" type="{{utils.getValue(item.type, 'switch')}}" color="{{utils.getValue(item.color, '#04BE02')}}" bindchange="eventHandler"/>
</template>

<template name="tmpl_6_movable-area">
  <movable-area 
    style="{{item.style}}" class="{{item.class}}" 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    scale-area="{{utils.getValue(item.scaleArea, false)}}" 
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-area>
</template>

<template name="tmpl_6_movable-view">
  <movable-view 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}" 
    direction="{{utils.getValue(item.direction, 'none')}}"
    inertia="{{utils.getValue(item.inertia, false)}}"
    out-of-bounds="{{utils.getValue(item.outOfBounds, false)}}"
    x="{{utils.getValue(item.x, '0')}}"
    y="{{utils.getValue(item.y, '0')}}"
    damping="{{utils.getValue(item.damping, 20)}}"
    friction="{{utils.getValue(item.friction, 2)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    scale="{{utils.getValue(item.scale, false)}}"
    scale-min="{{utils.getValue(item.scaleMin, 0.5)}}"
    scale-max="{{utils.getValue(item.scaleMax, 10)}}"
    scale-value="{{utils.getValue(item.scaleValue, 1)}}"
    animation="{{utils.getValue(item.animation, false)}}"
    width="{{utils.getValue(item.width, '10px')}}"
    height="{{utils.getValue(item.height, '10px')}}"
    bindchange="eventHandler"
    bindscale="eventHandler"
    bindhtouchmove="eventHandler"
    bindvtouchmove="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-view>
</template>

<template name="tmpl_6_map">
  <map
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    longitude="{{item.longitude}}"
    latitude="{{item.latitude}}"scale
    scale="{{utils.getValue(item.scale, 16)}}"
    min-scale="{{utils.getValue(item.minScale, 3)}}"
    max-scale="{{utils.getValue(item.maxScale, 20)}}"
    markers="{{utils.getValue(item.markers, [])}}"
    covers="{{utils.getValue(item.covers, [])}}"
    polyline="{{utils.getValue(item.polyline, [])}}"
    circles="{{utils.getValue(item.circles, [])}}"
    controls="{{utils.getValue(item.controls, [])}}"
    include-points="{{utils.getValue(item.includePoints, [])}}"
    show-location="{{utils.getValue(item.showLocation, false)}}"
    polygons="{{utils.getValue(item.polygons, [])}}"
    subkey="{{utils.getValue(item.subkey, '')}}"
    setting="{{utils.getValue(item.setting, '', true)}}"
    layer-style="{{utils.getValue(item.layerStyle, 1)}}"
    rotate="0utils.getValue(item.rotate, 0)}}"
    enable-3D="{{utils.getValue(item.enable3D, false)}}"
    show-compass="{{utils.getValue(item.showCompass, false)}}"
    show-scale="{{utils.getValue(item.showScale, false)}}"
    enable-overlooking="{{utils.getValue(item.enableOverlooking, false)}}"
    enable-zoom="{{utils.getValue(item.enableZoom, true)}}"
    enable-scroll="{{utils.getValue(item.scale, true)}}"
    enable-rotate="{{utils.getValue(item.enableRotate, false)}}"
    enable-satellite="{{utils.getValue(item.enableSatellite, false)}}"
    enable-traffic="{{utils.getValue(item.enableTraffic, false)}}"
    enable-poi="{{utils.getValue(item.enablePoi, true)}}"
    enable-building="{{utils.getValue(item.enableBuilding, false)}}"
    bindtap="eventHandler"
    bindmarkertap="eventHandler"
    bindlabeltap="eventHandler"
    bindcontroltap="eventHandler"
    bindcallouttap="eventHandler"
    bindregionchange="eventHandler"
    bindpoitap="eventHandler"
    bindanchorpointtap="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </map>
</template>

<template name="tmpl_6_cover-view">
    <cover-view
      wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
      style="{{item.style}}" class="{{item.class}}"
      hover-class="{{utils.getValue(item.hoverClass,'none')}}"
      hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}"
      hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}"
      hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}"
      animation="{{item.animation}}"
      bindtouchstart="eventHandler"
      bindtouchmove="eventHandler"
      bindtouchend="eventHandler"
      bindtouchcancel="eventHandler"
      bindlongpress="eventHandler"
      bindanimationstart="eventHandler"
      bindanimationiteration="eventHandler"
      bindanimationend="eventHandler"
      bindtransitionend="eventHandler"
      bindtap="eventHandler"
     >
      <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
    </cover-view>
</template>

<template name="tmpl_6_label">
  <label
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    for="{{utils.getValue(item.for, '')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </label>
</template>

<template name="tmpl_6_checkbox">
  <checkbox
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox>
</template>

<template name="tmpl_6_checkbox-group">
  <checkbox-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox-group>
</template>

<template name="tmpl_6_form">
  <form
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    report-submit="{{utils.getValue(item.reportSubmit, false)}}"
    report-submit-timeout="{{utils.getValue(item.reportSubmitTimeout, 0)}}"
    bindsubmit="eventHandler"
    bindreset="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </form>
</template>

<template name="tmpl_6_radio">
  <radio
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio>
</template>

<template name="tmpl_6_radio-group">
  <radio-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio-group>
</template>

<template name="tmpl_6_picker">
  <picker
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    header-text="{{utils.getValue(item.header-text, '')}}"
    mode="{{utils.getValue(item.mode, 'selector')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    range="{{utils.getValue(item.range, [])}}"
    range-key="{{utils.getValue(item.rangeKey, '')}}"
    value="{{utils.getValue(item.value, 0)}}"
    start="{{utils.getValue(item.start, '')}}"
    end="{{utils.getValue(item.end, '')}}"
    fields="{{utils.getValue(item.fields, 'day')}}"
    custom-item="{{utils.getValue(item.customItem, '')}}"
    bindchange="eventHandler"
    bindcancel="eventHandler"
    bindcolumnchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker>
</template>

<template name="tmpl_6_picker-view">
  <picker-view
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, [])}}"
    indicator-style="{{utils.getValue(item.indicatorStyle, '')}}"
    indicator-class="{{utils.getValue(item.indicatorClass, '')}}"
    mask-style="{{utils.getValue(item.maskStyle, '')}}"
    mask-class="{{utils.getValue(item.maskClass, '')}}"
    bindchange="eventHandler"
    bindpickstart="eventHandler"
    bindpickend="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view>
</template>

<template name="tmpl_6_picker-view-column">
  <picker-view-column
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view-column>
</template>

<template name="tmpl_6_slider">
  <slider
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    min="{{utils.getValue(item.min, 0)}}"
    max="{{utils.getValue(item.max, 100)}}"
    step="{{utils.getValue(item.step, 1)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    value="{{utils.getValue(item.value, 0)}}"
    color="{{utils.getValue(item.color, '#e9e9e9')}}"
    selected-color="{{utils.getValue(item.selectedColor, '#1aad19')}}"
    activeColor="{{utils.getValue(item.activeColor, '#1aad19')}}"
    backgroundColor="{{utils.getValue(item.backgroundColor, '#e9e9e9')}}"
    block-size="{{utils.getValue(item.blockSize, 28)}}"
    block-color="{{utils.getValue(item.blockColor, '#ffffff')}}"
    show-value="{{utils.getValue(item.showValue, false)}}"
    bindchanging="eventHandler"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </slider>
</template>

<template name="tmpl_6_textarea">
  <textarea
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    placeholder-style="{{utils.getValue(item.placeholderStyle, '')}}"
    placeholder-class="{{utils.getValue(item.placeholderClass, 'textarea-placeholder')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    maxlength="{{utils.getValue(item.maxlength, 140)}}"
    auto-focus="{{utils.getValue(item.autoFocus, false)}}"
    focus="{{utils.getValue(item.focus, false)}}"
    auto-height="{{utils.getValue(item.autoHeight, false)}}"
    fixed="{{utils.getValue(item.fixed, false)}}"
    cursor-spacing="{{utils.getValue(item.cursorSpacing, 0)}}"
    cursor="{{utils.getValue(item.cursor, -1)}}"
    show-confirm-bar="{{utils.getValue(item.showConfirmBar, true)}}"
    selection-start="{{utils.getValue(item.selectionStart, -1)}}"
    selection-end="{{utils.getValue(item.selectionEnd, -1)}}"
    adjust-position="{{utils.getValue(item.adjustPosition, true)}}"
    hold-keyboard="{{utils.getValue(item.holdKeyboard, false)}}"
    disable-default-padding="{{utils.getValue(item.disableDefaultPadding, false)}}"
    confirm-type="{{utils.getValue(item.confirmType, 'return')}}"
    confirm-hold="{{utils.getValue(item.confirmHold, false)}}"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindlinechange="eventHandler"
    bindinput="eventHandler"
    bindconfirm="eventHandler"
    bindkeyboardheightchange="eventHandler"
  >
  
  </textarea>
</template>

<template name="tmpl_6_editor">
  <editor
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    read-only="{{utils.getValue(item.readOnly, false)}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    show-img-size="{{utils.getValue(item.showImgSize, false)}}"
    show-img-toolbar="{{utils.getValue(item.showImgToolbar, false)}}"
    show-img-resize="{{utils.getValue(item.showImgResize, false)}}"
    bindready="eventHandler"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindinput="eventHandler"
    bindstatuschange="eventHandler"
  >
  
  </editor>
</template>

<template name="tmpl_6_navigator">
  <navigator
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    target="{{utils.getValue(item.target, 'self')}}"
    url="{{utils.getValue(item.url, '')}}"
    open-type="{{utils.getValue(item.openType, 'navigate')}}"
    delta="{{utils.getValue(item.delta, 1)}}"
    app-id="{{utils.getValue(item.appId, '')}}"
    path="{{utils.getValue(item.path, '')}}"
    extra-data="{{utils.getValue(item.extraData, '', true)}}"
    version="{{utils.getValue(item.version, 'release')}}"
    redirect="{{utils.getValue(item.redirect, false)}}"
    short-link="{{utils.getValue(item.shortLink, '')}}"
    hover-class="{{utils.getValue(item.hoverClass, 'navigator-hover')}}"
    hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation, false)}}"
    hover-start-time="{{utils.getValue(item.hoverStartTime, 50)}}"
    hover-stay-time="{{utils.getValue(item.hoverStayTime, 600)}}"
    bindsuccess="eventHandler"
    bindfail="eventHandler"
    bindcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </navigator>
</template>

<template name="tmpl_6_video">
  <video
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{item.src}}"
    duration="{{utils.getValue(item.duration, 0)}}"
    controls="{{utils.getValue(item.controls, true)}}"
    danmu-list="{{utils.getValue(item.danmuList, [])}}"
    danmu-btn="{{utils.getValue(item.danmuBtn, false)}}"
    enable-danmu="{{utils.getValue(item.enableDanmu, false)}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    loop="{{utils.getValue(item.loop, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    initial-time="{{utils.getValue(item.initialTime, 0)}}"
    page-gesture="{{utils.getValue(item.pageGesture, false)}}"
    direction="{{utils.getValue(item.direction, 0)}}"
    show-progress="{{utils.getValue(item.showProgress, true)}}"
    show-fullscreen-btn="{{utils.getValue(item.showFullscreenBtn, true)}}"
    show-play-btn="{{utils.getValue(item.showPlayBtn, true)}}"
    show-center-play-btn="{{utils.getValue(item.showCenterPlayBtn, true)}}"
    enable-progress-gesture="{{utils.getValue(item.enableProgressGesture, true)}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    poster="{{utils.getValue(item.poster, '')}}"
    show-mute-btn="{{utils.getValue(item.showMuteBtn, false)}}"
    title="{{utils.getValue(item.title, '')}}"
    play-btn-position="{{utils.getValue(item.playBtnPosition, 'bottom')}}"
    enable-play-gesture="{{utils.getValue(item.enablePlayGesture, false)}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    vslide-gesture="{{utils.getValue(item.vslideGesture, false)}}"
    vslide-gesture-in-fullscreen="{{utils.getValue(item.vslideGestureInFullscreen, true)}}"
    ad-unit-id="{{item.adUnitId}}"
    poster-for-crawler="{{item.posterForCrawler}}"
    show-casting-button="{{utils.getValue(item.showCastingButton, false)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    picture-in-picture-show-progress="{{utils.getValue(item.pictureInPictureShowProgress, false)}}"
    enable-auto-rotation="{{utils.getValue(item.enableAutoRotation, false)}}"
    show-screen-lock-button="{{utils.getValue(item.showScreenLockButton, false)}}"
    show-snapshot-button="{{utils.getValue(item.showSnapshotButton, false)}}"
    show-background-playback-button="{{utils.getValue(item.showBackgroundPlaybackButton, false)}}"
    background-poster="{{utils.getValue(item.backgroundPoster, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    is-drm="{{utils.getValue(item.isDrm, false)}}"
    provision-url="{{utils.getValue(item.provisionUrl, '')}}"
    certificate-url="{{utils.getValue(item.certificateUrl, '')}}"
    license-url="{{utils.getValue(item.licenseUrl, '')}}"
    bindplay="eventHandler"
    bindpause="eventHandler"
    bindended="eventHandler"
    bindtimeupdate="eventHandler"
    bindfullscreenchange="eventHandler"
    bindwaiting="eventHandler"
    binderror="eventHandler"
    bindprogress="eventHandler"
    bindloadedmetadata="eventHandler"
    bindcontrolstoggle="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
    bindseekcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </video>
</template>

<template name="tmpl_6_camera">
  <camera
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    mode="{{utils.getValue(item.mode, 'normal')}}"
    resolution="{{utils.getValue(item.resolution, 'medium')}}"
    device-position="{{utils.getValue(item.devicePosition, 'back')}}"
    flash="{{utils.getValue(item.flash, 'auto')}}"
    frame-size="{{utils.getValue(item.frameSize, 'medium')}}"
    bindstop="eventHandler"
    binderror="eventHandler"
    bindinitdone="eventHandler"
    bindscancode="eventHandler"
  >
    
  </camera>
</template>

<template name="tmpl_6_live-player">
  <live-player
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{utils.getValue(item.src, '')}}"
    mode="{{utils.getValue(item.mode, 'live')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    min-cache="{{utils.getValue(item.minCache, 1)}}"
    max-cache="{{utils.getValue(item.maxCache, 3)}}"
    sound-mode="{{utils.getValue(item.soundMode, 'speaker')}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    bindstatechange="eventHandler"
    bindfullscreenchange="eventHandler"
    bindnetstatus="eventHandler"
    bindaudiovolumenotify="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-player>
</template>

<template name="tmpl_6_live-pusher">
  <live-pusher
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    url="{{utils.getValue(item.url, '')}}"
    mode="{{utils.getValue(item.mode, 'RTC')}}"
    autopush="{{utils.getValue(item.autopush, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    enable-camera="{{utils.getValue(item.enableCamera, true)}}"
    auto-focus="{{utils.getValue(item.autoFocus, true)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    beauty="{{utils.getValue(item.beauty, 0)}}"
    whiteness="{{utils.getValue(item.whiteness, 0)}}"
    aspect="{{utils.getValue(item.aspect, '9:16')}}"
    min-bitrate="{{utils.getValue(item.minBitrate, 200)}}"
    max-bitrate="{{utils.getValue(item.maxBitrate, 1000)}}"
    audio-quality="{{utils.getValue(item.audioQuality, 'high')}}"
    waiting-image="{{utils.getValue(item.waitingImage, '')}}"
    waiting-image-hash="{{utils.getValue(item.waitingImageHash, '')}}"
    zoom="{{utils.getValue(item.zoom, false)}}"
    device-position="{{utils.getValue(item.devicePosition, 'front')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    mirror="{{utils.getValue(item.mirror, false)}}"
    remote-mirror="{{utils.getValue(item.remoteMirror, false)}}"
    local-mirror="{{utils.getValue(item.localMirror, 'auto')}}"
    audio-reverb-type="{{utils.getValue(item.audioReverbType, 0)}}"
    enable-mic="{{utils.getValue(item.enableMic, true)}}"
    enable-agc="{{utils.getValue(item.enableAgc, false)}}"
    enable-ans="{{utils.getValue(item.enableAns, false)}}"
    audio-volume-type="{{utils.getValue(item.audioVolumeType, 'auto')}}"
    video-width="{{utils.getValue(item.videoWidth, 360)}}"
    video-height="{{utils.getValue(item.videoHeight, 640)}}"
    beauty-style="{{utils.getValue(item.beautyStyle, 'smooth')}}"
    filter="{{utils.getValue(item.filter, 'standard')}}"
    bindstatechange="eventHandler"
    bindnetstatus="eventHandler"
    binderror="eventHandler"
    bindbgmstart="eventHandler"
    bindbgmprogress="eventHandler"
    bindbgmcomplete="eventHandler"
    bindaudiovolumenotify="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-pusher>
</template>

<template name="tmpl_6_canvas">
  <canvas
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    type="{{utils.getValue(item.type, '')}}"
    canvas-id="{{utils.getValue(item.canvasId, '')}}"
    disable-scroll="{{utils.getValue(item.disableScroll, false)}}"
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchend="eventHandler"
    bindtouchcancel="eventHandler"
    bindlongtap="eventHandler"
    binderror="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </canvas>
</template>

<template name="tmpl_6_keyboard-accessory">
  <keyboard-accessory
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </keyboard-accessory>
</template>

      

<template name="container_7"><block wx:for="{{vdoms}}" wx:key="key"><template is="{{utils.getTagName(item, cid)}}" data="{{item: item, cid}}"></template></block></template>

<template name="tmpl_7_view">
  <view wx:key="{{item.key}}" data-data="{{item.dataSet}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_7_pure-view">
  <view wx:key="{{item.key}}" style="{{item.style}}" class="{{item.class}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_7_static-view">
  <view wx:key="{{item.key}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}"  style="{{item.style}}" class="{{item.class}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>


<template name="tmpl_7_text">
  <text wx:key="{{item.key}}" data-data="{{item.dataSet}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}"><template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" /></text>
</template>

<template name="tmpl_7_template">
  <template is="{{item.is}}" data="{{...item.data}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </template>
</template>

<template name="tmpl_7_button">
  <button wx:key="{{item.key}}" data-data="{{item.dataSet}}" size="{{utils.getValue(item.size,'default')}}" type="{{item.type}}" plain="{{utils.getValue(item.plain,false)}}" disabled="{{item.disabled}}" loading="{{utils.getValue(item.loading,false)}}" form-type="{{item.formType}}" open-type="{{item.openType}}" hover-class="{{utils.getValue(item.hoverClass,'button-hover')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,20)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,70)}}" name="{{item.name}}" lang="{{utils.getValue(item.lang,en)}}" session-from="{{item.sessionFrom}}" send-message-title="{{item.sendMessageTitle}}" send-message-path="{{item.sendMessagePath}}" send-message-img="{{item.sendMessageImg}}" app-parameter="{{item.appParameter}}" show-message-card="{{utils.getValue(item.showMessageCard,false)}}" business-id="{{item.businessId}}" bindgetuserinfo="eventHandler" bindcontact="eventHandler" bindgetphonenumber="eventHandler" binderror="eventHandler" bindopensetting="eventHandler" bindlaunchapp="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </button>
</template>

<template name="tmpl_7_input">
  <template is="{{utils.isFocus(item, 'tmpl_7_')}}" data="{{item:item}}" />
</template>

<template name="tmpl_7_input_focus">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" focus="{{utils.getValue(item.focus,false)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_7_input_blur">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_7_text-node"> {{item.content}} </template>

<template name="tmpl_7_navbar">
  <navbar id="{{item.uid}}" title="{{item.title}}" noPlaceholder="{{item.noPlaceholder}}" css="{{item.css}}"></navbar>
</template>

<template name="tmpl_7_keyboard">
  <keyboard id="{{item.uid}}" limit="{{item.limit}}" price="{{item.price}}" bind:change="eventHandler" bind:confirm="eventHandler"></keyboard>
</template>

<template name="tmpl_7_scroll-view">
  <scroll-view wx:key="{{item.key}}" data-data="{{item.dataSet}}" scroll-x="{{utils.getValue(item.scrollX,false)}}" scroll-y="{{utils.getValue(item.scrollY,false)}}" upper-threshold="{{utils.getValue(item.upperThreshold,50)}}" lower-threshold="{{utils.getValue(item.lowerThreshold,50)}}" scroll-top="{{item.scrollTop}}" scroll-left="{{item.scrollLeft}}" scroll-into-view="{{item.scrollIntoView}}" scroll-with-animation="{{utils.getValue(item.scrollWithAnimation,false)}}" enable-back-to-top="{{utils.getValue(item.enableBackToTop,false)}}" bindscrolltoupper="eventHandler" bindscrolltolower="eventHandler" bindscroll="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" enable-flex="{{utils.getValue(item.enableFlex,false)}}" scroll-anchoring="{{utils.getValue(item.scrollAnchoring,false)}}" refresher-enabled="{{utils.getValue(item.refresherEnabled,false)}}" refresher-threshold="{{utils.getValue(item.refresherThreshold,45)}}" refresher-default-style="{{utils.getValue(item.refresherDefaultStyle,'black')}}" refresher-background="{{utils.getValue(item.refresherBackground,'#FFF')}}" refresher-triggered="{{utils.getValue(item.refresherTriggered,false)}}" enhanced="{{utils.getValue(item.enhanced,false)}}" bounces="{{utils.getValue(item.bounces,true)}}" show-scrollbar="{{utils.getValue(item.showScrollbar,true)}}" paging-enabled="{{utils.getValue(item.pagingEnabled,false)}}" fast-deceleration="{{utils.getValue(item.fastDeceleration,false)}}" binddragstart="eventHandler" binddragging="eventHandler" binddragend="eventHandler" bindrefresherpulling="eventHandler" bindrefresherrefresh="eventHandler" bindrefresherrestore="eventHandler" bindrefresherabort="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </scroll-view>
</template>

<template name="tmpl_7_block">
  <block wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </block>
</template>

<template name="tmpl_7_null">
</template>

<template name="tmpl_7_image">
  <image wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" src="{{item.src}}" mode="{{utils.getValue(item.mode,'scaleToFill')}}" lazy-load="{{utils.getValue(item.lazyLoad,false)}}" binderror="eventHandler" bindload="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" webp="{{utils.getValue(item.webp,false)}}" show-menu-by-longpress="{{utils.getValue(item.showMenuByLongpress,false)}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </image>
</template>

<template name="tmpl_7_icon">
  <icon wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" size="{{utils.getValue(item.size, 23)}}" type="{{item.type}}" color="{{utils.getValue(item.color, '')}}" bind:tap="eventHandler"/>
</template>

<template name="tmpl_7_i">
  <i 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    style="{{item.style}}" class="{{item.class}}" 
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchcancel="eventHandler"
    bindtouchend="eventHandler"
    bindtap="eventHandler"
    bindlongpress="eventHandler"
    bindlongtap="eventHandler"
    bindtransitionend="eventHandler"
    bindanimationstart="eventHandler"
    bindanimationiteration="eventHandler"
    bindanimationend="eventHandler"
    bindtouchforcechange="eventHandler"
  />
</template>

<template name="tmpl_7_progress">
  <progress wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" percent="{{utils.getValue(item.percent, 0)}}" color="{{utils.getValue(item.color, '#09BB07')}}" activeColor="{{utils.getValue(item.activeColor, '#09BB07')}}" backgroundColor="{{utils.getValue(item.backgroundColor, '#EBEBEB')}}" active="{{utils.getValue(item.active, false)}}" active-mode="{{utils.getValue(item.activeMode, backwards)}}"  show-info="{{utils.getValue(item.showInfo, false)}}" stroke-width="{{utils.getValue(item.strokeWidth, 6)}}" border-radius="{{util.getValue(item.borderRadius, 0)}}" font-size="{{utils.getValue(item.fontSize, 16)}}" duration="{{utils.getValue(item.duration, 30)}}" bindactiveend="eventHandler"/>
</template>

<template name="tmpl_7_rich-text">
  <rich-text wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" nodes="{{utils.getValue(item.nodes, [])}}" space="{{util.getVaue(item.space, 'ensp')}}" ></rich-text>
</template>

<template name="tmpl_7_swiper">
  <swiper
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"  
    indicator-dots="{{utils.getValue(item.indicatorDots, false)}}"
    indicator-color="{{utils.getValue(item.indicatorcolor, 'rgba(0, 0, 0, .3)')}}"
    indicator-active-color="{{utils.getValue(item.indicatorActiveColor, '#000000')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}" 
    interval="{{utils.getValue(item.interval, 5000)}}" 
    duration="{{utils.getValue(item.duration, 500)}}"
    circular="{{utils.getValue(item.circular, false)}}" 
    vertical="{{utils.getValue(item.vertical, false)}}" 
    previous-margin="{{utils.getValue(item.previousMargin, '0px')}}" 
    next-margin="{{utils.getValue(item.nextMargin, '0px')}}" 
    snap-to-edge="{{utils.getValue(item.snapToEdge, false)}}" 
    display-multiple-items="{{utils.getValue(item.displayMultipleItems, 1)}}" 
    easing-function="{{utils.getValue(item.easingFunction, 'default')}}" 
    bindchange="eventHandler"
    bindtransition="eventHandler"
    bindanimationfinish="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper>
</template>

<template name="tmpl_7_swiper-item">
  <swiper-item wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  item-id="{{utils.getValue(item.itemId, '')}}" skip-hidden-item-layout="{{utils.getValue(item.skipHiddenItemLayout, '')}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper-item>
</template>

<template name="tmpl_7_switch">
  <switch wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  checked="{{utils.getValue(item.checked, false)}}" disabled="{{utils.getValue(item.disabled, false)}}" type="{{utils.getValue(item.type, 'switch')}}" color="{{utils.getValue(item.color, '#04BE02')}}" bindchange="eventHandler"/>
</template>

<template name="tmpl_7_movable-area">
  <movable-area 
    style="{{item.style}}" class="{{item.class}}" 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    scale-area="{{utils.getValue(item.scaleArea, false)}}" 
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-area>
</template>

<template name="tmpl_7_movable-view">
  <movable-view 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}" 
    direction="{{utils.getValue(item.direction, 'none')}}"
    inertia="{{utils.getValue(item.inertia, false)}}"
    out-of-bounds="{{utils.getValue(item.outOfBounds, false)}}"
    x="{{utils.getValue(item.x, '0')}}"
    y="{{utils.getValue(item.y, '0')}}"
    damping="{{utils.getValue(item.damping, 20)}}"
    friction="{{utils.getValue(item.friction, 2)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    scale="{{utils.getValue(item.scale, false)}}"
    scale-min="{{utils.getValue(item.scaleMin, 0.5)}}"
    scale-max="{{utils.getValue(item.scaleMax, 10)}}"
    scale-value="{{utils.getValue(item.scaleValue, 1)}}"
    animation="{{utils.getValue(item.animation, false)}}"
    width="{{utils.getValue(item.width, '10px')}}"
    height="{{utils.getValue(item.height, '10px')}}"
    bindchange="eventHandler"
    bindscale="eventHandler"
    bindhtouchmove="eventHandler"
    bindvtouchmove="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-view>
</template>

<template name="tmpl_7_map">
  <map
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    longitude="{{item.longitude}}"
    latitude="{{item.latitude}}"scale
    scale="{{utils.getValue(item.scale, 16)}}"
    min-scale="{{utils.getValue(item.minScale, 3)}}"
    max-scale="{{utils.getValue(item.maxScale, 20)}}"
    markers="{{utils.getValue(item.markers, [])}}"
    covers="{{utils.getValue(item.covers, [])}}"
    polyline="{{utils.getValue(item.polyline, [])}}"
    circles="{{utils.getValue(item.circles, [])}}"
    controls="{{utils.getValue(item.controls, [])}}"
    include-points="{{utils.getValue(item.includePoints, [])}}"
    show-location="{{utils.getValue(item.showLocation, false)}}"
    polygons="{{utils.getValue(item.polygons, [])}}"
    subkey="{{utils.getValue(item.subkey, '')}}"
    setting="{{utils.getValue(item.setting, '', true)}}"
    layer-style="{{utils.getValue(item.layerStyle, 1)}}"
    rotate="0utils.getValue(item.rotate, 0)}}"
    enable-3D="{{utils.getValue(item.enable3D, false)}}"
    show-compass="{{utils.getValue(item.showCompass, false)}}"
    show-scale="{{utils.getValue(item.showScale, false)}}"
    enable-overlooking="{{utils.getValue(item.enableOverlooking, false)}}"
    enable-zoom="{{utils.getValue(item.enableZoom, true)}}"
    enable-scroll="{{utils.getValue(item.scale, true)}}"
    enable-rotate="{{utils.getValue(item.enableRotate, false)}}"
    enable-satellite="{{utils.getValue(item.enableSatellite, false)}}"
    enable-traffic="{{utils.getValue(item.enableTraffic, false)}}"
    enable-poi="{{utils.getValue(item.enablePoi, true)}}"
    enable-building="{{utils.getValue(item.enableBuilding, false)}}"
    bindtap="eventHandler"
    bindmarkertap="eventHandler"
    bindlabeltap="eventHandler"
    bindcontroltap="eventHandler"
    bindcallouttap="eventHandler"
    bindregionchange="eventHandler"
    bindpoitap="eventHandler"
    bindanchorpointtap="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </map>
</template>

<template name="tmpl_7_cover-view">
    <cover-view
      wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
      style="{{item.style}}" class="{{item.class}}"
      hover-class="{{utils.getValue(item.hoverClass,'none')}}"
      hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}"
      hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}"
      hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}"
      animation="{{item.animation}}"
      bindtouchstart="eventHandler"
      bindtouchmove="eventHandler"
      bindtouchend="eventHandler"
      bindtouchcancel="eventHandler"
      bindlongpress="eventHandler"
      bindanimationstart="eventHandler"
      bindanimationiteration="eventHandler"
      bindanimationend="eventHandler"
      bindtransitionend="eventHandler"
      bindtap="eventHandler"
     >
      <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
    </cover-view>
</template>

<template name="tmpl_7_label">
  <label
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    for="{{utils.getValue(item.for, '')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </label>
</template>

<template name="tmpl_7_checkbox">
  <checkbox
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox>
</template>

<template name="tmpl_7_checkbox-group">
  <checkbox-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox-group>
</template>

<template name="tmpl_7_form">
  <form
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    report-submit="{{utils.getValue(item.reportSubmit, false)}}"
    report-submit-timeout="{{utils.getValue(item.reportSubmitTimeout, 0)}}"
    bindsubmit="eventHandler"
    bindreset="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </form>
</template>

<template name="tmpl_7_radio">
  <radio
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio>
</template>

<template name="tmpl_7_radio-group">
  <radio-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio-group>
</template>

<template name="tmpl_7_picker">
  <picker
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    header-text="{{utils.getValue(item.header-text, '')}}"
    mode="{{utils.getValue(item.mode, 'selector')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    range="{{utils.getValue(item.range, [])}}"
    range-key="{{utils.getValue(item.rangeKey, '')}}"
    value="{{utils.getValue(item.value, 0)}}"
    start="{{utils.getValue(item.start, '')}}"
    end="{{utils.getValue(item.end, '')}}"
    fields="{{utils.getValue(item.fields, 'day')}}"
    custom-item="{{utils.getValue(item.customItem, '')}}"
    bindchange="eventHandler"
    bindcancel="eventHandler"
    bindcolumnchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker>
</template>

<template name="tmpl_7_picker-view">
  <picker-view
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, [])}}"
    indicator-style="{{utils.getValue(item.indicatorStyle, '')}}"
    indicator-class="{{utils.getValue(item.indicatorClass, '')}}"
    mask-style="{{utils.getValue(item.maskStyle, '')}}"
    mask-class="{{utils.getValue(item.maskClass, '')}}"
    bindchange="eventHandler"
    bindpickstart="eventHandler"
    bindpickend="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view>
</template>

<template name="tmpl_7_picker-view-column">
  <picker-view-column
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view-column>
</template>

<template name="tmpl_7_slider">
  <slider
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    min="{{utils.getValue(item.min, 0)}}"
    max="{{utils.getValue(item.max, 100)}}"
    step="{{utils.getValue(item.step, 1)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    value="{{utils.getValue(item.value, 0)}}"
    color="{{utils.getValue(item.color, '#e9e9e9')}}"
    selected-color="{{utils.getValue(item.selectedColor, '#1aad19')}}"
    activeColor="{{utils.getValue(item.activeColor, '#1aad19')}}"
    backgroundColor="{{utils.getValue(item.backgroundColor, '#e9e9e9')}}"
    block-size="{{utils.getValue(item.blockSize, 28)}}"
    block-color="{{utils.getValue(item.blockColor, '#ffffff')}}"
    show-value="{{utils.getValue(item.showValue, false)}}"
    bindchanging="eventHandler"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </slider>
</template>

<template name="tmpl_7_textarea">
  <textarea
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    placeholder-style="{{utils.getValue(item.placeholderStyle, '')}}"
    placeholder-class="{{utils.getValue(item.placeholderClass, 'textarea-placeholder')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    maxlength="{{utils.getValue(item.maxlength, 140)}}"
    auto-focus="{{utils.getValue(item.autoFocus, false)}}"
    focus="{{utils.getValue(item.focus, false)}}"
    auto-height="{{utils.getValue(item.autoHeight, false)}}"
    fixed="{{utils.getValue(item.fixed, false)}}"
    cursor-spacing="{{utils.getValue(item.cursorSpacing, 0)}}"
    cursor="{{utils.getValue(item.cursor, -1)}}"
    show-confirm-bar="{{utils.getValue(item.showConfirmBar, true)}}"
    selection-start="{{utils.getValue(item.selectionStart, -1)}}"
    selection-end="{{utils.getValue(item.selectionEnd, -1)}}"
    adjust-position="{{utils.getValue(item.adjustPosition, true)}}"
    hold-keyboard="{{utils.getValue(item.holdKeyboard, false)}}"
    disable-default-padding="{{utils.getValue(item.disableDefaultPadding, false)}}"
    confirm-type="{{utils.getValue(item.confirmType, 'return')}}"
    confirm-hold="{{utils.getValue(item.confirmHold, false)}}"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindlinechange="eventHandler"
    bindinput="eventHandler"
    bindconfirm="eventHandler"
    bindkeyboardheightchange="eventHandler"
  >
  
  </textarea>
</template>

<template name="tmpl_7_editor">
  <editor
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    read-only="{{utils.getValue(item.readOnly, false)}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    show-img-size="{{utils.getValue(item.showImgSize, false)}}"
    show-img-toolbar="{{utils.getValue(item.showImgToolbar, false)}}"
    show-img-resize="{{utils.getValue(item.showImgResize, false)}}"
    bindready="eventHandler"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindinput="eventHandler"
    bindstatuschange="eventHandler"
  >
  
  </editor>
</template>

<template name="tmpl_7_navigator">
  <navigator
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    target="{{utils.getValue(item.target, 'self')}}"
    url="{{utils.getValue(item.url, '')}}"
    open-type="{{utils.getValue(item.openType, 'navigate')}}"
    delta="{{utils.getValue(item.delta, 1)}}"
    app-id="{{utils.getValue(item.appId, '')}}"
    path="{{utils.getValue(item.path, '')}}"
    extra-data="{{utils.getValue(item.extraData, '', true)}}"
    version="{{utils.getValue(item.version, 'release')}}"
    redirect="{{utils.getValue(item.redirect, false)}}"
    short-link="{{utils.getValue(item.shortLink, '')}}"
    hover-class="{{utils.getValue(item.hoverClass, 'navigator-hover')}}"
    hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation, false)}}"
    hover-start-time="{{utils.getValue(item.hoverStartTime, 50)}}"
    hover-stay-time="{{utils.getValue(item.hoverStayTime, 600)}}"
    bindsuccess="eventHandler"
    bindfail="eventHandler"
    bindcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </navigator>
</template>

<template name="tmpl_7_video">
  <video
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{item.src}}"
    duration="{{utils.getValue(item.duration, 0)}}"
    controls="{{utils.getValue(item.controls, true)}}"
    danmu-list="{{utils.getValue(item.danmuList, [])}}"
    danmu-btn="{{utils.getValue(item.danmuBtn, false)}}"
    enable-danmu="{{utils.getValue(item.enableDanmu, false)}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    loop="{{utils.getValue(item.loop, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    initial-time="{{utils.getValue(item.initialTime, 0)}}"
    page-gesture="{{utils.getValue(item.pageGesture, false)}}"
    direction="{{utils.getValue(item.direction, 0)}}"
    show-progress="{{utils.getValue(item.showProgress, true)}}"
    show-fullscreen-btn="{{utils.getValue(item.showFullscreenBtn, true)}}"
    show-play-btn="{{utils.getValue(item.showPlayBtn, true)}}"
    show-center-play-btn="{{utils.getValue(item.showCenterPlayBtn, true)}}"
    enable-progress-gesture="{{utils.getValue(item.enableProgressGesture, true)}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    poster="{{utils.getValue(item.poster, '')}}"
    show-mute-btn="{{utils.getValue(item.showMuteBtn, false)}}"
    title="{{utils.getValue(item.title, '')}}"
    play-btn-position="{{utils.getValue(item.playBtnPosition, 'bottom')}}"
    enable-play-gesture="{{utils.getValue(item.enablePlayGesture, false)}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    vslide-gesture="{{utils.getValue(item.vslideGesture, false)}}"
    vslide-gesture-in-fullscreen="{{utils.getValue(item.vslideGestureInFullscreen, true)}}"
    ad-unit-id="{{item.adUnitId}}"
    poster-for-crawler="{{item.posterForCrawler}}"
    show-casting-button="{{utils.getValue(item.showCastingButton, false)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    picture-in-picture-show-progress="{{utils.getValue(item.pictureInPictureShowProgress, false)}}"
    enable-auto-rotation="{{utils.getValue(item.enableAutoRotation, false)}}"
    show-screen-lock-button="{{utils.getValue(item.showScreenLockButton, false)}}"
    show-snapshot-button="{{utils.getValue(item.showSnapshotButton, false)}}"
    show-background-playback-button="{{utils.getValue(item.showBackgroundPlaybackButton, false)}}"
    background-poster="{{utils.getValue(item.backgroundPoster, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    is-drm="{{utils.getValue(item.isDrm, false)}}"
    provision-url="{{utils.getValue(item.provisionUrl, '')}}"
    certificate-url="{{utils.getValue(item.certificateUrl, '')}}"
    license-url="{{utils.getValue(item.licenseUrl, '')}}"
    bindplay="eventHandler"
    bindpause="eventHandler"
    bindended="eventHandler"
    bindtimeupdate="eventHandler"
    bindfullscreenchange="eventHandler"
    bindwaiting="eventHandler"
    binderror="eventHandler"
    bindprogress="eventHandler"
    bindloadedmetadata="eventHandler"
    bindcontrolstoggle="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
    bindseekcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </video>
</template>

<template name="tmpl_7_camera">
  <camera
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    mode="{{utils.getValue(item.mode, 'normal')}}"
    resolution="{{utils.getValue(item.resolution, 'medium')}}"
    device-position="{{utils.getValue(item.devicePosition, 'back')}}"
    flash="{{utils.getValue(item.flash, 'auto')}}"
    frame-size="{{utils.getValue(item.frameSize, 'medium')}}"
    bindstop="eventHandler"
    binderror="eventHandler"
    bindinitdone="eventHandler"
    bindscancode="eventHandler"
  >
    
  </camera>
</template>

<template name="tmpl_7_live-player">
  <live-player
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{utils.getValue(item.src, '')}}"
    mode="{{utils.getValue(item.mode, 'live')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    min-cache="{{utils.getValue(item.minCache, 1)}}"
    max-cache="{{utils.getValue(item.maxCache, 3)}}"
    sound-mode="{{utils.getValue(item.soundMode, 'speaker')}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    bindstatechange="eventHandler"
    bindfullscreenchange="eventHandler"
    bindnetstatus="eventHandler"
    bindaudiovolumenotify="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-player>
</template>

<template name="tmpl_7_live-pusher">
  <live-pusher
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    url="{{utils.getValue(item.url, '')}}"
    mode="{{utils.getValue(item.mode, 'RTC')}}"
    autopush="{{utils.getValue(item.autopush, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    enable-camera="{{utils.getValue(item.enableCamera, true)}}"
    auto-focus="{{utils.getValue(item.autoFocus, true)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    beauty="{{utils.getValue(item.beauty, 0)}}"
    whiteness="{{utils.getValue(item.whiteness, 0)}}"
    aspect="{{utils.getValue(item.aspect, '9:16')}}"
    min-bitrate="{{utils.getValue(item.minBitrate, 200)}}"
    max-bitrate="{{utils.getValue(item.maxBitrate, 1000)}}"
    audio-quality="{{utils.getValue(item.audioQuality, 'high')}}"
    waiting-image="{{utils.getValue(item.waitingImage, '')}}"
    waiting-image-hash="{{utils.getValue(item.waitingImageHash, '')}}"
    zoom="{{utils.getValue(item.zoom, false)}}"
    device-position="{{utils.getValue(item.devicePosition, 'front')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    mirror="{{utils.getValue(item.mirror, false)}}"
    remote-mirror="{{utils.getValue(item.remoteMirror, false)}}"
    local-mirror="{{utils.getValue(item.localMirror, 'auto')}}"
    audio-reverb-type="{{utils.getValue(item.audioReverbType, 0)}}"
    enable-mic="{{utils.getValue(item.enableMic, true)}}"
    enable-agc="{{utils.getValue(item.enableAgc, false)}}"
    enable-ans="{{utils.getValue(item.enableAns, false)}}"
    audio-volume-type="{{utils.getValue(item.audioVolumeType, 'auto')}}"
    video-width="{{utils.getValue(item.videoWidth, 360)}}"
    video-height="{{utils.getValue(item.videoHeight, 640)}}"
    beauty-style="{{utils.getValue(item.beautyStyle, 'smooth')}}"
    filter="{{utils.getValue(item.filter, 'standard')}}"
    bindstatechange="eventHandler"
    bindnetstatus="eventHandler"
    binderror="eventHandler"
    bindbgmstart="eventHandler"
    bindbgmprogress="eventHandler"
    bindbgmcomplete="eventHandler"
    bindaudiovolumenotify="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-pusher>
</template>

<template name="tmpl_7_canvas">
  <canvas
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    type="{{utils.getValue(item.type, '')}}"
    canvas-id="{{utils.getValue(item.canvasId, '')}}"
    disable-scroll="{{utils.getValue(item.disableScroll, false)}}"
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchend="eventHandler"
    bindtouchcancel="eventHandler"
    bindlongtap="eventHandler"
    binderror="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </canvas>
</template>

<template name="tmpl_7_keyboard-accessory">
  <keyboard-accessory
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </keyboard-accessory>
</template>

      

<template name="container_8"><block wx:for="{{vdoms}}" wx:key="key"><template is="{{utils.getTagName(item, cid)}}" data="{{item: item, cid}}"></template></block></template>

<template name="tmpl_8_view">
  <view wx:key="{{item.key}}" data-data="{{item.dataSet}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_8_pure-view">
  <view wx:key="{{item.key}}" style="{{item.style}}" class="{{item.class}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_8_static-view">
  <view wx:key="{{item.key}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}"  style="{{item.style}}" class="{{item.class}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>


<template name="tmpl_8_text">
  <text wx:key="{{item.key}}" data-data="{{item.dataSet}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}"><template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" /></text>
</template>

<template name="tmpl_8_template">
  <template is="{{item.is}}" data="{{...item.data}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </template>
</template>

<template name="tmpl_8_button">
  <button wx:key="{{item.key}}" data-data="{{item.dataSet}}" size="{{utils.getValue(item.size,'default')}}" type="{{item.type}}" plain="{{utils.getValue(item.plain,false)}}" disabled="{{item.disabled}}" loading="{{utils.getValue(item.loading,false)}}" form-type="{{item.formType}}" open-type="{{item.openType}}" hover-class="{{utils.getValue(item.hoverClass,'button-hover')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,20)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,70)}}" name="{{item.name}}" lang="{{utils.getValue(item.lang,en)}}" session-from="{{item.sessionFrom}}" send-message-title="{{item.sendMessageTitle}}" send-message-path="{{item.sendMessagePath}}" send-message-img="{{item.sendMessageImg}}" app-parameter="{{item.appParameter}}" show-message-card="{{utils.getValue(item.showMessageCard,false)}}" business-id="{{item.businessId}}" bindgetuserinfo="eventHandler" bindcontact="eventHandler" bindgetphonenumber="eventHandler" binderror="eventHandler" bindopensetting="eventHandler" bindlaunchapp="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </button>
</template>

<template name="tmpl_8_input">
  <template is="{{utils.isFocus(item, 'tmpl_8_')}}" data="{{item:item}}" />
</template>

<template name="tmpl_8_input_focus">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" focus="{{utils.getValue(item.focus,false)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_8_input_blur">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_8_text-node"> {{item.content}} </template>

<template name="tmpl_8_navbar">
  <navbar id="{{item.uid}}" title="{{item.title}}" noPlaceholder="{{item.noPlaceholder}}" css="{{item.css}}"></navbar>
</template>

<template name="tmpl_8_keyboard">
  <keyboard id="{{item.uid}}" limit="{{item.limit}}" price="{{item.price}}" bind:change="eventHandler" bind:confirm="eventHandler"></keyboard>
</template>

<template name="tmpl_8_scroll-view">
  <scroll-view wx:key="{{item.key}}" data-data="{{item.dataSet}}" scroll-x="{{utils.getValue(item.scrollX,false)}}" scroll-y="{{utils.getValue(item.scrollY,false)}}" upper-threshold="{{utils.getValue(item.upperThreshold,50)}}" lower-threshold="{{utils.getValue(item.lowerThreshold,50)}}" scroll-top="{{item.scrollTop}}" scroll-left="{{item.scrollLeft}}" scroll-into-view="{{item.scrollIntoView}}" scroll-with-animation="{{utils.getValue(item.scrollWithAnimation,false)}}" enable-back-to-top="{{utils.getValue(item.enableBackToTop,false)}}" bindscrolltoupper="eventHandler" bindscrolltolower="eventHandler" bindscroll="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" enable-flex="{{utils.getValue(item.enableFlex,false)}}" scroll-anchoring="{{utils.getValue(item.scrollAnchoring,false)}}" refresher-enabled="{{utils.getValue(item.refresherEnabled,false)}}" refresher-threshold="{{utils.getValue(item.refresherThreshold,45)}}" refresher-default-style="{{utils.getValue(item.refresherDefaultStyle,'black')}}" refresher-background="{{utils.getValue(item.refresherBackground,'#FFF')}}" refresher-triggered="{{utils.getValue(item.refresherTriggered,false)}}" enhanced="{{utils.getValue(item.enhanced,false)}}" bounces="{{utils.getValue(item.bounces,true)}}" show-scrollbar="{{utils.getValue(item.showScrollbar,true)}}" paging-enabled="{{utils.getValue(item.pagingEnabled,false)}}" fast-deceleration="{{utils.getValue(item.fastDeceleration,false)}}" binddragstart="eventHandler" binddragging="eventHandler" binddragend="eventHandler" bindrefresherpulling="eventHandler" bindrefresherrefresh="eventHandler" bindrefresherrestore="eventHandler" bindrefresherabort="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </scroll-view>
</template>

<template name="tmpl_8_block">
  <block wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </block>
</template>

<template name="tmpl_8_null">
</template>

<template name="tmpl_8_image">
  <image wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" src="{{item.src}}" mode="{{utils.getValue(item.mode,'scaleToFill')}}" lazy-load="{{utils.getValue(item.lazyLoad,false)}}" binderror="eventHandler" bindload="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" webp="{{utils.getValue(item.webp,false)}}" show-menu-by-longpress="{{utils.getValue(item.showMenuByLongpress,false)}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </image>
</template>

<template name="tmpl_8_icon">
  <icon wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" size="{{utils.getValue(item.size, 23)}}" type="{{item.type}}" color="{{utils.getValue(item.color, '')}}" bind:tap="eventHandler"/>
</template>

<template name="tmpl_8_i">
  <i 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    style="{{item.style}}" class="{{item.class}}" 
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchcancel="eventHandler"
    bindtouchend="eventHandler"
    bindtap="eventHandler"
    bindlongpress="eventHandler"
    bindlongtap="eventHandler"
    bindtransitionend="eventHandler"
    bindanimationstart="eventHandler"
    bindanimationiteration="eventHandler"
    bindanimationend="eventHandler"
    bindtouchforcechange="eventHandler"
  />
</template>

<template name="tmpl_8_progress">
  <progress wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" percent="{{utils.getValue(item.percent, 0)}}" color="{{utils.getValue(item.color, '#09BB07')}}" activeColor="{{utils.getValue(item.activeColor, '#09BB07')}}" backgroundColor="{{utils.getValue(item.backgroundColor, '#EBEBEB')}}" active="{{utils.getValue(item.active, false)}}" active-mode="{{utils.getValue(item.activeMode, backwards)}}"  show-info="{{utils.getValue(item.showInfo, false)}}" stroke-width="{{utils.getValue(item.strokeWidth, 6)}}" border-radius="{{util.getValue(item.borderRadius, 0)}}" font-size="{{utils.getValue(item.fontSize, 16)}}" duration="{{utils.getValue(item.duration, 30)}}" bindactiveend="eventHandler"/>
</template>

<template name="tmpl_8_rich-text">
  <rich-text wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" nodes="{{utils.getValue(item.nodes, [])}}" space="{{util.getVaue(item.space, 'ensp')}}" ></rich-text>
</template>

<template name="tmpl_8_swiper">
  <swiper
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"  
    indicator-dots="{{utils.getValue(item.indicatorDots, false)}}"
    indicator-color="{{utils.getValue(item.indicatorcolor, 'rgba(0, 0, 0, .3)')}}"
    indicator-active-color="{{utils.getValue(item.indicatorActiveColor, '#000000')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}" 
    interval="{{utils.getValue(item.interval, 5000)}}" 
    duration="{{utils.getValue(item.duration, 500)}}"
    circular="{{utils.getValue(item.circular, false)}}" 
    vertical="{{utils.getValue(item.vertical, false)}}" 
    previous-margin="{{utils.getValue(item.previousMargin, '0px')}}" 
    next-margin="{{utils.getValue(item.nextMargin, '0px')}}" 
    snap-to-edge="{{utils.getValue(item.snapToEdge, false)}}" 
    display-multiple-items="{{utils.getValue(item.displayMultipleItems, 1)}}" 
    easing-function="{{utils.getValue(item.easingFunction, 'default')}}" 
    bindchange="eventHandler"
    bindtransition="eventHandler"
    bindanimationfinish="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper>
</template>

<template name="tmpl_8_swiper-item">
  <swiper-item wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  item-id="{{utils.getValue(item.itemId, '')}}" skip-hidden-item-layout="{{utils.getValue(item.skipHiddenItemLayout, '')}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper-item>
</template>

<template name="tmpl_8_switch">
  <switch wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  checked="{{utils.getValue(item.checked, false)}}" disabled="{{utils.getValue(item.disabled, false)}}" type="{{utils.getValue(item.type, 'switch')}}" color="{{utils.getValue(item.color, '#04BE02')}}" bindchange="eventHandler"/>
</template>

<template name="tmpl_8_movable-area">
  <movable-area 
    style="{{item.style}}" class="{{item.class}}" 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    scale-area="{{utils.getValue(item.scaleArea, false)}}" 
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-area>
</template>

<template name="tmpl_8_movable-view">
  <movable-view 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}" 
    direction="{{utils.getValue(item.direction, 'none')}}"
    inertia="{{utils.getValue(item.inertia, false)}}"
    out-of-bounds="{{utils.getValue(item.outOfBounds, false)}}"
    x="{{utils.getValue(item.x, '0')}}"
    y="{{utils.getValue(item.y, '0')}}"
    damping="{{utils.getValue(item.damping, 20)}}"
    friction="{{utils.getValue(item.friction, 2)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    scale="{{utils.getValue(item.scale, false)}}"
    scale-min="{{utils.getValue(item.scaleMin, 0.5)}}"
    scale-max="{{utils.getValue(item.scaleMax, 10)}}"
    scale-value="{{utils.getValue(item.scaleValue, 1)}}"
    animation="{{utils.getValue(item.animation, false)}}"
    width="{{utils.getValue(item.width, '10px')}}"
    height="{{utils.getValue(item.height, '10px')}}"
    bindchange="eventHandler"
    bindscale="eventHandler"
    bindhtouchmove="eventHandler"
    bindvtouchmove="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-view>
</template>

<template name="tmpl_8_map">
  <map
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    longitude="{{item.longitude}}"
    latitude="{{item.latitude}}"scale
    scale="{{utils.getValue(item.scale, 16)}}"
    min-scale="{{utils.getValue(item.minScale, 3)}}"
    max-scale="{{utils.getValue(item.maxScale, 20)}}"
    markers="{{utils.getValue(item.markers, [])}}"
    covers="{{utils.getValue(item.covers, [])}}"
    polyline="{{utils.getValue(item.polyline, [])}}"
    circles="{{utils.getValue(item.circles, [])}}"
    controls="{{utils.getValue(item.controls, [])}}"
    include-points="{{utils.getValue(item.includePoints, [])}}"
    show-location="{{utils.getValue(item.showLocation, false)}}"
    polygons="{{utils.getValue(item.polygons, [])}}"
    subkey="{{utils.getValue(item.subkey, '')}}"
    setting="{{utils.getValue(item.setting, '', true)}}"
    layer-style="{{utils.getValue(item.layerStyle, 1)}}"
    rotate="0utils.getValue(item.rotate, 0)}}"
    enable-3D="{{utils.getValue(item.enable3D, false)}}"
    show-compass="{{utils.getValue(item.showCompass, false)}}"
    show-scale="{{utils.getValue(item.showScale, false)}}"
    enable-overlooking="{{utils.getValue(item.enableOverlooking, false)}}"
    enable-zoom="{{utils.getValue(item.enableZoom, true)}}"
    enable-scroll="{{utils.getValue(item.scale, true)}}"
    enable-rotate="{{utils.getValue(item.enableRotate, false)}}"
    enable-satellite="{{utils.getValue(item.enableSatellite, false)}}"
    enable-traffic="{{utils.getValue(item.enableTraffic, false)}}"
    enable-poi="{{utils.getValue(item.enablePoi, true)}}"
    enable-building="{{utils.getValue(item.enableBuilding, false)}}"
    bindtap="eventHandler"
    bindmarkertap="eventHandler"
    bindlabeltap="eventHandler"
    bindcontroltap="eventHandler"
    bindcallouttap="eventHandler"
    bindregionchange="eventHandler"
    bindpoitap="eventHandler"
    bindanchorpointtap="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </map>
</template>

<template name="tmpl_8_cover-view">
    <cover-view
      wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
      style="{{item.style}}" class="{{item.class}}"
      hover-class="{{utils.getValue(item.hoverClass,'none')}}"
      hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}"
      hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}"
      hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}"
      animation="{{item.animation}}"
      bindtouchstart="eventHandler"
      bindtouchmove="eventHandler"
      bindtouchend="eventHandler"
      bindtouchcancel="eventHandler"
      bindlongpress="eventHandler"
      bindanimationstart="eventHandler"
      bindanimationiteration="eventHandler"
      bindanimationend="eventHandler"
      bindtransitionend="eventHandler"
      bindtap="eventHandler"
     >
      <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
    </cover-view>
</template>

<template name="tmpl_8_label">
  <label
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    for="{{utils.getValue(item.for, '')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </label>
</template>

<template name="tmpl_8_checkbox">
  <checkbox
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox>
</template>

<template name="tmpl_8_checkbox-group">
  <checkbox-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox-group>
</template>

<template name="tmpl_8_form">
  <form
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    report-submit="{{utils.getValue(item.reportSubmit, false)}}"
    report-submit-timeout="{{utils.getValue(item.reportSubmitTimeout, 0)}}"
    bindsubmit="eventHandler"
    bindreset="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </form>
</template>

<template name="tmpl_8_radio">
  <radio
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio>
</template>

<template name="tmpl_8_radio-group">
  <radio-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio-group>
</template>

<template name="tmpl_8_picker">
  <picker
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    header-text="{{utils.getValue(item.header-text, '')}}"
    mode="{{utils.getValue(item.mode, 'selector')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    range="{{utils.getValue(item.range, [])}}"
    range-key="{{utils.getValue(item.rangeKey, '')}}"
    value="{{utils.getValue(item.value, 0)}}"
    start="{{utils.getValue(item.start, '')}}"
    end="{{utils.getValue(item.end, '')}}"
    fields="{{utils.getValue(item.fields, 'day')}}"
    custom-item="{{utils.getValue(item.customItem, '')}}"
    bindchange="eventHandler"
    bindcancel="eventHandler"
    bindcolumnchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker>
</template>

<template name="tmpl_8_picker-view">
  <picker-view
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, [])}}"
    indicator-style="{{utils.getValue(item.indicatorStyle, '')}}"
    indicator-class="{{utils.getValue(item.indicatorClass, '')}}"
    mask-style="{{utils.getValue(item.maskStyle, '')}}"
    mask-class="{{utils.getValue(item.maskClass, '')}}"
    bindchange="eventHandler"
    bindpickstart="eventHandler"
    bindpickend="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view>
</template>

<template name="tmpl_8_picker-view-column">
  <picker-view-column
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view-column>
</template>

<template name="tmpl_8_slider">
  <slider
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    min="{{utils.getValue(item.min, 0)}}"
    max="{{utils.getValue(item.max, 100)}}"
    step="{{utils.getValue(item.step, 1)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    value="{{utils.getValue(item.value, 0)}}"
    color="{{utils.getValue(item.color, '#e9e9e9')}}"
    selected-color="{{utils.getValue(item.selectedColor, '#1aad19')}}"
    activeColor="{{utils.getValue(item.activeColor, '#1aad19')}}"
    backgroundColor="{{utils.getValue(item.backgroundColor, '#e9e9e9')}}"
    block-size="{{utils.getValue(item.blockSize, 28)}}"
    block-color="{{utils.getValue(item.blockColor, '#ffffff')}}"
    show-value="{{utils.getValue(item.showValue, false)}}"
    bindchanging="eventHandler"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </slider>
</template>

<template name="tmpl_8_textarea">
  <textarea
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    placeholder-style="{{utils.getValue(item.placeholderStyle, '')}}"
    placeholder-class="{{utils.getValue(item.placeholderClass, 'textarea-placeholder')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    maxlength="{{utils.getValue(item.maxlength, 140)}}"
    auto-focus="{{utils.getValue(item.autoFocus, false)}}"
    focus="{{utils.getValue(item.focus, false)}}"
    auto-height="{{utils.getValue(item.autoHeight, false)}}"
    fixed="{{utils.getValue(item.fixed, false)}}"
    cursor-spacing="{{utils.getValue(item.cursorSpacing, 0)}}"
    cursor="{{utils.getValue(item.cursor, -1)}}"
    show-confirm-bar="{{utils.getValue(item.showConfirmBar, true)}}"
    selection-start="{{utils.getValue(item.selectionStart, -1)}}"
    selection-end="{{utils.getValue(item.selectionEnd, -1)}}"
    adjust-position="{{utils.getValue(item.adjustPosition, true)}}"
    hold-keyboard="{{utils.getValue(item.holdKeyboard, false)}}"
    disable-default-padding="{{utils.getValue(item.disableDefaultPadding, false)}}"
    confirm-type="{{utils.getValue(item.confirmType, 'return')}}"
    confirm-hold="{{utils.getValue(item.confirmHold, false)}}"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindlinechange="eventHandler"
    bindinput="eventHandler"
    bindconfirm="eventHandler"
    bindkeyboardheightchange="eventHandler"
  >
  
  </textarea>
</template>

<template name="tmpl_8_editor">
  <editor
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    read-only="{{utils.getValue(item.readOnly, false)}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    show-img-size="{{utils.getValue(item.showImgSize, false)}}"
    show-img-toolbar="{{utils.getValue(item.showImgToolbar, false)}}"
    show-img-resize="{{utils.getValue(item.showImgResize, false)}}"
    bindready="eventHandler"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindinput="eventHandler"
    bindstatuschange="eventHandler"
  >
  
  </editor>
</template>

<template name="tmpl_8_navigator">
  <navigator
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    target="{{utils.getValue(item.target, 'self')}}"
    url="{{utils.getValue(item.url, '')}}"
    open-type="{{utils.getValue(item.openType, 'navigate')}}"
    delta="{{utils.getValue(item.delta, 1)}}"
    app-id="{{utils.getValue(item.appId, '')}}"
    path="{{utils.getValue(item.path, '')}}"
    extra-data="{{utils.getValue(item.extraData, '', true)}}"
    version="{{utils.getValue(item.version, 'release')}}"
    redirect="{{utils.getValue(item.redirect, false)}}"
    short-link="{{utils.getValue(item.shortLink, '')}}"
    hover-class="{{utils.getValue(item.hoverClass, 'navigator-hover')}}"
    hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation, false)}}"
    hover-start-time="{{utils.getValue(item.hoverStartTime, 50)}}"
    hover-stay-time="{{utils.getValue(item.hoverStayTime, 600)}}"
    bindsuccess="eventHandler"
    bindfail="eventHandler"
    bindcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </navigator>
</template>

<template name="tmpl_8_video">
  <video
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{item.src}}"
    duration="{{utils.getValue(item.duration, 0)}}"
    controls="{{utils.getValue(item.controls, true)}}"
    danmu-list="{{utils.getValue(item.danmuList, [])}}"
    danmu-btn="{{utils.getValue(item.danmuBtn, false)}}"
    enable-danmu="{{utils.getValue(item.enableDanmu, false)}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    loop="{{utils.getValue(item.loop, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    initial-time="{{utils.getValue(item.initialTime, 0)}}"
    page-gesture="{{utils.getValue(item.pageGesture, false)}}"
    direction="{{utils.getValue(item.direction, 0)}}"
    show-progress="{{utils.getValue(item.showProgress, true)}}"
    show-fullscreen-btn="{{utils.getValue(item.showFullscreenBtn, true)}}"
    show-play-btn="{{utils.getValue(item.showPlayBtn, true)}}"
    show-center-play-btn="{{utils.getValue(item.showCenterPlayBtn, true)}}"
    enable-progress-gesture="{{utils.getValue(item.enableProgressGesture, true)}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    poster="{{utils.getValue(item.poster, '')}}"
    show-mute-btn="{{utils.getValue(item.showMuteBtn, false)}}"
    title="{{utils.getValue(item.title, '')}}"
    play-btn-position="{{utils.getValue(item.playBtnPosition, 'bottom')}}"
    enable-play-gesture="{{utils.getValue(item.enablePlayGesture, false)}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    vslide-gesture="{{utils.getValue(item.vslideGesture, false)}}"
    vslide-gesture-in-fullscreen="{{utils.getValue(item.vslideGestureInFullscreen, true)}}"
    ad-unit-id="{{item.adUnitId}}"
    poster-for-crawler="{{item.posterForCrawler}}"
    show-casting-button="{{utils.getValue(item.showCastingButton, false)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    picture-in-picture-show-progress="{{utils.getValue(item.pictureInPictureShowProgress, false)}}"
    enable-auto-rotation="{{utils.getValue(item.enableAutoRotation, false)}}"
    show-screen-lock-button="{{utils.getValue(item.showScreenLockButton, false)}}"
    show-snapshot-button="{{utils.getValue(item.showSnapshotButton, false)}}"
    show-background-playback-button="{{utils.getValue(item.showBackgroundPlaybackButton, false)}}"
    background-poster="{{utils.getValue(item.backgroundPoster, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    is-drm="{{utils.getValue(item.isDrm, false)}}"
    provision-url="{{utils.getValue(item.provisionUrl, '')}}"
    certificate-url="{{utils.getValue(item.certificateUrl, '')}}"
    license-url="{{utils.getValue(item.licenseUrl, '')}}"
    bindplay="eventHandler"
    bindpause="eventHandler"
    bindended="eventHandler"
    bindtimeupdate="eventHandler"
    bindfullscreenchange="eventHandler"
    bindwaiting="eventHandler"
    binderror="eventHandler"
    bindprogress="eventHandler"
    bindloadedmetadata="eventHandler"
    bindcontrolstoggle="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
    bindseekcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </video>
</template>

<template name="tmpl_8_camera">
  <camera
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    mode="{{utils.getValue(item.mode, 'normal')}}"
    resolution="{{utils.getValue(item.resolution, 'medium')}}"
    device-position="{{utils.getValue(item.devicePosition, 'back')}}"
    flash="{{utils.getValue(item.flash, 'auto')}}"
    frame-size="{{utils.getValue(item.frameSize, 'medium')}}"
    bindstop="eventHandler"
    binderror="eventHandler"
    bindinitdone="eventHandler"
    bindscancode="eventHandler"
  >
    
  </camera>
</template>

<template name="tmpl_8_live-player">
  <live-player
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{utils.getValue(item.src, '')}}"
    mode="{{utils.getValue(item.mode, 'live')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    min-cache="{{utils.getValue(item.minCache, 1)}}"
    max-cache="{{utils.getValue(item.maxCache, 3)}}"
    sound-mode="{{utils.getValue(item.soundMode, 'speaker')}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    bindstatechange="eventHandler"
    bindfullscreenchange="eventHandler"
    bindnetstatus="eventHandler"
    bindaudiovolumenotify="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-player>
</template>

<template name="tmpl_8_live-pusher">
  <live-pusher
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    url="{{utils.getValue(item.url, '')}}"
    mode="{{utils.getValue(item.mode, 'RTC')}}"
    autopush="{{utils.getValue(item.autopush, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    enable-camera="{{utils.getValue(item.enableCamera, true)}}"
    auto-focus="{{utils.getValue(item.autoFocus, true)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    beauty="{{utils.getValue(item.beauty, 0)}}"
    whiteness="{{utils.getValue(item.whiteness, 0)}}"
    aspect="{{utils.getValue(item.aspect, '9:16')}}"
    min-bitrate="{{utils.getValue(item.minBitrate, 200)}}"
    max-bitrate="{{utils.getValue(item.maxBitrate, 1000)}}"
    audio-quality="{{utils.getValue(item.audioQuality, 'high')}}"
    waiting-image="{{utils.getValue(item.waitingImage, '')}}"
    waiting-image-hash="{{utils.getValue(item.waitingImageHash, '')}}"
    zoom="{{utils.getValue(item.zoom, false)}}"
    device-position="{{utils.getValue(item.devicePosition, 'front')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    mirror="{{utils.getValue(item.mirror, false)}}"
    remote-mirror="{{utils.getValue(item.remoteMirror, false)}}"
    local-mirror="{{utils.getValue(item.localMirror, 'auto')}}"
    audio-reverb-type="{{utils.getValue(item.audioReverbType, 0)}}"
    enable-mic="{{utils.getValue(item.enableMic, true)}}"
    enable-agc="{{utils.getValue(item.enableAgc, false)}}"
    enable-ans="{{utils.getValue(item.enableAns, false)}}"
    audio-volume-type="{{utils.getValue(item.audioVolumeType, 'auto')}}"
    video-width="{{utils.getValue(item.videoWidth, 360)}}"
    video-height="{{utils.getValue(item.videoHeight, 640)}}"
    beauty-style="{{utils.getValue(item.beautyStyle, 'smooth')}}"
    filter="{{utils.getValue(item.filter, 'standard')}}"
    bindstatechange="eventHandler"
    bindnetstatus="eventHandler"
    binderror="eventHandler"
    bindbgmstart="eventHandler"
    bindbgmprogress="eventHandler"
    bindbgmcomplete="eventHandler"
    bindaudiovolumenotify="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-pusher>
</template>

<template name="tmpl_8_canvas">
  <canvas
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    type="{{utils.getValue(item.type, '')}}"
    canvas-id="{{utils.getValue(item.canvasId, '')}}"
    disable-scroll="{{utils.getValue(item.disableScroll, false)}}"
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchend="eventHandler"
    bindtouchcancel="eventHandler"
    bindlongtap="eventHandler"
    binderror="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </canvas>
</template>

<template name="tmpl_8_keyboard-accessory">
  <keyboard-accessory
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </keyboard-accessory>
</template>

      

<template name="container_9"><block wx:for="{{vdoms}}" wx:key="key"><template is="{{utils.getTagName(item, cid)}}" data="{{item: item, cid}}"></template></block></template>

<template name="tmpl_9_view">
  <view wx:key="{{item.key}}" data-data="{{item.dataSet}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_9_pure-view">
  <view wx:key="{{item.key}}" style="{{item.style}}" class="{{item.class}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_9_static-view">
  <view wx:key="{{item.key}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}"  style="{{item.style}}" class="{{item.class}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>


<template name="tmpl_9_text">
  <text wx:key="{{item.key}}" data-data="{{item.dataSet}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}"><template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" /></text>
</template>

<template name="tmpl_9_template">
  <template is="{{item.is}}" data="{{...item.data}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </template>
</template>

<template name="tmpl_9_button">
  <button wx:key="{{item.key}}" data-data="{{item.dataSet}}" size="{{utils.getValue(item.size,'default')}}" type="{{item.type}}" plain="{{utils.getValue(item.plain,false)}}" disabled="{{item.disabled}}" loading="{{utils.getValue(item.loading,false)}}" form-type="{{item.formType}}" open-type="{{item.openType}}" hover-class="{{utils.getValue(item.hoverClass,'button-hover')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,20)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,70)}}" name="{{item.name}}" lang="{{utils.getValue(item.lang,en)}}" session-from="{{item.sessionFrom}}" send-message-title="{{item.sendMessageTitle}}" send-message-path="{{item.sendMessagePath}}" send-message-img="{{item.sendMessageImg}}" app-parameter="{{item.appParameter}}" show-message-card="{{utils.getValue(item.showMessageCard,false)}}" business-id="{{item.businessId}}" bindgetuserinfo="eventHandler" bindcontact="eventHandler" bindgetphonenumber="eventHandler" binderror="eventHandler" bindopensetting="eventHandler" bindlaunchapp="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </button>
</template>

<template name="tmpl_9_input">
  <template is="{{utils.isFocus(item, 'tmpl_9_')}}" data="{{item:item}}" />
</template>

<template name="tmpl_9_input_focus">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" focus="{{utils.getValue(item.focus,false)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_9_input_blur">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_9_text-node"> {{item.content}} </template>

<template name="tmpl_9_navbar">
  <navbar id="{{item.uid}}" title="{{item.title}}" noPlaceholder="{{item.noPlaceholder}}" css="{{item.css}}"></navbar>
</template>

<template name="tmpl_9_keyboard">
  <keyboard id="{{item.uid}}" limit="{{item.limit}}" price="{{item.price}}" bind:change="eventHandler" bind:confirm="eventHandler"></keyboard>
</template>

<template name="tmpl_9_scroll-view">
  <scroll-view wx:key="{{item.key}}" data-data="{{item.dataSet}}" scroll-x="{{utils.getValue(item.scrollX,false)}}" scroll-y="{{utils.getValue(item.scrollY,false)}}" upper-threshold="{{utils.getValue(item.upperThreshold,50)}}" lower-threshold="{{utils.getValue(item.lowerThreshold,50)}}" scroll-top="{{item.scrollTop}}" scroll-left="{{item.scrollLeft}}" scroll-into-view="{{item.scrollIntoView}}" scroll-with-animation="{{utils.getValue(item.scrollWithAnimation,false)}}" enable-back-to-top="{{utils.getValue(item.enableBackToTop,false)}}" bindscrolltoupper="eventHandler" bindscrolltolower="eventHandler" bindscroll="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" enable-flex="{{utils.getValue(item.enableFlex,false)}}" scroll-anchoring="{{utils.getValue(item.scrollAnchoring,false)}}" refresher-enabled="{{utils.getValue(item.refresherEnabled,false)}}" refresher-threshold="{{utils.getValue(item.refresherThreshold,45)}}" refresher-default-style="{{utils.getValue(item.refresherDefaultStyle,'black')}}" refresher-background="{{utils.getValue(item.refresherBackground,'#FFF')}}" refresher-triggered="{{utils.getValue(item.refresherTriggered,false)}}" enhanced="{{utils.getValue(item.enhanced,false)}}" bounces="{{utils.getValue(item.bounces,true)}}" show-scrollbar="{{utils.getValue(item.showScrollbar,true)}}" paging-enabled="{{utils.getValue(item.pagingEnabled,false)}}" fast-deceleration="{{utils.getValue(item.fastDeceleration,false)}}" binddragstart="eventHandler" binddragging="eventHandler" binddragend="eventHandler" bindrefresherpulling="eventHandler" bindrefresherrefresh="eventHandler" bindrefresherrestore="eventHandler" bindrefresherabort="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </scroll-view>
</template>

<template name="tmpl_9_block">
  <block wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </block>
</template>

<template name="tmpl_9_null">
</template>

<template name="tmpl_9_image">
  <image wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" src="{{item.src}}" mode="{{utils.getValue(item.mode,'scaleToFill')}}" lazy-load="{{utils.getValue(item.lazyLoad,false)}}" binderror="eventHandler" bindload="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" webp="{{utils.getValue(item.webp,false)}}" show-menu-by-longpress="{{utils.getValue(item.showMenuByLongpress,false)}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </image>
</template>

<template name="tmpl_9_icon">
  <icon wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" size="{{utils.getValue(item.size, 23)}}" type="{{item.type}}" color="{{utils.getValue(item.color, '')}}" bind:tap="eventHandler"/>
</template>

<template name="tmpl_9_i">
  <i 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    style="{{item.style}}" class="{{item.class}}" 
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchcancel="eventHandler"
    bindtouchend="eventHandler"
    bindtap="eventHandler"
    bindlongpress="eventHandler"
    bindlongtap="eventHandler"
    bindtransitionend="eventHandler"
    bindanimationstart="eventHandler"
    bindanimationiteration="eventHandler"
    bindanimationend="eventHandler"
    bindtouchforcechange="eventHandler"
  />
</template>

<template name="tmpl_9_progress">
  <progress wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" percent="{{utils.getValue(item.percent, 0)}}" color="{{utils.getValue(item.color, '#09BB07')}}" activeColor="{{utils.getValue(item.activeColor, '#09BB07')}}" backgroundColor="{{utils.getValue(item.backgroundColor, '#EBEBEB')}}" active="{{utils.getValue(item.active, false)}}" active-mode="{{utils.getValue(item.activeMode, backwards)}}"  show-info="{{utils.getValue(item.showInfo, false)}}" stroke-width="{{utils.getValue(item.strokeWidth, 6)}}" border-radius="{{util.getValue(item.borderRadius, 0)}}" font-size="{{utils.getValue(item.fontSize, 16)}}" duration="{{utils.getValue(item.duration, 30)}}" bindactiveend="eventHandler"/>
</template>

<template name="tmpl_9_rich-text">
  <rich-text wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" nodes="{{utils.getValue(item.nodes, [])}}" space="{{util.getVaue(item.space, 'ensp')}}" ></rich-text>
</template>

<template name="tmpl_9_swiper">
  <swiper
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"  
    indicator-dots="{{utils.getValue(item.indicatorDots, false)}}"
    indicator-color="{{utils.getValue(item.indicatorcolor, 'rgba(0, 0, 0, .3)')}}"
    indicator-active-color="{{utils.getValue(item.indicatorActiveColor, '#000000')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}" 
    interval="{{utils.getValue(item.interval, 5000)}}" 
    duration="{{utils.getValue(item.duration, 500)}}"
    circular="{{utils.getValue(item.circular, false)}}" 
    vertical="{{utils.getValue(item.vertical, false)}}" 
    previous-margin="{{utils.getValue(item.previousMargin, '0px')}}" 
    next-margin="{{utils.getValue(item.nextMargin, '0px')}}" 
    snap-to-edge="{{utils.getValue(item.snapToEdge, false)}}" 
    display-multiple-items="{{utils.getValue(item.displayMultipleItems, 1)}}" 
    easing-function="{{utils.getValue(item.easingFunction, 'default')}}" 
    bindchange="eventHandler"
    bindtransition="eventHandler"
    bindanimationfinish="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper>
</template>

<template name="tmpl_9_swiper-item">
  <swiper-item wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  item-id="{{utils.getValue(item.itemId, '')}}" skip-hidden-item-layout="{{utils.getValue(item.skipHiddenItemLayout, '')}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper-item>
</template>

<template name="tmpl_9_switch">
  <switch wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  checked="{{utils.getValue(item.checked, false)}}" disabled="{{utils.getValue(item.disabled, false)}}" type="{{utils.getValue(item.type, 'switch')}}" color="{{utils.getValue(item.color, '#04BE02')}}" bindchange="eventHandler"/>
</template>

<template name="tmpl_9_movable-area">
  <movable-area 
    style="{{item.style}}" class="{{item.class}}" 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    scale-area="{{utils.getValue(item.scaleArea, false)}}" 
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-area>
</template>

<template name="tmpl_9_movable-view">
  <movable-view 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}" 
    direction="{{utils.getValue(item.direction, 'none')}}"
    inertia="{{utils.getValue(item.inertia, false)}}"
    out-of-bounds="{{utils.getValue(item.outOfBounds, false)}}"
    x="{{utils.getValue(item.x, '0')}}"
    y="{{utils.getValue(item.y, '0')}}"
    damping="{{utils.getValue(item.damping, 20)}}"
    friction="{{utils.getValue(item.friction, 2)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    scale="{{utils.getValue(item.scale, false)}}"
    scale-min="{{utils.getValue(item.scaleMin, 0.5)}}"
    scale-max="{{utils.getValue(item.scaleMax, 10)}}"
    scale-value="{{utils.getValue(item.scaleValue, 1)}}"
    animation="{{utils.getValue(item.animation, false)}}"
    width="{{utils.getValue(item.width, '10px')}}"
    height="{{utils.getValue(item.height, '10px')}}"
    bindchange="eventHandler"
    bindscale="eventHandler"
    bindhtouchmove="eventHandler"
    bindvtouchmove="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-view>
</template>

<template name="tmpl_9_map">
  <map
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    longitude="{{item.longitude}}"
    latitude="{{item.latitude}}"scale
    scale="{{utils.getValue(item.scale, 16)}}"
    min-scale="{{utils.getValue(item.minScale, 3)}}"
    max-scale="{{utils.getValue(item.maxScale, 20)}}"
    markers="{{utils.getValue(item.markers, [])}}"
    covers="{{utils.getValue(item.covers, [])}}"
    polyline="{{utils.getValue(item.polyline, [])}}"
    circles="{{utils.getValue(item.circles, [])}}"
    controls="{{utils.getValue(item.controls, [])}}"
    include-points="{{utils.getValue(item.includePoints, [])}}"
    show-location="{{utils.getValue(item.showLocation, false)}}"
    polygons="{{utils.getValue(item.polygons, [])}}"
    subkey="{{utils.getValue(item.subkey, '')}}"
    setting="{{utils.getValue(item.setting, '', true)}}"
    layer-style="{{utils.getValue(item.layerStyle, 1)}}"
    rotate="0utils.getValue(item.rotate, 0)}}"
    enable-3D="{{utils.getValue(item.enable3D, false)}}"
    show-compass="{{utils.getValue(item.showCompass, false)}}"
    show-scale="{{utils.getValue(item.showScale, false)}}"
    enable-overlooking="{{utils.getValue(item.enableOverlooking, false)}}"
    enable-zoom="{{utils.getValue(item.enableZoom, true)}}"
    enable-scroll="{{utils.getValue(item.scale, true)}}"
    enable-rotate="{{utils.getValue(item.enableRotate, false)}}"
    enable-satellite="{{utils.getValue(item.enableSatellite, false)}}"
    enable-traffic="{{utils.getValue(item.enableTraffic, false)}}"
    enable-poi="{{utils.getValue(item.enablePoi, true)}}"
    enable-building="{{utils.getValue(item.enableBuilding, false)}}"
    bindtap="eventHandler"
    bindmarkertap="eventHandler"
    bindlabeltap="eventHandler"
    bindcontroltap="eventHandler"
    bindcallouttap="eventHandler"
    bindregionchange="eventHandler"
    bindpoitap="eventHandler"
    bindanchorpointtap="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </map>
</template>

<template name="tmpl_9_cover-view">
    <cover-view
      wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
      style="{{item.style}}" class="{{item.class}}"
      hover-class="{{utils.getValue(item.hoverClass,'none')}}"
      hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}"
      hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}"
      hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}"
      animation="{{item.animation}}"
      bindtouchstart="eventHandler"
      bindtouchmove="eventHandler"
      bindtouchend="eventHandler"
      bindtouchcancel="eventHandler"
      bindlongpress="eventHandler"
      bindanimationstart="eventHandler"
      bindanimationiteration="eventHandler"
      bindanimationend="eventHandler"
      bindtransitionend="eventHandler"
      bindtap="eventHandler"
     >
      <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
    </cover-view>
</template>

<template name="tmpl_9_label">
  <label
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    for="{{utils.getValue(item.for, '')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </label>
</template>

<template name="tmpl_9_checkbox">
  <checkbox
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox>
</template>

<template name="tmpl_9_checkbox-group">
  <checkbox-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox-group>
</template>

<template name="tmpl_9_form">
  <form
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    report-submit="{{utils.getValue(item.reportSubmit, false)}}"
    report-submit-timeout="{{utils.getValue(item.reportSubmitTimeout, 0)}}"
    bindsubmit="eventHandler"
    bindreset="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </form>
</template>

<template name="tmpl_9_radio">
  <radio
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio>
</template>

<template name="tmpl_9_radio-group">
  <radio-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio-group>
</template>

<template name="tmpl_9_picker">
  <picker
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    header-text="{{utils.getValue(item.header-text, '')}}"
    mode="{{utils.getValue(item.mode, 'selector')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    range="{{utils.getValue(item.range, [])}}"
    range-key="{{utils.getValue(item.rangeKey, '')}}"
    value="{{utils.getValue(item.value, 0)}}"
    start="{{utils.getValue(item.start, '')}}"
    end="{{utils.getValue(item.end, '')}}"
    fields="{{utils.getValue(item.fields, 'day')}}"
    custom-item="{{utils.getValue(item.customItem, '')}}"
    bindchange="eventHandler"
    bindcancel="eventHandler"
    bindcolumnchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker>
</template>

<template name="tmpl_9_picker-view">
  <picker-view
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, [])}}"
    indicator-style="{{utils.getValue(item.indicatorStyle, '')}}"
    indicator-class="{{utils.getValue(item.indicatorClass, '')}}"
    mask-style="{{utils.getValue(item.maskStyle, '')}}"
    mask-class="{{utils.getValue(item.maskClass, '')}}"
    bindchange="eventHandler"
    bindpickstart="eventHandler"
    bindpickend="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view>
</template>

<template name="tmpl_9_picker-view-column">
  <picker-view-column
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view-column>
</template>

<template name="tmpl_9_slider">
  <slider
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    min="{{utils.getValue(item.min, 0)}}"
    max="{{utils.getValue(item.max, 100)}}"
    step="{{utils.getValue(item.step, 1)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    value="{{utils.getValue(item.value, 0)}}"
    color="{{utils.getValue(item.color, '#e9e9e9')}}"
    selected-color="{{utils.getValue(item.selectedColor, '#1aad19')}}"
    activeColor="{{utils.getValue(item.activeColor, '#1aad19')}}"
    backgroundColor="{{utils.getValue(item.backgroundColor, '#e9e9e9')}}"
    block-size="{{utils.getValue(item.blockSize, 28)}}"
    block-color="{{utils.getValue(item.blockColor, '#ffffff')}}"
    show-value="{{utils.getValue(item.showValue, false)}}"
    bindchanging="eventHandler"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </slider>
</template>

<template name="tmpl_9_textarea">
  <textarea
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    placeholder-style="{{utils.getValue(item.placeholderStyle, '')}}"
    placeholder-class="{{utils.getValue(item.placeholderClass, 'textarea-placeholder')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    maxlength="{{utils.getValue(item.maxlength, 140)}}"
    auto-focus="{{utils.getValue(item.autoFocus, false)}}"
    focus="{{utils.getValue(item.focus, false)}}"
    auto-height="{{utils.getValue(item.autoHeight, false)}}"
    fixed="{{utils.getValue(item.fixed, false)}}"
    cursor-spacing="{{utils.getValue(item.cursorSpacing, 0)}}"
    cursor="{{utils.getValue(item.cursor, -1)}}"
    show-confirm-bar="{{utils.getValue(item.showConfirmBar, true)}}"
    selection-start="{{utils.getValue(item.selectionStart, -1)}}"
    selection-end="{{utils.getValue(item.selectionEnd, -1)}}"
    adjust-position="{{utils.getValue(item.adjustPosition, true)}}"
    hold-keyboard="{{utils.getValue(item.holdKeyboard, false)}}"
    disable-default-padding="{{utils.getValue(item.disableDefaultPadding, false)}}"
    confirm-type="{{utils.getValue(item.confirmType, 'return')}}"
    confirm-hold="{{utils.getValue(item.confirmHold, false)}}"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindlinechange="eventHandler"
    bindinput="eventHandler"
    bindconfirm="eventHandler"
    bindkeyboardheightchange="eventHandler"
  >
  
  </textarea>
</template>

<template name="tmpl_9_editor">
  <editor
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    read-only="{{utils.getValue(item.readOnly, false)}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    show-img-size="{{utils.getValue(item.showImgSize, false)}}"
    show-img-toolbar="{{utils.getValue(item.showImgToolbar, false)}}"
    show-img-resize="{{utils.getValue(item.showImgResize, false)}}"
    bindready="eventHandler"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindinput="eventHandler"
    bindstatuschange="eventHandler"
  >
  
  </editor>
</template>

<template name="tmpl_9_navigator">
  <navigator
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    target="{{utils.getValue(item.target, 'self')}}"
    url="{{utils.getValue(item.url, '')}}"
    open-type="{{utils.getValue(item.openType, 'navigate')}}"
    delta="{{utils.getValue(item.delta, 1)}}"
    app-id="{{utils.getValue(item.appId, '')}}"
    path="{{utils.getValue(item.path, '')}}"
    extra-data="{{utils.getValue(item.extraData, '', true)}}"
    version="{{utils.getValue(item.version, 'release')}}"
    redirect="{{utils.getValue(item.redirect, false)}}"
    short-link="{{utils.getValue(item.shortLink, '')}}"
    hover-class="{{utils.getValue(item.hoverClass, 'navigator-hover')}}"
    hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation, false)}}"
    hover-start-time="{{utils.getValue(item.hoverStartTime, 50)}}"
    hover-stay-time="{{utils.getValue(item.hoverStayTime, 600)}}"
    bindsuccess="eventHandler"
    bindfail="eventHandler"
    bindcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </navigator>
</template>

<template name="tmpl_9_video">
  <video
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{item.src}}"
    duration="{{utils.getValue(item.duration, 0)}}"
    controls="{{utils.getValue(item.controls, true)}}"
    danmu-list="{{utils.getValue(item.danmuList, [])}}"
    danmu-btn="{{utils.getValue(item.danmuBtn, false)}}"
    enable-danmu="{{utils.getValue(item.enableDanmu, false)}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    loop="{{utils.getValue(item.loop, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    initial-time="{{utils.getValue(item.initialTime, 0)}}"
    page-gesture="{{utils.getValue(item.pageGesture, false)}}"
    direction="{{utils.getValue(item.direction, 0)}}"
    show-progress="{{utils.getValue(item.showProgress, true)}}"
    show-fullscreen-btn="{{utils.getValue(item.showFullscreenBtn, true)}}"
    show-play-btn="{{utils.getValue(item.showPlayBtn, true)}}"
    show-center-play-btn="{{utils.getValue(item.showCenterPlayBtn, true)}}"
    enable-progress-gesture="{{utils.getValue(item.enableProgressGesture, true)}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    poster="{{utils.getValue(item.poster, '')}}"
    show-mute-btn="{{utils.getValue(item.showMuteBtn, false)}}"
    title="{{utils.getValue(item.title, '')}}"
    play-btn-position="{{utils.getValue(item.playBtnPosition, 'bottom')}}"
    enable-play-gesture="{{utils.getValue(item.enablePlayGesture, false)}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    vslide-gesture="{{utils.getValue(item.vslideGesture, false)}}"
    vslide-gesture-in-fullscreen="{{utils.getValue(item.vslideGestureInFullscreen, true)}}"
    ad-unit-id="{{item.adUnitId}}"
    poster-for-crawler="{{item.posterForCrawler}}"
    show-casting-button="{{utils.getValue(item.showCastingButton, false)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    picture-in-picture-show-progress="{{utils.getValue(item.pictureInPictureShowProgress, false)}}"
    enable-auto-rotation="{{utils.getValue(item.enableAutoRotation, false)}}"
    show-screen-lock-button="{{utils.getValue(item.showScreenLockButton, false)}}"
    show-snapshot-button="{{utils.getValue(item.showSnapshotButton, false)}}"
    show-background-playback-button="{{utils.getValue(item.showBackgroundPlaybackButton, false)}}"
    background-poster="{{utils.getValue(item.backgroundPoster, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    is-drm="{{utils.getValue(item.isDrm, false)}}"
    provision-url="{{utils.getValue(item.provisionUrl, '')}}"
    certificate-url="{{utils.getValue(item.certificateUrl, '')}}"
    license-url="{{utils.getValue(item.licenseUrl, '')}}"
    bindplay="eventHandler"
    bindpause="eventHandler"
    bindended="eventHandler"
    bindtimeupdate="eventHandler"
    bindfullscreenchange="eventHandler"
    bindwaiting="eventHandler"
    binderror="eventHandler"
    bindprogress="eventHandler"
    bindloadedmetadata="eventHandler"
    bindcontrolstoggle="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
    bindseekcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </video>
</template>

<template name="tmpl_9_camera">
  <camera
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    mode="{{utils.getValue(item.mode, 'normal')}}"
    resolution="{{utils.getValue(item.resolution, 'medium')}}"
    device-position="{{utils.getValue(item.devicePosition, 'back')}}"
    flash="{{utils.getValue(item.flash, 'auto')}}"
    frame-size="{{utils.getValue(item.frameSize, 'medium')}}"
    bindstop="eventHandler"
    binderror="eventHandler"
    bindinitdone="eventHandler"
    bindscancode="eventHandler"
  >
    
  </camera>
</template>

<template name="tmpl_9_live-player">
  <live-player
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{utils.getValue(item.src, '')}}"
    mode="{{utils.getValue(item.mode, 'live')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    min-cache="{{utils.getValue(item.minCache, 1)}}"
    max-cache="{{utils.getValue(item.maxCache, 3)}}"
    sound-mode="{{utils.getValue(item.soundMode, 'speaker')}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    bindstatechange="eventHandler"
    bindfullscreenchange="eventHandler"
    bindnetstatus="eventHandler"
    bindaudiovolumenotify="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-player>
</template>

<template name="tmpl_9_live-pusher">
  <live-pusher
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    url="{{utils.getValue(item.url, '')}}"
    mode="{{utils.getValue(item.mode, 'RTC')}}"
    autopush="{{utils.getValue(item.autopush, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    enable-camera="{{utils.getValue(item.enableCamera, true)}}"
    auto-focus="{{utils.getValue(item.autoFocus, true)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    beauty="{{utils.getValue(item.beauty, 0)}}"
    whiteness="{{utils.getValue(item.whiteness, 0)}}"
    aspect="{{utils.getValue(item.aspect, '9:16')}}"
    min-bitrate="{{utils.getValue(item.minBitrate, 200)}}"
    max-bitrate="{{utils.getValue(item.maxBitrate, 1000)}}"
    audio-quality="{{utils.getValue(item.audioQuality, 'high')}}"
    waiting-image="{{utils.getValue(item.waitingImage, '')}}"
    waiting-image-hash="{{utils.getValue(item.waitingImageHash, '')}}"
    zoom="{{utils.getValue(item.zoom, false)}}"
    device-position="{{utils.getValue(item.devicePosition, 'front')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    mirror="{{utils.getValue(item.mirror, false)}}"
    remote-mirror="{{utils.getValue(item.remoteMirror, false)}}"
    local-mirror="{{utils.getValue(item.localMirror, 'auto')}}"
    audio-reverb-type="{{utils.getValue(item.audioReverbType, 0)}}"
    enable-mic="{{utils.getValue(item.enableMic, true)}}"
    enable-agc="{{utils.getValue(item.enableAgc, false)}}"
    enable-ans="{{utils.getValue(item.enableAns, false)}}"
    audio-volume-type="{{utils.getValue(item.audioVolumeType, 'auto')}}"
    video-width="{{utils.getValue(item.videoWidth, 360)}}"
    video-height="{{utils.getValue(item.videoHeight, 640)}}"
    beauty-style="{{utils.getValue(item.beautyStyle, 'smooth')}}"
    filter="{{utils.getValue(item.filter, 'standard')}}"
    bindstatechange="eventHandler"
    bindnetstatus="eventHandler"
    binderror="eventHandler"
    bindbgmstart="eventHandler"
    bindbgmprogress="eventHandler"
    bindbgmcomplete="eventHandler"
    bindaudiovolumenotify="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-pusher>
</template>

<template name="tmpl_9_canvas">
  <canvas
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    type="{{utils.getValue(item.type, '')}}"
    canvas-id="{{utils.getValue(item.canvasId, '')}}"
    disable-scroll="{{utils.getValue(item.disableScroll, false)}}"
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchend="eventHandler"
    bindtouchcancel="eventHandler"
    bindlongtap="eventHandler"
    binderror="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </canvas>
</template>

<template name="tmpl_9_keyboard-accessory">
  <keyboard-accessory
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </keyboard-accessory>
</template>

      

<template name="container_10"><block wx:for="{{vdoms}}" wx:key="key"><template is="{{utils.getTagName(item, cid)}}" data="{{item: item, cid}}"></template></block></template>

<template name="tmpl_10_view">
  <view wx:key="{{item.key}}" data-data="{{item.dataSet}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_10_pure-view">
  <view wx:key="{{item.key}}" style="{{item.style}}" class="{{item.class}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_10_static-view">
  <view wx:key="{{item.key}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}"  style="{{item.style}}" class="{{item.class}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>


<template name="tmpl_10_text">
  <text wx:key="{{item.key}}" data-data="{{item.dataSet}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}"><template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" /></text>
</template>

<template name="tmpl_10_template">
  <template is="{{item.is}}" data="{{...item.data}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </template>
</template>

<template name="tmpl_10_button">
  <button wx:key="{{item.key}}" data-data="{{item.dataSet}}" size="{{utils.getValue(item.size,'default')}}" type="{{item.type}}" plain="{{utils.getValue(item.plain,false)}}" disabled="{{item.disabled}}" loading="{{utils.getValue(item.loading,false)}}" form-type="{{item.formType}}" open-type="{{item.openType}}" hover-class="{{utils.getValue(item.hoverClass,'button-hover')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,20)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,70)}}" name="{{item.name}}" lang="{{utils.getValue(item.lang,en)}}" session-from="{{item.sessionFrom}}" send-message-title="{{item.sendMessageTitle}}" send-message-path="{{item.sendMessagePath}}" send-message-img="{{item.sendMessageImg}}" app-parameter="{{item.appParameter}}" show-message-card="{{utils.getValue(item.showMessageCard,false)}}" business-id="{{item.businessId}}" bindgetuserinfo="eventHandler" bindcontact="eventHandler" bindgetphonenumber="eventHandler" binderror="eventHandler" bindopensetting="eventHandler" bindlaunchapp="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </button>
</template>

<template name="tmpl_10_input">
  <template is="{{utils.isFocus(item, 'tmpl_10_')}}" data="{{item:item}}" />
</template>

<template name="tmpl_10_input_focus">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" focus="{{utils.getValue(item.focus,false)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_10_input_blur">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_10_text-node"> {{item.content}} </template>

<template name="tmpl_10_navbar">
  <navbar id="{{item.uid}}" title="{{item.title}}" noPlaceholder="{{item.noPlaceholder}}" css="{{item.css}}"></navbar>
</template>

<template name="tmpl_10_keyboard">
  <keyboard id="{{item.uid}}" limit="{{item.limit}}" price="{{item.price}}" bind:change="eventHandler" bind:confirm="eventHandler"></keyboard>
</template>

<template name="tmpl_10_scroll-view">
  <scroll-view wx:key="{{item.key}}" data-data="{{item.dataSet}}" scroll-x="{{utils.getValue(item.scrollX,false)}}" scroll-y="{{utils.getValue(item.scrollY,false)}}" upper-threshold="{{utils.getValue(item.upperThreshold,50)}}" lower-threshold="{{utils.getValue(item.lowerThreshold,50)}}" scroll-top="{{item.scrollTop}}" scroll-left="{{item.scrollLeft}}" scroll-into-view="{{item.scrollIntoView}}" scroll-with-animation="{{utils.getValue(item.scrollWithAnimation,false)}}" enable-back-to-top="{{utils.getValue(item.enableBackToTop,false)}}" bindscrolltoupper="eventHandler" bindscrolltolower="eventHandler" bindscroll="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" enable-flex="{{utils.getValue(item.enableFlex,false)}}" scroll-anchoring="{{utils.getValue(item.scrollAnchoring,false)}}" refresher-enabled="{{utils.getValue(item.refresherEnabled,false)}}" refresher-threshold="{{utils.getValue(item.refresherThreshold,45)}}" refresher-default-style="{{utils.getValue(item.refresherDefaultStyle,'black')}}" refresher-background="{{utils.getValue(item.refresherBackground,'#FFF')}}" refresher-triggered="{{utils.getValue(item.refresherTriggered,false)}}" enhanced="{{utils.getValue(item.enhanced,false)}}" bounces="{{utils.getValue(item.bounces,true)}}" show-scrollbar="{{utils.getValue(item.showScrollbar,true)}}" paging-enabled="{{utils.getValue(item.pagingEnabled,false)}}" fast-deceleration="{{utils.getValue(item.fastDeceleration,false)}}" binddragstart="eventHandler" binddragging="eventHandler" binddragend="eventHandler" bindrefresherpulling="eventHandler" bindrefresherrefresh="eventHandler" bindrefresherrestore="eventHandler" bindrefresherabort="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </scroll-view>
</template>

<template name="tmpl_10_block">
  <block wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </block>
</template>

<template name="tmpl_10_null">
</template>

<template name="tmpl_10_image">
  <image wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" src="{{item.src}}" mode="{{utils.getValue(item.mode,'scaleToFill')}}" lazy-load="{{utils.getValue(item.lazyLoad,false)}}" binderror="eventHandler" bindload="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" webp="{{utils.getValue(item.webp,false)}}" show-menu-by-longpress="{{utils.getValue(item.showMenuByLongpress,false)}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </image>
</template>

<template name="tmpl_10_icon">
  <icon wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" size="{{utils.getValue(item.size, 23)}}" type="{{item.type}}" color="{{utils.getValue(item.color, '')}}" bind:tap="eventHandler"/>
</template>

<template name="tmpl_10_i">
  <i 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    style="{{item.style}}" class="{{item.class}}" 
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchcancel="eventHandler"
    bindtouchend="eventHandler"
    bindtap="eventHandler"
    bindlongpress="eventHandler"
    bindlongtap="eventHandler"
    bindtransitionend="eventHandler"
    bindanimationstart="eventHandler"
    bindanimationiteration="eventHandler"
    bindanimationend="eventHandler"
    bindtouchforcechange="eventHandler"
  />
</template>

<template name="tmpl_10_progress">
  <progress wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" percent="{{utils.getValue(item.percent, 0)}}" color="{{utils.getValue(item.color, '#09BB07')}}" activeColor="{{utils.getValue(item.activeColor, '#09BB07')}}" backgroundColor="{{utils.getValue(item.backgroundColor, '#EBEBEB')}}" active="{{utils.getValue(item.active, false)}}" active-mode="{{utils.getValue(item.activeMode, backwards)}}"  show-info="{{utils.getValue(item.showInfo, false)}}" stroke-width="{{utils.getValue(item.strokeWidth, 6)}}" border-radius="{{util.getValue(item.borderRadius, 0)}}" font-size="{{utils.getValue(item.fontSize, 16)}}" duration="{{utils.getValue(item.duration, 30)}}" bindactiveend="eventHandler"/>
</template>

<template name="tmpl_10_rich-text">
  <rich-text wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" nodes="{{utils.getValue(item.nodes, [])}}" space="{{util.getVaue(item.space, 'ensp')}}" ></rich-text>
</template>

<template name="tmpl_10_swiper">
  <swiper
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"  
    indicator-dots="{{utils.getValue(item.indicatorDots, false)}}"
    indicator-color="{{utils.getValue(item.indicatorcolor, 'rgba(0, 0, 0, .3)')}}"
    indicator-active-color="{{utils.getValue(item.indicatorActiveColor, '#000000')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}" 
    interval="{{utils.getValue(item.interval, 5000)}}" 
    duration="{{utils.getValue(item.duration, 500)}}"
    circular="{{utils.getValue(item.circular, false)}}" 
    vertical="{{utils.getValue(item.vertical, false)}}" 
    previous-margin="{{utils.getValue(item.previousMargin, '0px')}}" 
    next-margin="{{utils.getValue(item.nextMargin, '0px')}}" 
    snap-to-edge="{{utils.getValue(item.snapToEdge, false)}}" 
    display-multiple-items="{{utils.getValue(item.displayMultipleItems, 1)}}" 
    easing-function="{{utils.getValue(item.easingFunction, 'default')}}" 
    bindchange="eventHandler"
    bindtransition="eventHandler"
    bindanimationfinish="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper>
</template>

<template name="tmpl_10_swiper-item">
  <swiper-item wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  item-id="{{utils.getValue(item.itemId, '')}}" skip-hidden-item-layout="{{utils.getValue(item.skipHiddenItemLayout, '')}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper-item>
</template>

<template name="tmpl_10_switch">
  <switch wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  checked="{{utils.getValue(item.checked, false)}}" disabled="{{utils.getValue(item.disabled, false)}}" type="{{utils.getValue(item.type, 'switch')}}" color="{{utils.getValue(item.color, '#04BE02')}}" bindchange="eventHandler"/>
</template>

<template name="tmpl_10_movable-area">
  <movable-area 
    style="{{item.style}}" class="{{item.class}}" 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    scale-area="{{utils.getValue(item.scaleArea, false)}}" 
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-area>
</template>

<template name="tmpl_10_movable-view">
  <movable-view 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}" 
    direction="{{utils.getValue(item.direction, 'none')}}"
    inertia="{{utils.getValue(item.inertia, false)}}"
    out-of-bounds="{{utils.getValue(item.outOfBounds, false)}}"
    x="{{utils.getValue(item.x, '0')}}"
    y="{{utils.getValue(item.y, '0')}}"
    damping="{{utils.getValue(item.damping, 20)}}"
    friction="{{utils.getValue(item.friction, 2)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    scale="{{utils.getValue(item.scale, false)}}"
    scale-min="{{utils.getValue(item.scaleMin, 0.5)}}"
    scale-max="{{utils.getValue(item.scaleMax, 10)}}"
    scale-value="{{utils.getValue(item.scaleValue, 1)}}"
    animation="{{utils.getValue(item.animation, false)}}"
    width="{{utils.getValue(item.width, '10px')}}"
    height="{{utils.getValue(item.height, '10px')}}"
    bindchange="eventHandler"
    bindscale="eventHandler"
    bindhtouchmove="eventHandler"
    bindvtouchmove="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-view>
</template>

<template name="tmpl_10_map">
  <map
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    longitude="{{item.longitude}}"
    latitude="{{item.latitude}}"scale
    scale="{{utils.getValue(item.scale, 16)}}"
    min-scale="{{utils.getValue(item.minScale, 3)}}"
    max-scale="{{utils.getValue(item.maxScale, 20)}}"
    markers="{{utils.getValue(item.markers, [])}}"
    covers="{{utils.getValue(item.covers, [])}}"
    polyline="{{utils.getValue(item.polyline, [])}}"
    circles="{{utils.getValue(item.circles, [])}}"
    controls="{{utils.getValue(item.controls, [])}}"
    include-points="{{utils.getValue(item.includePoints, [])}}"
    show-location="{{utils.getValue(item.showLocation, false)}}"
    polygons="{{utils.getValue(item.polygons, [])}}"
    subkey="{{utils.getValue(item.subkey, '')}}"
    setting="{{utils.getValue(item.setting, '', true)}}"
    layer-style="{{utils.getValue(item.layerStyle, 1)}}"
    rotate="0utils.getValue(item.rotate, 0)}}"
    enable-3D="{{utils.getValue(item.enable3D, false)}}"
    show-compass="{{utils.getValue(item.showCompass, false)}}"
    show-scale="{{utils.getValue(item.showScale, false)}}"
    enable-overlooking="{{utils.getValue(item.enableOverlooking, false)}}"
    enable-zoom="{{utils.getValue(item.enableZoom, true)}}"
    enable-scroll="{{utils.getValue(item.scale, true)}}"
    enable-rotate="{{utils.getValue(item.enableRotate, false)}}"
    enable-satellite="{{utils.getValue(item.enableSatellite, false)}}"
    enable-traffic="{{utils.getValue(item.enableTraffic, false)}}"
    enable-poi="{{utils.getValue(item.enablePoi, true)}}"
    enable-building="{{utils.getValue(item.enableBuilding, false)}}"
    bindtap="eventHandler"
    bindmarkertap="eventHandler"
    bindlabeltap="eventHandler"
    bindcontroltap="eventHandler"
    bindcallouttap="eventHandler"
    bindregionchange="eventHandler"
    bindpoitap="eventHandler"
    bindanchorpointtap="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </map>
</template>

<template name="tmpl_10_cover-view">
    <cover-view
      wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
      style="{{item.style}}" class="{{item.class}}"
      hover-class="{{utils.getValue(item.hoverClass,'none')}}"
      hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}"
      hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}"
      hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}"
      animation="{{item.animation}}"
      bindtouchstart="eventHandler"
      bindtouchmove="eventHandler"
      bindtouchend="eventHandler"
      bindtouchcancel="eventHandler"
      bindlongpress="eventHandler"
      bindanimationstart="eventHandler"
      bindanimationiteration="eventHandler"
      bindanimationend="eventHandler"
      bindtransitionend="eventHandler"
      bindtap="eventHandler"
     >
      <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
    </cover-view>
</template>

<template name="tmpl_10_label">
  <label
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    for="{{utils.getValue(item.for, '')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </label>
</template>

<template name="tmpl_10_checkbox">
  <checkbox
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox>
</template>

<template name="tmpl_10_checkbox-group">
  <checkbox-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox-group>
</template>

<template name="tmpl_10_form">
  <form
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    report-submit="{{utils.getValue(item.reportSubmit, false)}}"
    report-submit-timeout="{{utils.getValue(item.reportSubmitTimeout, 0)}}"
    bindsubmit="eventHandler"
    bindreset="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </form>
</template>

<template name="tmpl_10_radio">
  <radio
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio>
</template>

<template name="tmpl_10_radio-group">
  <radio-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio-group>
</template>

<template name="tmpl_10_picker">
  <picker
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    header-text="{{utils.getValue(item.header-text, '')}}"
    mode="{{utils.getValue(item.mode, 'selector')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    range="{{utils.getValue(item.range, [])}}"
    range-key="{{utils.getValue(item.rangeKey, '')}}"
    value="{{utils.getValue(item.value, 0)}}"
    start="{{utils.getValue(item.start, '')}}"
    end="{{utils.getValue(item.end, '')}}"
    fields="{{utils.getValue(item.fields, 'day')}}"
    custom-item="{{utils.getValue(item.customItem, '')}}"
    bindchange="eventHandler"
    bindcancel="eventHandler"
    bindcolumnchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker>
</template>

<template name="tmpl_10_picker-view">
  <picker-view
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, [])}}"
    indicator-style="{{utils.getValue(item.indicatorStyle, '')}}"
    indicator-class="{{utils.getValue(item.indicatorClass, '')}}"
    mask-style="{{utils.getValue(item.maskStyle, '')}}"
    mask-class="{{utils.getValue(item.maskClass, '')}}"
    bindchange="eventHandler"
    bindpickstart="eventHandler"
    bindpickend="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view>
</template>

<template name="tmpl_10_picker-view-column">
  <picker-view-column
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view-column>
</template>

<template name="tmpl_10_slider">
  <slider
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    min="{{utils.getValue(item.min, 0)}}"
    max="{{utils.getValue(item.max, 100)}}"
    step="{{utils.getValue(item.step, 1)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    value="{{utils.getValue(item.value, 0)}}"
    color="{{utils.getValue(item.color, '#e9e9e9')}}"
    selected-color="{{utils.getValue(item.selectedColor, '#1aad19')}}"
    activeColor="{{utils.getValue(item.activeColor, '#1aad19')}}"
    backgroundColor="{{utils.getValue(item.backgroundColor, '#e9e9e9')}}"
    block-size="{{utils.getValue(item.blockSize, 28)}}"
    block-color="{{utils.getValue(item.blockColor, '#ffffff')}}"
    show-value="{{utils.getValue(item.showValue, false)}}"
    bindchanging="eventHandler"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </slider>
</template>

<template name="tmpl_10_textarea">
  <textarea
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    placeholder-style="{{utils.getValue(item.placeholderStyle, '')}}"
    placeholder-class="{{utils.getValue(item.placeholderClass, 'textarea-placeholder')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    maxlength="{{utils.getValue(item.maxlength, 140)}}"
    auto-focus="{{utils.getValue(item.autoFocus, false)}}"
    focus="{{utils.getValue(item.focus, false)}}"
    auto-height="{{utils.getValue(item.autoHeight, false)}}"
    fixed="{{utils.getValue(item.fixed, false)}}"
    cursor-spacing="{{utils.getValue(item.cursorSpacing, 0)}}"
    cursor="{{utils.getValue(item.cursor, -1)}}"
    show-confirm-bar="{{utils.getValue(item.showConfirmBar, true)}}"
    selection-start="{{utils.getValue(item.selectionStart, -1)}}"
    selection-end="{{utils.getValue(item.selectionEnd, -1)}}"
    adjust-position="{{utils.getValue(item.adjustPosition, true)}}"
    hold-keyboard="{{utils.getValue(item.holdKeyboard, false)}}"
    disable-default-padding="{{utils.getValue(item.disableDefaultPadding, false)}}"
    confirm-type="{{utils.getValue(item.confirmType, 'return')}}"
    confirm-hold="{{utils.getValue(item.confirmHold, false)}}"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindlinechange="eventHandler"
    bindinput="eventHandler"
    bindconfirm="eventHandler"
    bindkeyboardheightchange="eventHandler"
  >
  
  </textarea>
</template>

<template name="tmpl_10_editor">
  <editor
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    read-only="{{utils.getValue(item.readOnly, false)}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    show-img-size="{{utils.getValue(item.showImgSize, false)}}"
    show-img-toolbar="{{utils.getValue(item.showImgToolbar, false)}}"
    show-img-resize="{{utils.getValue(item.showImgResize, false)}}"
    bindready="eventHandler"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindinput="eventHandler"
    bindstatuschange="eventHandler"
  >
  
  </editor>
</template>

<template name="tmpl_10_navigator">
  <navigator
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    target="{{utils.getValue(item.target, 'self')}}"
    url="{{utils.getValue(item.url, '')}}"
    open-type="{{utils.getValue(item.openType, 'navigate')}}"
    delta="{{utils.getValue(item.delta, 1)}}"
    app-id="{{utils.getValue(item.appId, '')}}"
    path="{{utils.getValue(item.path, '')}}"
    extra-data="{{utils.getValue(item.extraData, '', true)}}"
    version="{{utils.getValue(item.version, 'release')}}"
    redirect="{{utils.getValue(item.redirect, false)}}"
    short-link="{{utils.getValue(item.shortLink, '')}}"
    hover-class="{{utils.getValue(item.hoverClass, 'navigator-hover')}}"
    hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation, false)}}"
    hover-start-time="{{utils.getValue(item.hoverStartTime, 50)}}"
    hover-stay-time="{{utils.getValue(item.hoverStayTime, 600)}}"
    bindsuccess="eventHandler"
    bindfail="eventHandler"
    bindcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </navigator>
</template>

<template name="tmpl_10_video">
  <video
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{item.src}}"
    duration="{{utils.getValue(item.duration, 0)}}"
    controls="{{utils.getValue(item.controls, true)}}"
    danmu-list="{{utils.getValue(item.danmuList, [])}}"
    danmu-btn="{{utils.getValue(item.danmuBtn, false)}}"
    enable-danmu="{{utils.getValue(item.enableDanmu, false)}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    loop="{{utils.getValue(item.loop, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    initial-time="{{utils.getValue(item.initialTime, 0)}}"
    page-gesture="{{utils.getValue(item.pageGesture, false)}}"
    direction="{{utils.getValue(item.direction, 0)}}"
    show-progress="{{utils.getValue(item.showProgress, true)}}"
    show-fullscreen-btn="{{utils.getValue(item.showFullscreenBtn, true)}}"
    show-play-btn="{{utils.getValue(item.showPlayBtn, true)}}"
    show-center-play-btn="{{utils.getValue(item.showCenterPlayBtn, true)}}"
    enable-progress-gesture="{{utils.getValue(item.enableProgressGesture, true)}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    poster="{{utils.getValue(item.poster, '')}}"
    show-mute-btn="{{utils.getValue(item.showMuteBtn, false)}}"
    title="{{utils.getValue(item.title, '')}}"
    play-btn-position="{{utils.getValue(item.playBtnPosition, 'bottom')}}"
    enable-play-gesture="{{utils.getValue(item.enablePlayGesture, false)}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    vslide-gesture="{{utils.getValue(item.vslideGesture, false)}}"
    vslide-gesture-in-fullscreen="{{utils.getValue(item.vslideGestureInFullscreen, true)}}"
    ad-unit-id="{{item.adUnitId}}"
    poster-for-crawler="{{item.posterForCrawler}}"
    show-casting-button="{{utils.getValue(item.showCastingButton, false)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    picture-in-picture-show-progress="{{utils.getValue(item.pictureInPictureShowProgress, false)}}"
    enable-auto-rotation="{{utils.getValue(item.enableAutoRotation, false)}}"
    show-screen-lock-button="{{utils.getValue(item.showScreenLockButton, false)}}"
    show-snapshot-button="{{utils.getValue(item.showSnapshotButton, false)}}"
    show-background-playback-button="{{utils.getValue(item.showBackgroundPlaybackButton, false)}}"
    background-poster="{{utils.getValue(item.backgroundPoster, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    is-drm="{{utils.getValue(item.isDrm, false)}}"
    provision-url="{{utils.getValue(item.provisionUrl, '')}}"
    certificate-url="{{utils.getValue(item.certificateUrl, '')}}"
    license-url="{{utils.getValue(item.licenseUrl, '')}}"
    bindplay="eventHandler"
    bindpause="eventHandler"
    bindended="eventHandler"
    bindtimeupdate="eventHandler"
    bindfullscreenchange="eventHandler"
    bindwaiting="eventHandler"
    binderror="eventHandler"
    bindprogress="eventHandler"
    bindloadedmetadata="eventHandler"
    bindcontrolstoggle="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
    bindseekcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </video>
</template>

<template name="tmpl_10_camera">
  <camera
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    mode="{{utils.getValue(item.mode, 'normal')}}"
    resolution="{{utils.getValue(item.resolution, 'medium')}}"
    device-position="{{utils.getValue(item.devicePosition, 'back')}}"
    flash="{{utils.getValue(item.flash, 'auto')}}"
    frame-size="{{utils.getValue(item.frameSize, 'medium')}}"
    bindstop="eventHandler"
    binderror="eventHandler"
    bindinitdone="eventHandler"
    bindscancode="eventHandler"
  >
    
  </camera>
</template>

<template name="tmpl_10_live-player">
  <live-player
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{utils.getValue(item.src, '')}}"
    mode="{{utils.getValue(item.mode, 'live')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    min-cache="{{utils.getValue(item.minCache, 1)}}"
    max-cache="{{utils.getValue(item.maxCache, 3)}}"
    sound-mode="{{utils.getValue(item.soundMode, 'speaker')}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    bindstatechange="eventHandler"
    bindfullscreenchange="eventHandler"
    bindnetstatus="eventHandler"
    bindaudiovolumenotify="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-player>
</template>

<template name="tmpl_10_live-pusher">
  <live-pusher
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    url="{{utils.getValue(item.url, '')}}"
    mode="{{utils.getValue(item.mode, 'RTC')}}"
    autopush="{{utils.getValue(item.autopush, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    enable-camera="{{utils.getValue(item.enableCamera, true)}}"
    auto-focus="{{utils.getValue(item.autoFocus, true)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    beauty="{{utils.getValue(item.beauty, 0)}}"
    whiteness="{{utils.getValue(item.whiteness, 0)}}"
    aspect="{{utils.getValue(item.aspect, '9:16')}}"
    min-bitrate="{{utils.getValue(item.minBitrate, 200)}}"
    max-bitrate="{{utils.getValue(item.maxBitrate, 1000)}}"
    audio-quality="{{utils.getValue(item.audioQuality, 'high')}}"
    waiting-image="{{utils.getValue(item.waitingImage, '')}}"
    waiting-image-hash="{{utils.getValue(item.waitingImageHash, '')}}"
    zoom="{{utils.getValue(item.zoom, false)}}"
    device-position="{{utils.getValue(item.devicePosition, 'front')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    mirror="{{utils.getValue(item.mirror, false)}}"
    remote-mirror="{{utils.getValue(item.remoteMirror, false)}}"
    local-mirror="{{utils.getValue(item.localMirror, 'auto')}}"
    audio-reverb-type="{{utils.getValue(item.audioReverbType, 0)}}"
    enable-mic="{{utils.getValue(item.enableMic, true)}}"
    enable-agc="{{utils.getValue(item.enableAgc, false)}}"
    enable-ans="{{utils.getValue(item.enableAns, false)}}"
    audio-volume-type="{{utils.getValue(item.audioVolumeType, 'auto')}}"
    video-width="{{utils.getValue(item.videoWidth, 360)}}"
    video-height="{{utils.getValue(item.videoHeight, 640)}}"
    beauty-style="{{utils.getValue(item.beautyStyle, 'smooth')}}"
    filter="{{utils.getValue(item.filter, 'standard')}}"
    bindstatechange="eventHandler"
    bindnetstatus="eventHandler"
    binderror="eventHandler"
    bindbgmstart="eventHandler"
    bindbgmprogress="eventHandler"
    bindbgmcomplete="eventHandler"
    bindaudiovolumenotify="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-pusher>
</template>

<template name="tmpl_10_canvas">
  <canvas
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    type="{{utils.getValue(item.type, '')}}"
    canvas-id="{{utils.getValue(item.canvasId, '')}}"
    disable-scroll="{{utils.getValue(item.disableScroll, false)}}"
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchend="eventHandler"
    bindtouchcancel="eventHandler"
    bindlongtap="eventHandler"
    binderror="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </canvas>
</template>

<template name="tmpl_10_keyboard-accessory">
  <keyboard-accessory
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </keyboard-accessory>
</template>

      

<template name="container_11"><block wx:for="{{vdoms}}" wx:key="key"><template is="{{utils.getTagName(item, cid)}}" data="{{item: item, cid}}"></template></block></template>

<template name="tmpl_11_view">
  <view wx:key="{{item.key}}" data-data="{{item.dataSet}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_11_pure-view">
  <view wx:key="{{item.key}}" style="{{item.style}}" class="{{item.class}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_11_static-view">
  <view wx:key="{{item.key}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}"  style="{{item.style}}" class="{{item.class}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>


<template name="tmpl_11_text">
  <text wx:key="{{item.key}}" data-data="{{item.dataSet}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}"><template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" /></text>
</template>

<template name="tmpl_11_template">
  <template is="{{item.is}}" data="{{...item.data}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </template>
</template>

<template name="tmpl_11_button">
  <button wx:key="{{item.key}}" data-data="{{item.dataSet}}" size="{{utils.getValue(item.size,'default')}}" type="{{item.type}}" plain="{{utils.getValue(item.plain,false)}}" disabled="{{item.disabled}}" loading="{{utils.getValue(item.loading,false)}}" form-type="{{item.formType}}" open-type="{{item.openType}}" hover-class="{{utils.getValue(item.hoverClass,'button-hover')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,20)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,70)}}" name="{{item.name}}" lang="{{utils.getValue(item.lang,en)}}" session-from="{{item.sessionFrom}}" send-message-title="{{item.sendMessageTitle}}" send-message-path="{{item.sendMessagePath}}" send-message-img="{{item.sendMessageImg}}" app-parameter="{{item.appParameter}}" show-message-card="{{utils.getValue(item.showMessageCard,false)}}" business-id="{{item.businessId}}" bindgetuserinfo="eventHandler" bindcontact="eventHandler" bindgetphonenumber="eventHandler" binderror="eventHandler" bindopensetting="eventHandler" bindlaunchapp="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </button>
</template>

<template name="tmpl_11_input">
  <template is="{{utils.isFocus(item, 'tmpl_11_')}}" data="{{item:item}}" />
</template>

<template name="tmpl_11_input_focus">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" focus="{{utils.getValue(item.focus,false)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_11_input_blur">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_11_text-node"> {{item.content}} </template>

<template name="tmpl_11_navbar">
  <navbar id="{{item.uid}}" title="{{item.title}}" noPlaceholder="{{item.noPlaceholder}}" css="{{item.css}}"></navbar>
</template>

<template name="tmpl_11_keyboard">
  <keyboard id="{{item.uid}}" limit="{{item.limit}}" price="{{item.price}}" bind:change="eventHandler" bind:confirm="eventHandler"></keyboard>
</template>

<template name="tmpl_11_scroll-view">
  <scroll-view wx:key="{{item.key}}" data-data="{{item.dataSet}}" scroll-x="{{utils.getValue(item.scrollX,false)}}" scroll-y="{{utils.getValue(item.scrollY,false)}}" upper-threshold="{{utils.getValue(item.upperThreshold,50)}}" lower-threshold="{{utils.getValue(item.lowerThreshold,50)}}" scroll-top="{{item.scrollTop}}" scroll-left="{{item.scrollLeft}}" scroll-into-view="{{item.scrollIntoView}}" scroll-with-animation="{{utils.getValue(item.scrollWithAnimation,false)}}" enable-back-to-top="{{utils.getValue(item.enableBackToTop,false)}}" bindscrolltoupper="eventHandler" bindscrolltolower="eventHandler" bindscroll="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" enable-flex="{{utils.getValue(item.enableFlex,false)}}" scroll-anchoring="{{utils.getValue(item.scrollAnchoring,false)}}" refresher-enabled="{{utils.getValue(item.refresherEnabled,false)}}" refresher-threshold="{{utils.getValue(item.refresherThreshold,45)}}" refresher-default-style="{{utils.getValue(item.refresherDefaultStyle,'black')}}" refresher-background="{{utils.getValue(item.refresherBackground,'#FFF')}}" refresher-triggered="{{utils.getValue(item.refresherTriggered,false)}}" enhanced="{{utils.getValue(item.enhanced,false)}}" bounces="{{utils.getValue(item.bounces,true)}}" show-scrollbar="{{utils.getValue(item.showScrollbar,true)}}" paging-enabled="{{utils.getValue(item.pagingEnabled,false)}}" fast-deceleration="{{utils.getValue(item.fastDeceleration,false)}}" binddragstart="eventHandler" binddragging="eventHandler" binddragend="eventHandler" bindrefresherpulling="eventHandler" bindrefresherrefresh="eventHandler" bindrefresherrestore="eventHandler" bindrefresherabort="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </scroll-view>
</template>

<template name="tmpl_11_block">
  <block wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </block>
</template>

<template name="tmpl_11_null">
</template>

<template name="tmpl_11_image">
  <image wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" src="{{item.src}}" mode="{{utils.getValue(item.mode,'scaleToFill')}}" lazy-load="{{utils.getValue(item.lazyLoad,false)}}" binderror="eventHandler" bindload="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" webp="{{utils.getValue(item.webp,false)}}" show-menu-by-longpress="{{utils.getValue(item.showMenuByLongpress,false)}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </image>
</template>

<template name="tmpl_11_icon">
  <icon wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" size="{{utils.getValue(item.size, 23)}}" type="{{item.type}}" color="{{utils.getValue(item.color, '')}}" bind:tap="eventHandler"/>
</template>

<template name="tmpl_11_i">
  <i 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    style="{{item.style}}" class="{{item.class}}" 
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchcancel="eventHandler"
    bindtouchend="eventHandler"
    bindtap="eventHandler"
    bindlongpress="eventHandler"
    bindlongtap="eventHandler"
    bindtransitionend="eventHandler"
    bindanimationstart="eventHandler"
    bindanimationiteration="eventHandler"
    bindanimationend="eventHandler"
    bindtouchforcechange="eventHandler"
  />
</template>

<template name="tmpl_11_progress">
  <progress wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" percent="{{utils.getValue(item.percent, 0)}}" color="{{utils.getValue(item.color, '#09BB07')}}" activeColor="{{utils.getValue(item.activeColor, '#09BB07')}}" backgroundColor="{{utils.getValue(item.backgroundColor, '#EBEBEB')}}" active="{{utils.getValue(item.active, false)}}" active-mode="{{utils.getValue(item.activeMode, backwards)}}"  show-info="{{utils.getValue(item.showInfo, false)}}" stroke-width="{{utils.getValue(item.strokeWidth, 6)}}" border-radius="{{util.getValue(item.borderRadius, 0)}}" font-size="{{utils.getValue(item.fontSize, 16)}}" duration="{{utils.getValue(item.duration, 30)}}" bindactiveend="eventHandler"/>
</template>

<template name="tmpl_11_rich-text">
  <rich-text wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" nodes="{{utils.getValue(item.nodes, [])}}" space="{{util.getVaue(item.space, 'ensp')}}" ></rich-text>
</template>

<template name="tmpl_11_swiper">
  <swiper
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"  
    indicator-dots="{{utils.getValue(item.indicatorDots, false)}}"
    indicator-color="{{utils.getValue(item.indicatorcolor, 'rgba(0, 0, 0, .3)')}}"
    indicator-active-color="{{utils.getValue(item.indicatorActiveColor, '#000000')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}" 
    interval="{{utils.getValue(item.interval, 5000)}}" 
    duration="{{utils.getValue(item.duration, 500)}}"
    circular="{{utils.getValue(item.circular, false)}}" 
    vertical="{{utils.getValue(item.vertical, false)}}" 
    previous-margin="{{utils.getValue(item.previousMargin, '0px')}}" 
    next-margin="{{utils.getValue(item.nextMargin, '0px')}}" 
    snap-to-edge="{{utils.getValue(item.snapToEdge, false)}}" 
    display-multiple-items="{{utils.getValue(item.displayMultipleItems, 1)}}" 
    easing-function="{{utils.getValue(item.easingFunction, 'default')}}" 
    bindchange="eventHandler"
    bindtransition="eventHandler"
    bindanimationfinish="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper>
</template>

<template name="tmpl_11_swiper-item">
  <swiper-item wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  item-id="{{utils.getValue(item.itemId, '')}}" skip-hidden-item-layout="{{utils.getValue(item.skipHiddenItemLayout, '')}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper-item>
</template>

<template name="tmpl_11_switch">
  <switch wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  checked="{{utils.getValue(item.checked, false)}}" disabled="{{utils.getValue(item.disabled, false)}}" type="{{utils.getValue(item.type, 'switch')}}" color="{{utils.getValue(item.color, '#04BE02')}}" bindchange="eventHandler"/>
</template>

<template name="tmpl_11_movable-area">
  <movable-area 
    style="{{item.style}}" class="{{item.class}}" 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    scale-area="{{utils.getValue(item.scaleArea, false)}}" 
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-area>
</template>

<template name="tmpl_11_movable-view">
  <movable-view 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}" 
    direction="{{utils.getValue(item.direction, 'none')}}"
    inertia="{{utils.getValue(item.inertia, false)}}"
    out-of-bounds="{{utils.getValue(item.outOfBounds, false)}}"
    x="{{utils.getValue(item.x, '0')}}"
    y="{{utils.getValue(item.y, '0')}}"
    damping="{{utils.getValue(item.damping, 20)}}"
    friction="{{utils.getValue(item.friction, 2)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    scale="{{utils.getValue(item.scale, false)}}"
    scale-min="{{utils.getValue(item.scaleMin, 0.5)}}"
    scale-max="{{utils.getValue(item.scaleMax, 10)}}"
    scale-value="{{utils.getValue(item.scaleValue, 1)}}"
    animation="{{utils.getValue(item.animation, false)}}"
    width="{{utils.getValue(item.width, '10px')}}"
    height="{{utils.getValue(item.height, '10px')}}"
    bindchange="eventHandler"
    bindscale="eventHandler"
    bindhtouchmove="eventHandler"
    bindvtouchmove="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-view>
</template>

<template name="tmpl_11_map">
  <map
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    longitude="{{item.longitude}}"
    latitude="{{item.latitude}}"scale
    scale="{{utils.getValue(item.scale, 16)}}"
    min-scale="{{utils.getValue(item.minScale, 3)}}"
    max-scale="{{utils.getValue(item.maxScale, 20)}}"
    markers="{{utils.getValue(item.markers, [])}}"
    covers="{{utils.getValue(item.covers, [])}}"
    polyline="{{utils.getValue(item.polyline, [])}}"
    circles="{{utils.getValue(item.circles, [])}}"
    controls="{{utils.getValue(item.controls, [])}}"
    include-points="{{utils.getValue(item.includePoints, [])}}"
    show-location="{{utils.getValue(item.showLocation, false)}}"
    polygons="{{utils.getValue(item.polygons, [])}}"
    subkey="{{utils.getValue(item.subkey, '')}}"
    setting="{{utils.getValue(item.setting, '', true)}}"
    layer-style="{{utils.getValue(item.layerStyle, 1)}}"
    rotate="0utils.getValue(item.rotate, 0)}}"
    enable-3D="{{utils.getValue(item.enable3D, false)}}"
    show-compass="{{utils.getValue(item.showCompass, false)}}"
    show-scale="{{utils.getValue(item.showScale, false)}}"
    enable-overlooking="{{utils.getValue(item.enableOverlooking, false)}}"
    enable-zoom="{{utils.getValue(item.enableZoom, true)}}"
    enable-scroll="{{utils.getValue(item.scale, true)}}"
    enable-rotate="{{utils.getValue(item.enableRotate, false)}}"
    enable-satellite="{{utils.getValue(item.enableSatellite, false)}}"
    enable-traffic="{{utils.getValue(item.enableTraffic, false)}}"
    enable-poi="{{utils.getValue(item.enablePoi, true)}}"
    enable-building="{{utils.getValue(item.enableBuilding, false)}}"
    bindtap="eventHandler"
    bindmarkertap="eventHandler"
    bindlabeltap="eventHandler"
    bindcontroltap="eventHandler"
    bindcallouttap="eventHandler"
    bindregionchange="eventHandler"
    bindpoitap="eventHandler"
    bindanchorpointtap="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </map>
</template>

<template name="tmpl_11_cover-view">
    <cover-view
      wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
      style="{{item.style}}" class="{{item.class}}"
      hover-class="{{utils.getValue(item.hoverClass,'none')}}"
      hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}"
      hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}"
      hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}"
      animation="{{item.animation}}"
      bindtouchstart="eventHandler"
      bindtouchmove="eventHandler"
      bindtouchend="eventHandler"
      bindtouchcancel="eventHandler"
      bindlongpress="eventHandler"
      bindanimationstart="eventHandler"
      bindanimationiteration="eventHandler"
      bindanimationend="eventHandler"
      bindtransitionend="eventHandler"
      bindtap="eventHandler"
     >
      <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
    </cover-view>
</template>

<template name="tmpl_11_label">
  <label
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    for="{{utils.getValue(item.for, '')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </label>
</template>

<template name="tmpl_11_checkbox">
  <checkbox
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox>
</template>

<template name="tmpl_11_checkbox-group">
  <checkbox-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox-group>
</template>

<template name="tmpl_11_form">
  <form
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    report-submit="{{utils.getValue(item.reportSubmit, false)}}"
    report-submit-timeout="{{utils.getValue(item.reportSubmitTimeout, 0)}}"
    bindsubmit="eventHandler"
    bindreset="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </form>
</template>

<template name="tmpl_11_radio">
  <radio
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio>
</template>

<template name="tmpl_11_radio-group">
  <radio-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio-group>
</template>

<template name="tmpl_11_picker">
  <picker
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    header-text="{{utils.getValue(item.header-text, '')}}"
    mode="{{utils.getValue(item.mode, 'selector')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    range="{{utils.getValue(item.range, [])}}"
    range-key="{{utils.getValue(item.rangeKey, '')}}"
    value="{{utils.getValue(item.value, 0)}}"
    start="{{utils.getValue(item.start, '')}}"
    end="{{utils.getValue(item.end, '')}}"
    fields="{{utils.getValue(item.fields, 'day')}}"
    custom-item="{{utils.getValue(item.customItem, '')}}"
    bindchange="eventHandler"
    bindcancel="eventHandler"
    bindcolumnchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker>
</template>

<template name="tmpl_11_picker-view">
  <picker-view
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, [])}}"
    indicator-style="{{utils.getValue(item.indicatorStyle, '')}}"
    indicator-class="{{utils.getValue(item.indicatorClass, '')}}"
    mask-style="{{utils.getValue(item.maskStyle, '')}}"
    mask-class="{{utils.getValue(item.maskClass, '')}}"
    bindchange="eventHandler"
    bindpickstart="eventHandler"
    bindpickend="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view>
</template>

<template name="tmpl_11_picker-view-column">
  <picker-view-column
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view-column>
</template>

<template name="tmpl_11_slider">
  <slider
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    min="{{utils.getValue(item.min, 0)}}"
    max="{{utils.getValue(item.max, 100)}}"
    step="{{utils.getValue(item.step, 1)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    value="{{utils.getValue(item.value, 0)}}"
    color="{{utils.getValue(item.color, '#e9e9e9')}}"
    selected-color="{{utils.getValue(item.selectedColor, '#1aad19')}}"
    activeColor="{{utils.getValue(item.activeColor, '#1aad19')}}"
    backgroundColor="{{utils.getValue(item.backgroundColor, '#e9e9e9')}}"
    block-size="{{utils.getValue(item.blockSize, 28)}}"
    block-color="{{utils.getValue(item.blockColor, '#ffffff')}}"
    show-value="{{utils.getValue(item.showValue, false)}}"
    bindchanging="eventHandler"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </slider>
</template>

<template name="tmpl_11_textarea">
  <textarea
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    placeholder-style="{{utils.getValue(item.placeholderStyle, '')}}"
    placeholder-class="{{utils.getValue(item.placeholderClass, 'textarea-placeholder')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    maxlength="{{utils.getValue(item.maxlength, 140)}}"
    auto-focus="{{utils.getValue(item.autoFocus, false)}}"
    focus="{{utils.getValue(item.focus, false)}}"
    auto-height="{{utils.getValue(item.autoHeight, false)}}"
    fixed="{{utils.getValue(item.fixed, false)}}"
    cursor-spacing="{{utils.getValue(item.cursorSpacing, 0)}}"
    cursor="{{utils.getValue(item.cursor, -1)}}"
    show-confirm-bar="{{utils.getValue(item.showConfirmBar, true)}}"
    selection-start="{{utils.getValue(item.selectionStart, -1)}}"
    selection-end="{{utils.getValue(item.selectionEnd, -1)}}"
    adjust-position="{{utils.getValue(item.adjustPosition, true)}}"
    hold-keyboard="{{utils.getValue(item.holdKeyboard, false)}}"
    disable-default-padding="{{utils.getValue(item.disableDefaultPadding, false)}}"
    confirm-type="{{utils.getValue(item.confirmType, 'return')}}"
    confirm-hold="{{utils.getValue(item.confirmHold, false)}}"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindlinechange="eventHandler"
    bindinput="eventHandler"
    bindconfirm="eventHandler"
    bindkeyboardheightchange="eventHandler"
  >
  
  </textarea>
</template>

<template name="tmpl_11_editor">
  <editor
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    read-only="{{utils.getValue(item.readOnly, false)}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    show-img-size="{{utils.getValue(item.showImgSize, false)}}"
    show-img-toolbar="{{utils.getValue(item.showImgToolbar, false)}}"
    show-img-resize="{{utils.getValue(item.showImgResize, false)}}"
    bindready="eventHandler"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindinput="eventHandler"
    bindstatuschange="eventHandler"
  >
  
  </editor>
</template>

<template name="tmpl_11_navigator">
  <navigator
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    target="{{utils.getValue(item.target, 'self')}}"
    url="{{utils.getValue(item.url, '')}}"
    open-type="{{utils.getValue(item.openType, 'navigate')}}"
    delta="{{utils.getValue(item.delta, 1)}}"
    app-id="{{utils.getValue(item.appId, '')}}"
    path="{{utils.getValue(item.path, '')}}"
    extra-data="{{utils.getValue(item.extraData, '', true)}}"
    version="{{utils.getValue(item.version, 'release')}}"
    redirect="{{utils.getValue(item.redirect, false)}}"
    short-link="{{utils.getValue(item.shortLink, '')}}"
    hover-class="{{utils.getValue(item.hoverClass, 'navigator-hover')}}"
    hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation, false)}}"
    hover-start-time="{{utils.getValue(item.hoverStartTime, 50)}}"
    hover-stay-time="{{utils.getValue(item.hoverStayTime, 600)}}"
    bindsuccess="eventHandler"
    bindfail="eventHandler"
    bindcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </navigator>
</template>

<template name="tmpl_11_video">
  <video
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{item.src}}"
    duration="{{utils.getValue(item.duration, 0)}}"
    controls="{{utils.getValue(item.controls, true)}}"
    danmu-list="{{utils.getValue(item.danmuList, [])}}"
    danmu-btn="{{utils.getValue(item.danmuBtn, false)}}"
    enable-danmu="{{utils.getValue(item.enableDanmu, false)}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    loop="{{utils.getValue(item.loop, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    initial-time="{{utils.getValue(item.initialTime, 0)}}"
    page-gesture="{{utils.getValue(item.pageGesture, false)}}"
    direction="{{utils.getValue(item.direction, 0)}}"
    show-progress="{{utils.getValue(item.showProgress, true)}}"
    show-fullscreen-btn="{{utils.getValue(item.showFullscreenBtn, true)}}"
    show-play-btn="{{utils.getValue(item.showPlayBtn, true)}}"
    show-center-play-btn="{{utils.getValue(item.showCenterPlayBtn, true)}}"
    enable-progress-gesture="{{utils.getValue(item.enableProgressGesture, true)}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    poster="{{utils.getValue(item.poster, '')}}"
    show-mute-btn="{{utils.getValue(item.showMuteBtn, false)}}"
    title="{{utils.getValue(item.title, '')}}"
    play-btn-position="{{utils.getValue(item.playBtnPosition, 'bottom')}}"
    enable-play-gesture="{{utils.getValue(item.enablePlayGesture, false)}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    vslide-gesture="{{utils.getValue(item.vslideGesture, false)}}"
    vslide-gesture-in-fullscreen="{{utils.getValue(item.vslideGestureInFullscreen, true)}}"
    ad-unit-id="{{item.adUnitId}}"
    poster-for-crawler="{{item.posterForCrawler}}"
    show-casting-button="{{utils.getValue(item.showCastingButton, false)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    picture-in-picture-show-progress="{{utils.getValue(item.pictureInPictureShowProgress, false)}}"
    enable-auto-rotation="{{utils.getValue(item.enableAutoRotation, false)}}"
    show-screen-lock-button="{{utils.getValue(item.showScreenLockButton, false)}}"
    show-snapshot-button="{{utils.getValue(item.showSnapshotButton, false)}}"
    show-background-playback-button="{{utils.getValue(item.showBackgroundPlaybackButton, false)}}"
    background-poster="{{utils.getValue(item.backgroundPoster, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    is-drm="{{utils.getValue(item.isDrm, false)}}"
    provision-url="{{utils.getValue(item.provisionUrl, '')}}"
    certificate-url="{{utils.getValue(item.certificateUrl, '')}}"
    license-url="{{utils.getValue(item.licenseUrl, '')}}"
    bindplay="eventHandler"
    bindpause="eventHandler"
    bindended="eventHandler"
    bindtimeupdate="eventHandler"
    bindfullscreenchange="eventHandler"
    bindwaiting="eventHandler"
    binderror="eventHandler"
    bindprogress="eventHandler"
    bindloadedmetadata="eventHandler"
    bindcontrolstoggle="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
    bindseekcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </video>
</template>

<template name="tmpl_11_camera">
  <camera
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    mode="{{utils.getValue(item.mode, 'normal')}}"
    resolution="{{utils.getValue(item.resolution, 'medium')}}"
    device-position="{{utils.getValue(item.devicePosition, 'back')}}"
    flash="{{utils.getValue(item.flash, 'auto')}}"
    frame-size="{{utils.getValue(item.frameSize, 'medium')}}"
    bindstop="eventHandler"
    binderror="eventHandler"
    bindinitdone="eventHandler"
    bindscancode="eventHandler"
  >
    
  </camera>
</template>

<template name="tmpl_11_live-player">
  <live-player
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{utils.getValue(item.src, '')}}"
    mode="{{utils.getValue(item.mode, 'live')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    min-cache="{{utils.getValue(item.minCache, 1)}}"
    max-cache="{{utils.getValue(item.maxCache, 3)}}"
    sound-mode="{{utils.getValue(item.soundMode, 'speaker')}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    bindstatechange="eventHandler"
    bindfullscreenchange="eventHandler"
    bindnetstatus="eventHandler"
    bindaudiovolumenotify="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-player>
</template>

<template name="tmpl_11_live-pusher">
  <live-pusher
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    url="{{utils.getValue(item.url, '')}}"
    mode="{{utils.getValue(item.mode, 'RTC')}}"
    autopush="{{utils.getValue(item.autopush, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    enable-camera="{{utils.getValue(item.enableCamera, true)}}"
    auto-focus="{{utils.getValue(item.autoFocus, true)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    beauty="{{utils.getValue(item.beauty, 0)}}"
    whiteness="{{utils.getValue(item.whiteness, 0)}}"
    aspect="{{utils.getValue(item.aspect, '9:16')}}"
    min-bitrate="{{utils.getValue(item.minBitrate, 200)}}"
    max-bitrate="{{utils.getValue(item.maxBitrate, 1000)}}"
    audio-quality="{{utils.getValue(item.audioQuality, 'high')}}"
    waiting-image="{{utils.getValue(item.waitingImage, '')}}"
    waiting-image-hash="{{utils.getValue(item.waitingImageHash, '')}}"
    zoom="{{utils.getValue(item.zoom, false)}}"
    device-position="{{utils.getValue(item.devicePosition, 'front')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    mirror="{{utils.getValue(item.mirror, false)}}"
    remote-mirror="{{utils.getValue(item.remoteMirror, false)}}"
    local-mirror="{{utils.getValue(item.localMirror, 'auto')}}"
    audio-reverb-type="{{utils.getValue(item.audioReverbType, 0)}}"
    enable-mic="{{utils.getValue(item.enableMic, true)}}"
    enable-agc="{{utils.getValue(item.enableAgc, false)}}"
    enable-ans="{{utils.getValue(item.enableAns, false)}}"
    audio-volume-type="{{utils.getValue(item.audioVolumeType, 'auto')}}"
    video-width="{{utils.getValue(item.videoWidth, 360)}}"
    video-height="{{utils.getValue(item.videoHeight, 640)}}"
    beauty-style="{{utils.getValue(item.beautyStyle, 'smooth')}}"
    filter="{{utils.getValue(item.filter, 'standard')}}"
    bindstatechange="eventHandler"
    bindnetstatus="eventHandler"
    binderror="eventHandler"
    bindbgmstart="eventHandler"
    bindbgmprogress="eventHandler"
    bindbgmcomplete="eventHandler"
    bindaudiovolumenotify="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-pusher>
</template>

<template name="tmpl_11_canvas">
  <canvas
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    type="{{utils.getValue(item.type, '')}}"
    canvas-id="{{utils.getValue(item.canvasId, '')}}"
    disable-scroll="{{utils.getValue(item.disableScroll, false)}}"
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchend="eventHandler"
    bindtouchcancel="eventHandler"
    bindlongtap="eventHandler"
    binderror="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </canvas>
</template>

<template name="tmpl_11_keyboard-accessory">
  <keyboard-accessory
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </keyboard-accessory>
</template>

      

<template name="container_12"><block wx:for="{{vdoms}}" wx:key="key"><template is="{{utils.getTagName(item, cid)}}" data="{{item: item, cid}}"></template></block></template>

<template name="tmpl_12_view">
  <view wx:key="{{item.key}}" data-data="{{item.dataSet}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_12_pure-view">
  <view wx:key="{{item.key}}" style="{{item.style}}" class="{{item.class}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_12_static-view">
  <view wx:key="{{item.key}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}"  style="{{item.style}}" class="{{item.class}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>


<template name="tmpl_12_text">
  <text wx:key="{{item.key}}" data-data="{{item.dataSet}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}"><template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" /></text>
</template>

<template name="tmpl_12_template">
  <template is="{{item.is}}" data="{{...item.data}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </template>
</template>

<template name="tmpl_12_button">
  <button wx:key="{{item.key}}" data-data="{{item.dataSet}}" size="{{utils.getValue(item.size,'default')}}" type="{{item.type}}" plain="{{utils.getValue(item.plain,false)}}" disabled="{{item.disabled}}" loading="{{utils.getValue(item.loading,false)}}" form-type="{{item.formType}}" open-type="{{item.openType}}" hover-class="{{utils.getValue(item.hoverClass,'button-hover')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,20)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,70)}}" name="{{item.name}}" lang="{{utils.getValue(item.lang,en)}}" session-from="{{item.sessionFrom}}" send-message-title="{{item.sendMessageTitle}}" send-message-path="{{item.sendMessagePath}}" send-message-img="{{item.sendMessageImg}}" app-parameter="{{item.appParameter}}" show-message-card="{{utils.getValue(item.showMessageCard,false)}}" business-id="{{item.businessId}}" bindgetuserinfo="eventHandler" bindcontact="eventHandler" bindgetphonenumber="eventHandler" binderror="eventHandler" bindopensetting="eventHandler" bindlaunchapp="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </button>
</template>

<template name="tmpl_12_input">
  <template is="{{utils.isFocus(item, 'tmpl_12_')}}" data="{{item:item}}" />
</template>

<template name="tmpl_12_input_focus">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" focus="{{utils.getValue(item.focus,false)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_12_input_blur">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_12_text-node"> {{item.content}} </template>

<template name="tmpl_12_navbar">
  <navbar id="{{item.uid}}" title="{{item.title}}" noPlaceholder="{{item.noPlaceholder}}" css="{{item.css}}"></navbar>
</template>

<template name="tmpl_12_keyboard">
  <keyboard id="{{item.uid}}" limit="{{item.limit}}" price="{{item.price}}" bind:change="eventHandler" bind:confirm="eventHandler"></keyboard>
</template>

<template name="tmpl_12_scroll-view">
  <scroll-view wx:key="{{item.key}}" data-data="{{item.dataSet}}" scroll-x="{{utils.getValue(item.scrollX,false)}}" scroll-y="{{utils.getValue(item.scrollY,false)}}" upper-threshold="{{utils.getValue(item.upperThreshold,50)}}" lower-threshold="{{utils.getValue(item.lowerThreshold,50)}}" scroll-top="{{item.scrollTop}}" scroll-left="{{item.scrollLeft}}" scroll-into-view="{{item.scrollIntoView}}" scroll-with-animation="{{utils.getValue(item.scrollWithAnimation,false)}}" enable-back-to-top="{{utils.getValue(item.enableBackToTop,false)}}" bindscrolltoupper="eventHandler" bindscrolltolower="eventHandler" bindscroll="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" enable-flex="{{utils.getValue(item.enableFlex,false)}}" scroll-anchoring="{{utils.getValue(item.scrollAnchoring,false)}}" refresher-enabled="{{utils.getValue(item.refresherEnabled,false)}}" refresher-threshold="{{utils.getValue(item.refresherThreshold,45)}}" refresher-default-style="{{utils.getValue(item.refresherDefaultStyle,'black')}}" refresher-background="{{utils.getValue(item.refresherBackground,'#FFF')}}" refresher-triggered="{{utils.getValue(item.refresherTriggered,false)}}" enhanced="{{utils.getValue(item.enhanced,false)}}" bounces="{{utils.getValue(item.bounces,true)}}" show-scrollbar="{{utils.getValue(item.showScrollbar,true)}}" paging-enabled="{{utils.getValue(item.pagingEnabled,false)}}" fast-deceleration="{{utils.getValue(item.fastDeceleration,false)}}" binddragstart="eventHandler" binddragging="eventHandler" binddragend="eventHandler" bindrefresherpulling="eventHandler" bindrefresherrefresh="eventHandler" bindrefresherrestore="eventHandler" bindrefresherabort="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </scroll-view>
</template>

<template name="tmpl_12_block">
  <block wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </block>
</template>

<template name="tmpl_12_null">
</template>

<template name="tmpl_12_image">
  <image wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" src="{{item.src}}" mode="{{utils.getValue(item.mode,'scaleToFill')}}" lazy-load="{{utils.getValue(item.lazyLoad,false)}}" binderror="eventHandler" bindload="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" webp="{{utils.getValue(item.webp,false)}}" show-menu-by-longpress="{{utils.getValue(item.showMenuByLongpress,false)}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </image>
</template>

<template name="tmpl_12_icon">
  <icon wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" size="{{utils.getValue(item.size, 23)}}" type="{{item.type}}" color="{{utils.getValue(item.color, '')}}" bind:tap="eventHandler"/>
</template>

<template name="tmpl_12_i">
  <i 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    style="{{item.style}}" class="{{item.class}}" 
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchcancel="eventHandler"
    bindtouchend="eventHandler"
    bindtap="eventHandler"
    bindlongpress="eventHandler"
    bindlongtap="eventHandler"
    bindtransitionend="eventHandler"
    bindanimationstart="eventHandler"
    bindanimationiteration="eventHandler"
    bindanimationend="eventHandler"
    bindtouchforcechange="eventHandler"
  />
</template>

<template name="tmpl_12_progress">
  <progress wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" percent="{{utils.getValue(item.percent, 0)}}" color="{{utils.getValue(item.color, '#09BB07')}}" activeColor="{{utils.getValue(item.activeColor, '#09BB07')}}" backgroundColor="{{utils.getValue(item.backgroundColor, '#EBEBEB')}}" active="{{utils.getValue(item.active, false)}}" active-mode="{{utils.getValue(item.activeMode, backwards)}}"  show-info="{{utils.getValue(item.showInfo, false)}}" stroke-width="{{utils.getValue(item.strokeWidth, 6)}}" border-radius="{{util.getValue(item.borderRadius, 0)}}" font-size="{{utils.getValue(item.fontSize, 16)}}" duration="{{utils.getValue(item.duration, 30)}}" bindactiveend="eventHandler"/>
</template>

<template name="tmpl_12_rich-text">
  <rich-text wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" nodes="{{utils.getValue(item.nodes, [])}}" space="{{util.getVaue(item.space, 'ensp')}}" ></rich-text>
</template>

<template name="tmpl_12_swiper">
  <swiper
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"  
    indicator-dots="{{utils.getValue(item.indicatorDots, false)}}"
    indicator-color="{{utils.getValue(item.indicatorcolor, 'rgba(0, 0, 0, .3)')}}"
    indicator-active-color="{{utils.getValue(item.indicatorActiveColor, '#000000')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}" 
    interval="{{utils.getValue(item.interval, 5000)}}" 
    duration="{{utils.getValue(item.duration, 500)}}"
    circular="{{utils.getValue(item.circular, false)}}" 
    vertical="{{utils.getValue(item.vertical, false)}}" 
    previous-margin="{{utils.getValue(item.previousMargin, '0px')}}" 
    next-margin="{{utils.getValue(item.nextMargin, '0px')}}" 
    snap-to-edge="{{utils.getValue(item.snapToEdge, false)}}" 
    display-multiple-items="{{utils.getValue(item.displayMultipleItems, 1)}}" 
    easing-function="{{utils.getValue(item.easingFunction, 'default')}}" 
    bindchange="eventHandler"
    bindtransition="eventHandler"
    bindanimationfinish="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper>
</template>

<template name="tmpl_12_swiper-item">
  <swiper-item wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  item-id="{{utils.getValue(item.itemId, '')}}" skip-hidden-item-layout="{{utils.getValue(item.skipHiddenItemLayout, '')}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper-item>
</template>

<template name="tmpl_12_switch">
  <switch wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  checked="{{utils.getValue(item.checked, false)}}" disabled="{{utils.getValue(item.disabled, false)}}" type="{{utils.getValue(item.type, 'switch')}}" color="{{utils.getValue(item.color, '#04BE02')}}" bindchange="eventHandler"/>
</template>

<template name="tmpl_12_movable-area">
  <movable-area 
    style="{{item.style}}" class="{{item.class}}" 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    scale-area="{{utils.getValue(item.scaleArea, false)}}" 
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-area>
</template>

<template name="tmpl_12_movable-view">
  <movable-view 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}" 
    direction="{{utils.getValue(item.direction, 'none')}}"
    inertia="{{utils.getValue(item.inertia, false)}}"
    out-of-bounds="{{utils.getValue(item.outOfBounds, false)}}"
    x="{{utils.getValue(item.x, '0')}}"
    y="{{utils.getValue(item.y, '0')}}"
    damping="{{utils.getValue(item.damping, 20)}}"
    friction="{{utils.getValue(item.friction, 2)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    scale="{{utils.getValue(item.scale, false)}}"
    scale-min="{{utils.getValue(item.scaleMin, 0.5)}}"
    scale-max="{{utils.getValue(item.scaleMax, 10)}}"
    scale-value="{{utils.getValue(item.scaleValue, 1)}}"
    animation="{{utils.getValue(item.animation, false)}}"
    width="{{utils.getValue(item.width, '10px')}}"
    height="{{utils.getValue(item.height, '10px')}}"
    bindchange="eventHandler"
    bindscale="eventHandler"
    bindhtouchmove="eventHandler"
    bindvtouchmove="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-view>
</template>

<template name="tmpl_12_map">
  <map
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    longitude="{{item.longitude}}"
    latitude="{{item.latitude}}"scale
    scale="{{utils.getValue(item.scale, 16)}}"
    min-scale="{{utils.getValue(item.minScale, 3)}}"
    max-scale="{{utils.getValue(item.maxScale, 20)}}"
    markers="{{utils.getValue(item.markers, [])}}"
    covers="{{utils.getValue(item.covers, [])}}"
    polyline="{{utils.getValue(item.polyline, [])}}"
    circles="{{utils.getValue(item.circles, [])}}"
    controls="{{utils.getValue(item.controls, [])}}"
    include-points="{{utils.getValue(item.includePoints, [])}}"
    show-location="{{utils.getValue(item.showLocation, false)}}"
    polygons="{{utils.getValue(item.polygons, [])}}"
    subkey="{{utils.getValue(item.subkey, '')}}"
    setting="{{utils.getValue(item.setting, '', true)}}"
    layer-style="{{utils.getValue(item.layerStyle, 1)}}"
    rotate="0utils.getValue(item.rotate, 0)}}"
    enable-3D="{{utils.getValue(item.enable3D, false)}}"
    show-compass="{{utils.getValue(item.showCompass, false)}}"
    show-scale="{{utils.getValue(item.showScale, false)}}"
    enable-overlooking="{{utils.getValue(item.enableOverlooking, false)}}"
    enable-zoom="{{utils.getValue(item.enableZoom, true)}}"
    enable-scroll="{{utils.getValue(item.scale, true)}}"
    enable-rotate="{{utils.getValue(item.enableRotate, false)}}"
    enable-satellite="{{utils.getValue(item.enableSatellite, false)}}"
    enable-traffic="{{utils.getValue(item.enableTraffic, false)}}"
    enable-poi="{{utils.getValue(item.enablePoi, true)}}"
    enable-building="{{utils.getValue(item.enableBuilding, false)}}"
    bindtap="eventHandler"
    bindmarkertap="eventHandler"
    bindlabeltap="eventHandler"
    bindcontroltap="eventHandler"
    bindcallouttap="eventHandler"
    bindregionchange="eventHandler"
    bindpoitap="eventHandler"
    bindanchorpointtap="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </map>
</template>

<template name="tmpl_12_cover-view">
    <cover-view
      wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
      style="{{item.style}}" class="{{item.class}}"
      hover-class="{{utils.getValue(item.hoverClass,'none')}}"
      hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}"
      hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}"
      hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}"
      animation="{{item.animation}}"
      bindtouchstart="eventHandler"
      bindtouchmove="eventHandler"
      bindtouchend="eventHandler"
      bindtouchcancel="eventHandler"
      bindlongpress="eventHandler"
      bindanimationstart="eventHandler"
      bindanimationiteration="eventHandler"
      bindanimationend="eventHandler"
      bindtransitionend="eventHandler"
      bindtap="eventHandler"
     >
      <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
    </cover-view>
</template>

<template name="tmpl_12_label">
  <label
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    for="{{utils.getValue(item.for, '')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </label>
</template>

<template name="tmpl_12_checkbox">
  <checkbox
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox>
</template>

<template name="tmpl_12_checkbox-group">
  <checkbox-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox-group>
</template>

<template name="tmpl_12_form">
  <form
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    report-submit="{{utils.getValue(item.reportSubmit, false)}}"
    report-submit-timeout="{{utils.getValue(item.reportSubmitTimeout, 0)}}"
    bindsubmit="eventHandler"
    bindreset="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </form>
</template>

<template name="tmpl_12_radio">
  <radio
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio>
</template>

<template name="tmpl_12_radio-group">
  <radio-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio-group>
</template>

<template name="tmpl_12_picker">
  <picker
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    header-text="{{utils.getValue(item.header-text, '')}}"
    mode="{{utils.getValue(item.mode, 'selector')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    range="{{utils.getValue(item.range, [])}}"
    range-key="{{utils.getValue(item.rangeKey, '')}}"
    value="{{utils.getValue(item.value, 0)}}"
    start="{{utils.getValue(item.start, '')}}"
    end="{{utils.getValue(item.end, '')}}"
    fields="{{utils.getValue(item.fields, 'day')}}"
    custom-item="{{utils.getValue(item.customItem, '')}}"
    bindchange="eventHandler"
    bindcancel="eventHandler"
    bindcolumnchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker>
</template>

<template name="tmpl_12_picker-view">
  <picker-view
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, [])}}"
    indicator-style="{{utils.getValue(item.indicatorStyle, '')}}"
    indicator-class="{{utils.getValue(item.indicatorClass, '')}}"
    mask-style="{{utils.getValue(item.maskStyle, '')}}"
    mask-class="{{utils.getValue(item.maskClass, '')}}"
    bindchange="eventHandler"
    bindpickstart="eventHandler"
    bindpickend="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view>
</template>

<template name="tmpl_12_picker-view-column">
  <picker-view-column
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view-column>
</template>

<template name="tmpl_12_slider">
  <slider
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    min="{{utils.getValue(item.min, 0)}}"
    max="{{utils.getValue(item.max, 100)}}"
    step="{{utils.getValue(item.step, 1)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    value="{{utils.getValue(item.value, 0)}}"
    color="{{utils.getValue(item.color, '#e9e9e9')}}"
    selected-color="{{utils.getValue(item.selectedColor, '#1aad19')}}"
    activeColor="{{utils.getValue(item.activeColor, '#1aad19')}}"
    backgroundColor="{{utils.getValue(item.backgroundColor, '#e9e9e9')}}"
    block-size="{{utils.getValue(item.blockSize, 28)}}"
    block-color="{{utils.getValue(item.blockColor, '#ffffff')}}"
    show-value="{{utils.getValue(item.showValue, false)}}"
    bindchanging="eventHandler"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </slider>
</template>

<template name="tmpl_12_textarea">
  <textarea
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    placeholder-style="{{utils.getValue(item.placeholderStyle, '')}}"
    placeholder-class="{{utils.getValue(item.placeholderClass, 'textarea-placeholder')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    maxlength="{{utils.getValue(item.maxlength, 140)}}"
    auto-focus="{{utils.getValue(item.autoFocus, false)}}"
    focus="{{utils.getValue(item.focus, false)}}"
    auto-height="{{utils.getValue(item.autoHeight, false)}}"
    fixed="{{utils.getValue(item.fixed, false)}}"
    cursor-spacing="{{utils.getValue(item.cursorSpacing, 0)}}"
    cursor="{{utils.getValue(item.cursor, -1)}}"
    show-confirm-bar="{{utils.getValue(item.showConfirmBar, true)}}"
    selection-start="{{utils.getValue(item.selectionStart, -1)}}"
    selection-end="{{utils.getValue(item.selectionEnd, -1)}}"
    adjust-position="{{utils.getValue(item.adjustPosition, true)}}"
    hold-keyboard="{{utils.getValue(item.holdKeyboard, false)}}"
    disable-default-padding="{{utils.getValue(item.disableDefaultPadding, false)}}"
    confirm-type="{{utils.getValue(item.confirmType, 'return')}}"
    confirm-hold="{{utils.getValue(item.confirmHold, false)}}"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindlinechange="eventHandler"
    bindinput="eventHandler"
    bindconfirm="eventHandler"
    bindkeyboardheightchange="eventHandler"
  >
  
  </textarea>
</template>

<template name="tmpl_12_editor">
  <editor
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    read-only="{{utils.getValue(item.readOnly, false)}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    show-img-size="{{utils.getValue(item.showImgSize, false)}}"
    show-img-toolbar="{{utils.getValue(item.showImgToolbar, false)}}"
    show-img-resize="{{utils.getValue(item.showImgResize, false)}}"
    bindready="eventHandler"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindinput="eventHandler"
    bindstatuschange="eventHandler"
  >
  
  </editor>
</template>

<template name="tmpl_12_navigator">
  <navigator
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    target="{{utils.getValue(item.target, 'self')}}"
    url="{{utils.getValue(item.url, '')}}"
    open-type="{{utils.getValue(item.openType, 'navigate')}}"
    delta="{{utils.getValue(item.delta, 1)}}"
    app-id="{{utils.getValue(item.appId, '')}}"
    path="{{utils.getValue(item.path, '')}}"
    extra-data="{{utils.getValue(item.extraData, '', true)}}"
    version="{{utils.getValue(item.version, 'release')}}"
    redirect="{{utils.getValue(item.redirect, false)}}"
    short-link="{{utils.getValue(item.shortLink, '')}}"
    hover-class="{{utils.getValue(item.hoverClass, 'navigator-hover')}}"
    hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation, false)}}"
    hover-start-time="{{utils.getValue(item.hoverStartTime, 50)}}"
    hover-stay-time="{{utils.getValue(item.hoverStayTime, 600)}}"
    bindsuccess="eventHandler"
    bindfail="eventHandler"
    bindcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </navigator>
</template>

<template name="tmpl_12_video">
  <video
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{item.src}}"
    duration="{{utils.getValue(item.duration, 0)}}"
    controls="{{utils.getValue(item.controls, true)}}"
    danmu-list="{{utils.getValue(item.danmuList, [])}}"
    danmu-btn="{{utils.getValue(item.danmuBtn, false)}}"
    enable-danmu="{{utils.getValue(item.enableDanmu, false)}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    loop="{{utils.getValue(item.loop, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    initial-time="{{utils.getValue(item.initialTime, 0)}}"
    page-gesture="{{utils.getValue(item.pageGesture, false)}}"
    direction="{{utils.getValue(item.direction, 0)}}"
    show-progress="{{utils.getValue(item.showProgress, true)}}"
    show-fullscreen-btn="{{utils.getValue(item.showFullscreenBtn, true)}}"
    show-play-btn="{{utils.getValue(item.showPlayBtn, true)}}"
    show-center-play-btn="{{utils.getValue(item.showCenterPlayBtn, true)}}"
    enable-progress-gesture="{{utils.getValue(item.enableProgressGesture, true)}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    poster="{{utils.getValue(item.poster, '')}}"
    show-mute-btn="{{utils.getValue(item.showMuteBtn, false)}}"
    title="{{utils.getValue(item.title, '')}}"
    play-btn-position="{{utils.getValue(item.playBtnPosition, 'bottom')}}"
    enable-play-gesture="{{utils.getValue(item.enablePlayGesture, false)}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    vslide-gesture="{{utils.getValue(item.vslideGesture, false)}}"
    vslide-gesture-in-fullscreen="{{utils.getValue(item.vslideGestureInFullscreen, true)}}"
    ad-unit-id="{{item.adUnitId}}"
    poster-for-crawler="{{item.posterForCrawler}}"
    show-casting-button="{{utils.getValue(item.showCastingButton, false)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    picture-in-picture-show-progress="{{utils.getValue(item.pictureInPictureShowProgress, false)}}"
    enable-auto-rotation="{{utils.getValue(item.enableAutoRotation, false)}}"
    show-screen-lock-button="{{utils.getValue(item.showScreenLockButton, false)}}"
    show-snapshot-button="{{utils.getValue(item.showSnapshotButton, false)}}"
    show-background-playback-button="{{utils.getValue(item.showBackgroundPlaybackButton, false)}}"
    background-poster="{{utils.getValue(item.backgroundPoster, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    is-drm="{{utils.getValue(item.isDrm, false)}}"
    provision-url="{{utils.getValue(item.provisionUrl, '')}}"
    certificate-url="{{utils.getValue(item.certificateUrl, '')}}"
    license-url="{{utils.getValue(item.licenseUrl, '')}}"
    bindplay="eventHandler"
    bindpause="eventHandler"
    bindended="eventHandler"
    bindtimeupdate="eventHandler"
    bindfullscreenchange="eventHandler"
    bindwaiting="eventHandler"
    binderror="eventHandler"
    bindprogress="eventHandler"
    bindloadedmetadata="eventHandler"
    bindcontrolstoggle="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
    bindseekcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </video>
</template>

<template name="tmpl_12_camera">
  <camera
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    mode="{{utils.getValue(item.mode, 'normal')}}"
    resolution="{{utils.getValue(item.resolution, 'medium')}}"
    device-position="{{utils.getValue(item.devicePosition, 'back')}}"
    flash="{{utils.getValue(item.flash, 'auto')}}"
    frame-size="{{utils.getValue(item.frameSize, 'medium')}}"
    bindstop="eventHandler"
    binderror="eventHandler"
    bindinitdone="eventHandler"
    bindscancode="eventHandler"
  >
    
  </camera>
</template>

<template name="tmpl_12_live-player">
  <live-player
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{utils.getValue(item.src, '')}}"
    mode="{{utils.getValue(item.mode, 'live')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    min-cache="{{utils.getValue(item.minCache, 1)}}"
    max-cache="{{utils.getValue(item.maxCache, 3)}}"
    sound-mode="{{utils.getValue(item.soundMode, 'speaker')}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    bindstatechange="eventHandler"
    bindfullscreenchange="eventHandler"
    bindnetstatus="eventHandler"
    bindaudiovolumenotify="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-player>
</template>

<template name="tmpl_12_live-pusher">
  <live-pusher
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    url="{{utils.getValue(item.url, '')}}"
    mode="{{utils.getValue(item.mode, 'RTC')}}"
    autopush="{{utils.getValue(item.autopush, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    enable-camera="{{utils.getValue(item.enableCamera, true)}}"
    auto-focus="{{utils.getValue(item.autoFocus, true)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    beauty="{{utils.getValue(item.beauty, 0)}}"
    whiteness="{{utils.getValue(item.whiteness, 0)}}"
    aspect="{{utils.getValue(item.aspect, '9:16')}}"
    min-bitrate="{{utils.getValue(item.minBitrate, 200)}}"
    max-bitrate="{{utils.getValue(item.maxBitrate, 1000)}}"
    audio-quality="{{utils.getValue(item.audioQuality, 'high')}}"
    waiting-image="{{utils.getValue(item.waitingImage, '')}}"
    waiting-image-hash="{{utils.getValue(item.waitingImageHash, '')}}"
    zoom="{{utils.getValue(item.zoom, false)}}"
    device-position="{{utils.getValue(item.devicePosition, 'front')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    mirror="{{utils.getValue(item.mirror, false)}}"
    remote-mirror="{{utils.getValue(item.remoteMirror, false)}}"
    local-mirror="{{utils.getValue(item.localMirror, 'auto')}}"
    audio-reverb-type="{{utils.getValue(item.audioReverbType, 0)}}"
    enable-mic="{{utils.getValue(item.enableMic, true)}}"
    enable-agc="{{utils.getValue(item.enableAgc, false)}}"
    enable-ans="{{utils.getValue(item.enableAns, false)}}"
    audio-volume-type="{{utils.getValue(item.audioVolumeType, 'auto')}}"
    video-width="{{utils.getValue(item.videoWidth, 360)}}"
    video-height="{{utils.getValue(item.videoHeight, 640)}}"
    beauty-style="{{utils.getValue(item.beautyStyle, 'smooth')}}"
    filter="{{utils.getValue(item.filter, 'standard')}}"
    bindstatechange="eventHandler"
    bindnetstatus="eventHandler"
    binderror="eventHandler"
    bindbgmstart="eventHandler"
    bindbgmprogress="eventHandler"
    bindbgmcomplete="eventHandler"
    bindaudiovolumenotify="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-pusher>
</template>

<template name="tmpl_12_canvas">
  <canvas
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    type="{{utils.getValue(item.type, '')}}"
    canvas-id="{{utils.getValue(item.canvasId, '')}}"
    disable-scroll="{{utils.getValue(item.disableScroll, false)}}"
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchend="eventHandler"
    bindtouchcancel="eventHandler"
    bindlongtap="eventHandler"
    binderror="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </canvas>
</template>

<template name="tmpl_12_keyboard-accessory">
  <keyboard-accessory
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </keyboard-accessory>
</template>

      

<template name="container_13"><block wx:for="{{vdoms}}" wx:key="key"><template is="{{utils.getTagName(item, cid)}}" data="{{item: item, cid}}"></template></block></template>

<template name="tmpl_13_view">
  <view wx:key="{{item.key}}" data-data="{{item.dataSet}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_13_pure-view">
  <view wx:key="{{item.key}}" style="{{item.style}}" class="{{item.class}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_13_static-view">
  <view wx:key="{{item.key}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}"  style="{{item.style}}" class="{{item.class}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>


<template name="tmpl_13_text">
  <text wx:key="{{item.key}}" data-data="{{item.dataSet}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}"><template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" /></text>
</template>

<template name="tmpl_13_template">
  <template is="{{item.is}}" data="{{...item.data}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </template>
</template>

<template name="tmpl_13_button">
  <button wx:key="{{item.key}}" data-data="{{item.dataSet}}" size="{{utils.getValue(item.size,'default')}}" type="{{item.type}}" plain="{{utils.getValue(item.plain,false)}}" disabled="{{item.disabled}}" loading="{{utils.getValue(item.loading,false)}}" form-type="{{item.formType}}" open-type="{{item.openType}}" hover-class="{{utils.getValue(item.hoverClass,'button-hover')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,20)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,70)}}" name="{{item.name}}" lang="{{utils.getValue(item.lang,en)}}" session-from="{{item.sessionFrom}}" send-message-title="{{item.sendMessageTitle}}" send-message-path="{{item.sendMessagePath}}" send-message-img="{{item.sendMessageImg}}" app-parameter="{{item.appParameter}}" show-message-card="{{utils.getValue(item.showMessageCard,false)}}" business-id="{{item.businessId}}" bindgetuserinfo="eventHandler" bindcontact="eventHandler" bindgetphonenumber="eventHandler" binderror="eventHandler" bindopensetting="eventHandler" bindlaunchapp="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </button>
</template>

<template name="tmpl_13_input">
  <template is="{{utils.isFocus(item, 'tmpl_13_')}}" data="{{item:item}}" />
</template>

<template name="tmpl_13_input_focus">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" focus="{{utils.getValue(item.focus,false)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_13_input_blur">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_13_text-node"> {{item.content}} </template>

<template name="tmpl_13_navbar">
  <navbar id="{{item.uid}}" title="{{item.title}}" noPlaceholder="{{item.noPlaceholder}}" css="{{item.css}}"></navbar>
</template>

<template name="tmpl_13_keyboard">
  <keyboard id="{{item.uid}}" limit="{{item.limit}}" price="{{item.price}}" bind:change="eventHandler" bind:confirm="eventHandler"></keyboard>
</template>

<template name="tmpl_13_scroll-view">
  <scroll-view wx:key="{{item.key}}" data-data="{{item.dataSet}}" scroll-x="{{utils.getValue(item.scrollX,false)}}" scroll-y="{{utils.getValue(item.scrollY,false)}}" upper-threshold="{{utils.getValue(item.upperThreshold,50)}}" lower-threshold="{{utils.getValue(item.lowerThreshold,50)}}" scroll-top="{{item.scrollTop}}" scroll-left="{{item.scrollLeft}}" scroll-into-view="{{item.scrollIntoView}}" scroll-with-animation="{{utils.getValue(item.scrollWithAnimation,false)}}" enable-back-to-top="{{utils.getValue(item.enableBackToTop,false)}}" bindscrolltoupper="eventHandler" bindscrolltolower="eventHandler" bindscroll="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" enable-flex="{{utils.getValue(item.enableFlex,false)}}" scroll-anchoring="{{utils.getValue(item.scrollAnchoring,false)}}" refresher-enabled="{{utils.getValue(item.refresherEnabled,false)}}" refresher-threshold="{{utils.getValue(item.refresherThreshold,45)}}" refresher-default-style="{{utils.getValue(item.refresherDefaultStyle,'black')}}" refresher-background="{{utils.getValue(item.refresherBackground,'#FFF')}}" refresher-triggered="{{utils.getValue(item.refresherTriggered,false)}}" enhanced="{{utils.getValue(item.enhanced,false)}}" bounces="{{utils.getValue(item.bounces,true)}}" show-scrollbar="{{utils.getValue(item.showScrollbar,true)}}" paging-enabled="{{utils.getValue(item.pagingEnabled,false)}}" fast-deceleration="{{utils.getValue(item.fastDeceleration,false)}}" binddragstart="eventHandler" binddragging="eventHandler" binddragend="eventHandler" bindrefresherpulling="eventHandler" bindrefresherrefresh="eventHandler" bindrefresherrestore="eventHandler" bindrefresherabort="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </scroll-view>
</template>

<template name="tmpl_13_block">
  <block wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </block>
</template>

<template name="tmpl_13_null">
</template>

<template name="tmpl_13_image">
  <image wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" src="{{item.src}}" mode="{{utils.getValue(item.mode,'scaleToFill')}}" lazy-load="{{utils.getValue(item.lazyLoad,false)}}" binderror="eventHandler" bindload="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" webp="{{utils.getValue(item.webp,false)}}" show-menu-by-longpress="{{utils.getValue(item.showMenuByLongpress,false)}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </image>
</template>

<template name="tmpl_13_icon">
  <icon wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" size="{{utils.getValue(item.size, 23)}}" type="{{item.type}}" color="{{utils.getValue(item.color, '')}}" bind:tap="eventHandler"/>
</template>

<template name="tmpl_13_i">
  <i 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    style="{{item.style}}" class="{{item.class}}" 
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchcancel="eventHandler"
    bindtouchend="eventHandler"
    bindtap="eventHandler"
    bindlongpress="eventHandler"
    bindlongtap="eventHandler"
    bindtransitionend="eventHandler"
    bindanimationstart="eventHandler"
    bindanimationiteration="eventHandler"
    bindanimationend="eventHandler"
    bindtouchforcechange="eventHandler"
  />
</template>

<template name="tmpl_13_progress">
  <progress wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" percent="{{utils.getValue(item.percent, 0)}}" color="{{utils.getValue(item.color, '#09BB07')}}" activeColor="{{utils.getValue(item.activeColor, '#09BB07')}}" backgroundColor="{{utils.getValue(item.backgroundColor, '#EBEBEB')}}" active="{{utils.getValue(item.active, false)}}" active-mode="{{utils.getValue(item.activeMode, backwards)}}"  show-info="{{utils.getValue(item.showInfo, false)}}" stroke-width="{{utils.getValue(item.strokeWidth, 6)}}" border-radius="{{util.getValue(item.borderRadius, 0)}}" font-size="{{utils.getValue(item.fontSize, 16)}}" duration="{{utils.getValue(item.duration, 30)}}" bindactiveend="eventHandler"/>
</template>

<template name="tmpl_13_rich-text">
  <rich-text wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" nodes="{{utils.getValue(item.nodes, [])}}" space="{{util.getVaue(item.space, 'ensp')}}" ></rich-text>
</template>

<template name="tmpl_13_swiper">
  <swiper
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"  
    indicator-dots="{{utils.getValue(item.indicatorDots, false)}}"
    indicator-color="{{utils.getValue(item.indicatorcolor, 'rgba(0, 0, 0, .3)')}}"
    indicator-active-color="{{utils.getValue(item.indicatorActiveColor, '#000000')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}" 
    interval="{{utils.getValue(item.interval, 5000)}}" 
    duration="{{utils.getValue(item.duration, 500)}}"
    circular="{{utils.getValue(item.circular, false)}}" 
    vertical="{{utils.getValue(item.vertical, false)}}" 
    previous-margin="{{utils.getValue(item.previousMargin, '0px')}}" 
    next-margin="{{utils.getValue(item.nextMargin, '0px')}}" 
    snap-to-edge="{{utils.getValue(item.snapToEdge, false)}}" 
    display-multiple-items="{{utils.getValue(item.displayMultipleItems, 1)}}" 
    easing-function="{{utils.getValue(item.easingFunction, 'default')}}" 
    bindchange="eventHandler"
    bindtransition="eventHandler"
    bindanimationfinish="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper>
</template>

<template name="tmpl_13_swiper-item">
  <swiper-item wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  item-id="{{utils.getValue(item.itemId, '')}}" skip-hidden-item-layout="{{utils.getValue(item.skipHiddenItemLayout, '')}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper-item>
</template>

<template name="tmpl_13_switch">
  <switch wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  checked="{{utils.getValue(item.checked, false)}}" disabled="{{utils.getValue(item.disabled, false)}}" type="{{utils.getValue(item.type, 'switch')}}" color="{{utils.getValue(item.color, '#04BE02')}}" bindchange="eventHandler"/>
</template>

<template name="tmpl_13_movable-area">
  <movable-area 
    style="{{item.style}}" class="{{item.class}}" 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    scale-area="{{utils.getValue(item.scaleArea, false)}}" 
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-area>
</template>

<template name="tmpl_13_movable-view">
  <movable-view 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}" 
    direction="{{utils.getValue(item.direction, 'none')}}"
    inertia="{{utils.getValue(item.inertia, false)}}"
    out-of-bounds="{{utils.getValue(item.outOfBounds, false)}}"
    x="{{utils.getValue(item.x, '0')}}"
    y="{{utils.getValue(item.y, '0')}}"
    damping="{{utils.getValue(item.damping, 20)}}"
    friction="{{utils.getValue(item.friction, 2)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    scale="{{utils.getValue(item.scale, false)}}"
    scale-min="{{utils.getValue(item.scaleMin, 0.5)}}"
    scale-max="{{utils.getValue(item.scaleMax, 10)}}"
    scale-value="{{utils.getValue(item.scaleValue, 1)}}"
    animation="{{utils.getValue(item.animation, false)}}"
    width="{{utils.getValue(item.width, '10px')}}"
    height="{{utils.getValue(item.height, '10px')}}"
    bindchange="eventHandler"
    bindscale="eventHandler"
    bindhtouchmove="eventHandler"
    bindvtouchmove="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-view>
</template>

<template name="tmpl_13_map">
  <map
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    longitude="{{item.longitude}}"
    latitude="{{item.latitude}}"scale
    scale="{{utils.getValue(item.scale, 16)}}"
    min-scale="{{utils.getValue(item.minScale, 3)}}"
    max-scale="{{utils.getValue(item.maxScale, 20)}}"
    markers="{{utils.getValue(item.markers, [])}}"
    covers="{{utils.getValue(item.covers, [])}}"
    polyline="{{utils.getValue(item.polyline, [])}}"
    circles="{{utils.getValue(item.circles, [])}}"
    controls="{{utils.getValue(item.controls, [])}}"
    include-points="{{utils.getValue(item.includePoints, [])}}"
    show-location="{{utils.getValue(item.showLocation, false)}}"
    polygons="{{utils.getValue(item.polygons, [])}}"
    subkey="{{utils.getValue(item.subkey, '')}}"
    setting="{{utils.getValue(item.setting, '', true)}}"
    layer-style="{{utils.getValue(item.layerStyle, 1)}}"
    rotate="0utils.getValue(item.rotate, 0)}}"
    enable-3D="{{utils.getValue(item.enable3D, false)}}"
    show-compass="{{utils.getValue(item.showCompass, false)}}"
    show-scale="{{utils.getValue(item.showScale, false)}}"
    enable-overlooking="{{utils.getValue(item.enableOverlooking, false)}}"
    enable-zoom="{{utils.getValue(item.enableZoom, true)}}"
    enable-scroll="{{utils.getValue(item.scale, true)}}"
    enable-rotate="{{utils.getValue(item.enableRotate, false)}}"
    enable-satellite="{{utils.getValue(item.enableSatellite, false)}}"
    enable-traffic="{{utils.getValue(item.enableTraffic, false)}}"
    enable-poi="{{utils.getValue(item.enablePoi, true)}}"
    enable-building="{{utils.getValue(item.enableBuilding, false)}}"
    bindtap="eventHandler"
    bindmarkertap="eventHandler"
    bindlabeltap="eventHandler"
    bindcontroltap="eventHandler"
    bindcallouttap="eventHandler"
    bindregionchange="eventHandler"
    bindpoitap="eventHandler"
    bindanchorpointtap="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </map>
</template>

<template name="tmpl_13_cover-view">
    <cover-view
      wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
      style="{{item.style}}" class="{{item.class}}"
      hover-class="{{utils.getValue(item.hoverClass,'none')}}"
      hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}"
      hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}"
      hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}"
      animation="{{item.animation}}"
      bindtouchstart="eventHandler"
      bindtouchmove="eventHandler"
      bindtouchend="eventHandler"
      bindtouchcancel="eventHandler"
      bindlongpress="eventHandler"
      bindanimationstart="eventHandler"
      bindanimationiteration="eventHandler"
      bindanimationend="eventHandler"
      bindtransitionend="eventHandler"
      bindtap="eventHandler"
     >
      <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
    </cover-view>
</template>

<template name="tmpl_13_label">
  <label
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    for="{{utils.getValue(item.for, '')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </label>
</template>

<template name="tmpl_13_checkbox">
  <checkbox
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox>
</template>

<template name="tmpl_13_checkbox-group">
  <checkbox-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox-group>
</template>

<template name="tmpl_13_form">
  <form
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    report-submit="{{utils.getValue(item.reportSubmit, false)}}"
    report-submit-timeout="{{utils.getValue(item.reportSubmitTimeout, 0)}}"
    bindsubmit="eventHandler"
    bindreset="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </form>
</template>

<template name="tmpl_13_radio">
  <radio
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio>
</template>

<template name="tmpl_13_radio-group">
  <radio-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio-group>
</template>

<template name="tmpl_13_picker">
  <picker
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    header-text="{{utils.getValue(item.header-text, '')}}"
    mode="{{utils.getValue(item.mode, 'selector')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    range="{{utils.getValue(item.range, [])}}"
    range-key="{{utils.getValue(item.rangeKey, '')}}"
    value="{{utils.getValue(item.value, 0)}}"
    start="{{utils.getValue(item.start, '')}}"
    end="{{utils.getValue(item.end, '')}}"
    fields="{{utils.getValue(item.fields, 'day')}}"
    custom-item="{{utils.getValue(item.customItem, '')}}"
    bindchange="eventHandler"
    bindcancel="eventHandler"
    bindcolumnchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker>
</template>

<template name="tmpl_13_picker-view">
  <picker-view
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, [])}}"
    indicator-style="{{utils.getValue(item.indicatorStyle, '')}}"
    indicator-class="{{utils.getValue(item.indicatorClass, '')}}"
    mask-style="{{utils.getValue(item.maskStyle, '')}}"
    mask-class="{{utils.getValue(item.maskClass, '')}}"
    bindchange="eventHandler"
    bindpickstart="eventHandler"
    bindpickend="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view>
</template>

<template name="tmpl_13_picker-view-column">
  <picker-view-column
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view-column>
</template>

<template name="tmpl_13_slider">
  <slider
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    min="{{utils.getValue(item.min, 0)}}"
    max="{{utils.getValue(item.max, 100)}}"
    step="{{utils.getValue(item.step, 1)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    value="{{utils.getValue(item.value, 0)}}"
    color="{{utils.getValue(item.color, '#e9e9e9')}}"
    selected-color="{{utils.getValue(item.selectedColor, '#1aad19')}}"
    activeColor="{{utils.getValue(item.activeColor, '#1aad19')}}"
    backgroundColor="{{utils.getValue(item.backgroundColor, '#e9e9e9')}}"
    block-size="{{utils.getValue(item.blockSize, 28)}}"
    block-color="{{utils.getValue(item.blockColor, '#ffffff')}}"
    show-value="{{utils.getValue(item.showValue, false)}}"
    bindchanging="eventHandler"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </slider>
</template>

<template name="tmpl_13_textarea">
  <textarea
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    placeholder-style="{{utils.getValue(item.placeholderStyle, '')}}"
    placeholder-class="{{utils.getValue(item.placeholderClass, 'textarea-placeholder')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    maxlength="{{utils.getValue(item.maxlength, 140)}}"
    auto-focus="{{utils.getValue(item.autoFocus, false)}}"
    focus="{{utils.getValue(item.focus, false)}}"
    auto-height="{{utils.getValue(item.autoHeight, false)}}"
    fixed="{{utils.getValue(item.fixed, false)}}"
    cursor-spacing="{{utils.getValue(item.cursorSpacing, 0)}}"
    cursor="{{utils.getValue(item.cursor, -1)}}"
    show-confirm-bar="{{utils.getValue(item.showConfirmBar, true)}}"
    selection-start="{{utils.getValue(item.selectionStart, -1)}}"
    selection-end="{{utils.getValue(item.selectionEnd, -1)}}"
    adjust-position="{{utils.getValue(item.adjustPosition, true)}}"
    hold-keyboard="{{utils.getValue(item.holdKeyboard, false)}}"
    disable-default-padding="{{utils.getValue(item.disableDefaultPadding, false)}}"
    confirm-type="{{utils.getValue(item.confirmType, 'return')}}"
    confirm-hold="{{utils.getValue(item.confirmHold, false)}}"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindlinechange="eventHandler"
    bindinput="eventHandler"
    bindconfirm="eventHandler"
    bindkeyboardheightchange="eventHandler"
  >
  
  </textarea>
</template>

<template name="tmpl_13_editor">
  <editor
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    read-only="{{utils.getValue(item.readOnly, false)}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    show-img-size="{{utils.getValue(item.showImgSize, false)}}"
    show-img-toolbar="{{utils.getValue(item.showImgToolbar, false)}}"
    show-img-resize="{{utils.getValue(item.showImgResize, false)}}"
    bindready="eventHandler"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindinput="eventHandler"
    bindstatuschange="eventHandler"
  >
  
  </editor>
</template>

<template name="tmpl_13_navigator">
  <navigator
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    target="{{utils.getValue(item.target, 'self')}}"
    url="{{utils.getValue(item.url, '')}}"
    open-type="{{utils.getValue(item.openType, 'navigate')}}"
    delta="{{utils.getValue(item.delta, 1)}}"
    app-id="{{utils.getValue(item.appId, '')}}"
    path="{{utils.getValue(item.path, '')}}"
    extra-data="{{utils.getValue(item.extraData, '', true)}}"
    version="{{utils.getValue(item.version, 'release')}}"
    redirect="{{utils.getValue(item.redirect, false)}}"
    short-link="{{utils.getValue(item.shortLink, '')}}"
    hover-class="{{utils.getValue(item.hoverClass, 'navigator-hover')}}"
    hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation, false)}}"
    hover-start-time="{{utils.getValue(item.hoverStartTime, 50)}}"
    hover-stay-time="{{utils.getValue(item.hoverStayTime, 600)}}"
    bindsuccess="eventHandler"
    bindfail="eventHandler"
    bindcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </navigator>
</template>

<template name="tmpl_13_video">
  <video
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{item.src}}"
    duration="{{utils.getValue(item.duration, 0)}}"
    controls="{{utils.getValue(item.controls, true)}}"
    danmu-list="{{utils.getValue(item.danmuList, [])}}"
    danmu-btn="{{utils.getValue(item.danmuBtn, false)}}"
    enable-danmu="{{utils.getValue(item.enableDanmu, false)}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    loop="{{utils.getValue(item.loop, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    initial-time="{{utils.getValue(item.initialTime, 0)}}"
    page-gesture="{{utils.getValue(item.pageGesture, false)}}"
    direction="{{utils.getValue(item.direction, 0)}}"
    show-progress="{{utils.getValue(item.showProgress, true)}}"
    show-fullscreen-btn="{{utils.getValue(item.showFullscreenBtn, true)}}"
    show-play-btn="{{utils.getValue(item.showPlayBtn, true)}}"
    show-center-play-btn="{{utils.getValue(item.showCenterPlayBtn, true)}}"
    enable-progress-gesture="{{utils.getValue(item.enableProgressGesture, true)}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    poster="{{utils.getValue(item.poster, '')}}"
    show-mute-btn="{{utils.getValue(item.showMuteBtn, false)}}"
    title="{{utils.getValue(item.title, '')}}"
    play-btn-position="{{utils.getValue(item.playBtnPosition, 'bottom')}}"
    enable-play-gesture="{{utils.getValue(item.enablePlayGesture, false)}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    vslide-gesture="{{utils.getValue(item.vslideGesture, false)}}"
    vslide-gesture-in-fullscreen="{{utils.getValue(item.vslideGestureInFullscreen, true)}}"
    ad-unit-id="{{item.adUnitId}}"
    poster-for-crawler="{{item.posterForCrawler}}"
    show-casting-button="{{utils.getValue(item.showCastingButton, false)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    picture-in-picture-show-progress="{{utils.getValue(item.pictureInPictureShowProgress, false)}}"
    enable-auto-rotation="{{utils.getValue(item.enableAutoRotation, false)}}"
    show-screen-lock-button="{{utils.getValue(item.showScreenLockButton, false)}}"
    show-snapshot-button="{{utils.getValue(item.showSnapshotButton, false)}}"
    show-background-playback-button="{{utils.getValue(item.showBackgroundPlaybackButton, false)}}"
    background-poster="{{utils.getValue(item.backgroundPoster, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    is-drm="{{utils.getValue(item.isDrm, false)}}"
    provision-url="{{utils.getValue(item.provisionUrl, '')}}"
    certificate-url="{{utils.getValue(item.certificateUrl, '')}}"
    license-url="{{utils.getValue(item.licenseUrl, '')}}"
    bindplay="eventHandler"
    bindpause="eventHandler"
    bindended="eventHandler"
    bindtimeupdate="eventHandler"
    bindfullscreenchange="eventHandler"
    bindwaiting="eventHandler"
    binderror="eventHandler"
    bindprogress="eventHandler"
    bindloadedmetadata="eventHandler"
    bindcontrolstoggle="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
    bindseekcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </video>
</template>

<template name="tmpl_13_camera">
  <camera
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    mode="{{utils.getValue(item.mode, 'normal')}}"
    resolution="{{utils.getValue(item.resolution, 'medium')}}"
    device-position="{{utils.getValue(item.devicePosition, 'back')}}"
    flash="{{utils.getValue(item.flash, 'auto')}}"
    frame-size="{{utils.getValue(item.frameSize, 'medium')}}"
    bindstop="eventHandler"
    binderror="eventHandler"
    bindinitdone="eventHandler"
    bindscancode="eventHandler"
  >
    
  </camera>
</template>

<template name="tmpl_13_live-player">
  <live-player
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{utils.getValue(item.src, '')}}"
    mode="{{utils.getValue(item.mode, 'live')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    min-cache="{{utils.getValue(item.minCache, 1)}}"
    max-cache="{{utils.getValue(item.maxCache, 3)}}"
    sound-mode="{{utils.getValue(item.soundMode, 'speaker')}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    bindstatechange="eventHandler"
    bindfullscreenchange="eventHandler"
    bindnetstatus="eventHandler"
    bindaudiovolumenotify="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-player>
</template>

<template name="tmpl_13_live-pusher">
  <live-pusher
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    url="{{utils.getValue(item.url, '')}}"
    mode="{{utils.getValue(item.mode, 'RTC')}}"
    autopush="{{utils.getValue(item.autopush, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    enable-camera="{{utils.getValue(item.enableCamera, true)}}"
    auto-focus="{{utils.getValue(item.autoFocus, true)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    beauty="{{utils.getValue(item.beauty, 0)}}"
    whiteness="{{utils.getValue(item.whiteness, 0)}}"
    aspect="{{utils.getValue(item.aspect, '9:16')}}"
    min-bitrate="{{utils.getValue(item.minBitrate, 200)}}"
    max-bitrate="{{utils.getValue(item.maxBitrate, 1000)}}"
    audio-quality="{{utils.getValue(item.audioQuality, 'high')}}"
    waiting-image="{{utils.getValue(item.waitingImage, '')}}"
    waiting-image-hash="{{utils.getValue(item.waitingImageHash, '')}}"
    zoom="{{utils.getValue(item.zoom, false)}}"
    device-position="{{utils.getValue(item.devicePosition, 'front')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    mirror="{{utils.getValue(item.mirror, false)}}"
    remote-mirror="{{utils.getValue(item.remoteMirror, false)}}"
    local-mirror="{{utils.getValue(item.localMirror, 'auto')}}"
    audio-reverb-type="{{utils.getValue(item.audioReverbType, 0)}}"
    enable-mic="{{utils.getValue(item.enableMic, true)}}"
    enable-agc="{{utils.getValue(item.enableAgc, false)}}"
    enable-ans="{{utils.getValue(item.enableAns, false)}}"
    audio-volume-type="{{utils.getValue(item.audioVolumeType, 'auto')}}"
    video-width="{{utils.getValue(item.videoWidth, 360)}}"
    video-height="{{utils.getValue(item.videoHeight, 640)}}"
    beauty-style="{{utils.getValue(item.beautyStyle, 'smooth')}}"
    filter="{{utils.getValue(item.filter, 'standard')}}"
    bindstatechange="eventHandler"
    bindnetstatus="eventHandler"
    binderror="eventHandler"
    bindbgmstart="eventHandler"
    bindbgmprogress="eventHandler"
    bindbgmcomplete="eventHandler"
    bindaudiovolumenotify="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-pusher>
</template>

<template name="tmpl_13_canvas">
  <canvas
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    type="{{utils.getValue(item.type, '')}}"
    canvas-id="{{utils.getValue(item.canvasId, '')}}"
    disable-scroll="{{utils.getValue(item.disableScroll, false)}}"
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchend="eventHandler"
    bindtouchcancel="eventHandler"
    bindlongtap="eventHandler"
    binderror="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </canvas>
</template>

<template name="tmpl_13_keyboard-accessory">
  <keyboard-accessory
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </keyboard-accessory>
</template>

      

<template name="container_14"><block wx:for="{{vdoms}}" wx:key="key"><template is="{{utils.getTagName(item, cid)}}" data="{{item: item, cid}}"></template></block></template>

<template name="tmpl_14_view">
  <view wx:key="{{item.key}}" data-data="{{item.dataSet}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_14_pure-view">
  <view wx:key="{{item.key}}" style="{{item.style}}" class="{{item.class}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>

<template name="tmpl_14_static-view">
  <view wx:key="{{item.key}}" hover-class="{{utils.getValue(item.hoverClass,'none')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}" animation="{{item.animation}}"  style="{{item.style}}" class="{{item.class}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" />
  </view>
</template>


<template name="tmpl_14_text">
  <text wx:key="{{item.key}}" data-data="{{item.dataSet}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}"><template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms:item.children,cid: cid+1}}" /></text>
</template>

<template name="tmpl_14_template">
  <template is="{{item.is}}" data="{{...item.data}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </template>
</template>

<template name="tmpl_14_button">
  <button wx:key="{{item.key}}" data-data="{{item.dataSet}}" size="{{utils.getValue(item.size,'default')}}" type="{{item.type}}" plain="{{utils.getValue(item.plain,false)}}" disabled="{{item.disabled}}" loading="{{utils.getValue(item.loading,false)}}" form-type="{{item.formType}}" open-type="{{item.openType}}" hover-class="{{utils.getValue(item.hoverClass,'button-hover')}}" hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}" hover-start-time="{{utils.getValue(item.hoverStartTime,20)}}" hover-stay-time="{{utils.getValue(item.hoverStayTime,70)}}" name="{{item.name}}" lang="{{utils.getValue(item.lang,en)}}" session-from="{{item.sessionFrom}}" send-message-title="{{item.sendMessageTitle}}" send-message-path="{{item.sendMessagePath}}" send-message-img="{{item.sendMessageImg}}" app-parameter="{{item.appParameter}}" show-message-card="{{utils.getValue(item.showMessageCard,false)}}" business-id="{{item.businessId}}" bindgetuserinfo="eventHandler" bindcontact="eventHandler" bindgetphonenumber="eventHandler" binderror="eventHandler" bindopensetting="eventHandler" bindlaunchapp="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </button>
</template>

<template name="tmpl_14_input">
  <template is="{{utils.isFocus(item, 'tmpl_14_')}}" data="{{item:item}}" />
</template>

<template name="tmpl_14_input_focus">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" focus="{{utils.getValue(item.focus,false)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_14_input_blur">
  <input wx:key="{{item.key}}" data-data="{{item.dataSet}}" value="{{item.value}}" type="{{utils.getValue(item.type,'')}}" password="{{utils.getValue(item.password,false)}}" placeholder="{{item.placeholder}}" placeholder-style="{{item.placeholderStyle}}" placeholder-class="{{utils.getValue(item.placeholderClass,'input-placeholder')}}" disabled="{{item.disabled}}" maxlength="{{utils.getValue(item.maxlength,140)}}" cursor-spacing="{{utils.getValue(item.cursorSpacing,0)}}" confirm-type="{{utils.getValue(item.confirmType,'done')}}" confirm-hold="{{utils.getValue(item.confirmHold,false)}}" cursor="{{utils.getValue(item.cursor,item.value.length)}}" selection-start="{{utils.getValue(item.selectionStart,-1)}}" selection-end="{{utils.getValue(item.selectionEnd,-1)}}" bindinput="eventHandler" bindfocus="eventHandler" bindblur="eventHandler" bindconfirm="eventHandler" name="{{item.name}}" auto-focus="{{utils.getValue(item.autoFocus,false)}}" always-embed="{{utils.getValue(item.alwaysEmbed,false)}}" adjust-position="{{utils.getValue(item.adjustPosition,true)}}" hold-keyboard="{{utils.getValue(item.holdKeyboard,false)}}" bindkeyboardheightchange="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}" />
</template>

<template name="tmpl_14_text-node"> {{item.content}} </template>

<template name="tmpl_14_navbar">
  <navbar id="{{item.uid}}" title="{{item.title}}" noPlaceholder="{{item.noPlaceholder}}" css="{{item.css}}"></navbar>
</template>

<template name="tmpl_14_keyboard">
  <keyboard id="{{item.uid}}" limit="{{item.limit}}" price="{{item.price}}" bind:change="eventHandler" bind:confirm="eventHandler"></keyboard>
</template>

<template name="tmpl_14_scroll-view">
  <scroll-view wx:key="{{item.key}}" data-data="{{item.dataSet}}" scroll-x="{{utils.getValue(item.scrollX,false)}}" scroll-y="{{utils.getValue(item.scrollY,false)}}" upper-threshold="{{utils.getValue(item.upperThreshold,50)}}" lower-threshold="{{utils.getValue(item.lowerThreshold,50)}}" scroll-top="{{item.scrollTop}}" scroll-left="{{item.scrollLeft}}" scroll-into-view="{{item.scrollIntoView}}" scroll-with-animation="{{utils.getValue(item.scrollWithAnimation,false)}}" enable-back-to-top="{{utils.getValue(item.enableBackToTop,false)}}" bindscrolltoupper="eventHandler" bindscrolltolower="eventHandler" bindscroll="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" bindanimationstart="eventHandler" bindanimationiteration="eventHandler" bindanimationend="eventHandler" bindtransitionend="eventHandler" enable-flex="{{utils.getValue(item.enableFlex,false)}}" scroll-anchoring="{{utils.getValue(item.scrollAnchoring,false)}}" refresher-enabled="{{utils.getValue(item.refresherEnabled,false)}}" refresher-threshold="{{utils.getValue(item.refresherThreshold,45)}}" refresher-default-style="{{utils.getValue(item.refresherDefaultStyle,'black')}}" refresher-background="{{utils.getValue(item.refresherBackground,'#FFF')}}" refresher-triggered="{{utils.getValue(item.refresherTriggered,false)}}" enhanced="{{utils.getValue(item.enhanced,false)}}" bounces="{{utils.getValue(item.bounces,true)}}" show-scrollbar="{{utils.getValue(item.showScrollbar,true)}}" paging-enabled="{{utils.getValue(item.pagingEnabled,false)}}" fast-deceleration="{{utils.getValue(item.fastDeceleration,false)}}" binddragstart="eventHandler" binddragging="eventHandler" binddragend="eventHandler" bindrefresherpulling="eventHandler" bindrefresherrefresh="eventHandler" bindrefresherrestore="eventHandler" bindrefresherabort="eventHandler" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </scroll-view>
</template>

<template name="tmpl_14_block">
  <block wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </block>
</template>

<template name="tmpl_14_null">
</template>

<template name="tmpl_14_image">
  <image wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" src="{{item.src}}" mode="{{utils.getValue(item.mode,'scaleToFill')}}" lazy-load="{{utils.getValue(item.lazyLoad,false)}}" binderror="eventHandler" bindload="eventHandler" bindtouchstart="eventHandler" bindtouchmove="eventHandler" bindtouchend="eventHandler" bindtouchcancel="eventHandler" bindlongpress="eventHandler" webp="{{utils.getValue(item.webp,false)}}" show-menu-by-longpress="{{utils.getValue(item.showMenuByLongpress,false)}}" style="{{item.style}}" class="{{item.class}}" bindtap="eventHandler"  id="{{item.uid}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </image>
</template>

<template name="tmpl_14_icon">
  <icon wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" size="{{utils.getValue(item.size, 23)}}" type="{{item.type}}" color="{{utils.getValue(item.color, '')}}" bind:tap="eventHandler"/>
</template>

<template name="tmpl_14_i">
  <i 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    style="{{item.style}}" class="{{item.class}}" 
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchcancel="eventHandler"
    bindtouchend="eventHandler"
    bindtap="eventHandler"
    bindlongpress="eventHandler"
    bindlongtap="eventHandler"
    bindtransitionend="eventHandler"
    bindanimationstart="eventHandler"
    bindanimationiteration="eventHandler"
    bindanimationend="eventHandler"
    bindtouchforcechange="eventHandler"
  />
</template>

<template name="tmpl_14_progress">
  <progress wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" percent="{{utils.getValue(item.percent, 0)}}" color="{{utils.getValue(item.color, '#09BB07')}}" activeColor="{{utils.getValue(item.activeColor, '#09BB07')}}" backgroundColor="{{utils.getValue(item.backgroundColor, '#EBEBEB')}}" active="{{utils.getValue(item.active, false)}}" active-mode="{{utils.getValue(item.activeMode, backwards)}}"  show-info="{{utils.getValue(item.showInfo, false)}}" stroke-width="{{utils.getValue(item.strokeWidth, 6)}}" border-radius="{{util.getValue(item.borderRadius, 0)}}" font-size="{{utils.getValue(item.fontSize, 16)}}" duration="{{utils.getValue(item.duration, 30)}}" bindactiveend="eventHandler"/>
</template>

<template name="tmpl_14_rich-text">
  <rich-text wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}" nodes="{{utils.getValue(item.nodes, [])}}" space="{{util.getVaue(item.space, 'ensp')}}" ></rich-text>
</template>

<template name="tmpl_14_swiper">
  <swiper
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"  
    indicator-dots="{{utils.getValue(item.indicatorDots, false)}}"
    indicator-color="{{utils.getValue(item.indicatorcolor, 'rgba(0, 0, 0, .3)')}}"
    indicator-active-color="{{utils.getValue(item.indicatorActiveColor, '#000000')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}" 
    interval="{{utils.getValue(item.interval, 5000)}}" 
    duration="{{utils.getValue(item.duration, 500)}}"
    circular="{{utils.getValue(item.circular, false)}}" 
    vertical="{{utils.getValue(item.vertical, false)}}" 
    previous-margin="{{utils.getValue(item.previousMargin, '0px')}}" 
    next-margin="{{utils.getValue(item.nextMargin, '0px')}}" 
    snap-to-edge="{{utils.getValue(item.snapToEdge, false)}}" 
    display-multiple-items="{{utils.getValue(item.displayMultipleItems, 1)}}" 
    easing-function="{{utils.getValue(item.easingFunction, 'default')}}" 
    bindchange="eventHandler"
    bindtransition="eventHandler"
    bindanimationfinish="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper>
</template>

<template name="tmpl_14_swiper-item">
  <swiper-item wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  item-id="{{utils.getValue(item.itemId, '')}}" skip-hidden-item-layout="{{utils.getValue(item.skipHiddenItemLayout, '')}}">
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </swiper-item>
</template>

<template name="tmpl_14_switch">
  <switch wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" style="{{item.style}}" class="{{item.class}}"  checked="{{utils.getValue(item.checked, false)}}" disabled="{{utils.getValue(item.disabled, false)}}" type="{{utils.getValue(item.type, 'switch')}}" color="{{utils.getValue(item.color, '#04BE02')}}" bindchange="eventHandler"/>
</template>

<template name="tmpl_14_movable-area">
  <movable-area 
    style="{{item.style}}" class="{{item.class}}" 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}" 
    scale-area="{{utils.getValue(item.scaleArea, false)}}" 
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-area>
</template>

<template name="tmpl_14_movable-view">
  <movable-view 
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}" 
    direction="{{utils.getValue(item.direction, 'none')}}"
    inertia="{{utils.getValue(item.inertia, false)}}"
    out-of-bounds="{{utils.getValue(item.outOfBounds, false)}}"
    x="{{utils.getValue(item.x, '0')}}"
    y="{{utils.getValue(item.y, '0')}}"
    damping="{{utils.getValue(item.damping, 20)}}"
    friction="{{utils.getValue(item.friction, 2)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    scale="{{utils.getValue(item.scale, false)}}"
    scale-min="{{utils.getValue(item.scaleMin, 0.5)}}"
    scale-max="{{utils.getValue(item.scaleMax, 10)}}"
    scale-value="{{utils.getValue(item.scaleValue, 1)}}"
    animation="{{utils.getValue(item.animation, false)}}"
    width="{{utils.getValue(item.width, '10px')}}"
    height="{{utils.getValue(item.height, '10px')}}"
    bindchange="eventHandler"
    bindscale="eventHandler"
    bindhtouchmove="eventHandler"
    bindvtouchmove="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </movable-view>
</template>

<template name="tmpl_14_map">
  <map
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    longitude="{{item.longitude}}"
    latitude="{{item.latitude}}"scale
    scale="{{utils.getValue(item.scale, 16)}}"
    min-scale="{{utils.getValue(item.minScale, 3)}}"
    max-scale="{{utils.getValue(item.maxScale, 20)}}"
    markers="{{utils.getValue(item.markers, [])}}"
    covers="{{utils.getValue(item.covers, [])}}"
    polyline="{{utils.getValue(item.polyline, [])}}"
    circles="{{utils.getValue(item.circles, [])}}"
    controls="{{utils.getValue(item.controls, [])}}"
    include-points="{{utils.getValue(item.includePoints, [])}}"
    show-location="{{utils.getValue(item.showLocation, false)}}"
    polygons="{{utils.getValue(item.polygons, [])}}"
    subkey="{{utils.getValue(item.subkey, '')}}"
    setting="{{utils.getValue(item.setting, '', true)}}"
    layer-style="{{utils.getValue(item.layerStyle, 1)}}"
    rotate="0utils.getValue(item.rotate, 0)}}"
    enable-3D="{{utils.getValue(item.enable3D, false)}}"
    show-compass="{{utils.getValue(item.showCompass, false)}}"
    show-scale="{{utils.getValue(item.showScale, false)}}"
    enable-overlooking="{{utils.getValue(item.enableOverlooking, false)}}"
    enable-zoom="{{utils.getValue(item.enableZoom, true)}}"
    enable-scroll="{{utils.getValue(item.scale, true)}}"
    enable-rotate="{{utils.getValue(item.enableRotate, false)}}"
    enable-satellite="{{utils.getValue(item.enableSatellite, false)}}"
    enable-traffic="{{utils.getValue(item.enableTraffic, false)}}"
    enable-poi="{{utils.getValue(item.enablePoi, true)}}"
    enable-building="{{utils.getValue(item.enableBuilding, false)}}"
    bindtap="eventHandler"
    bindmarkertap="eventHandler"
    bindlabeltap="eventHandler"
    bindcontroltap="eventHandler"
    bindcallouttap="eventHandler"
    bindregionchange="eventHandler"
    bindpoitap="eventHandler"
    bindanchorpointtap="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </map>
</template>

<template name="tmpl_14_cover-view">
    <cover-view
      wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
      style="{{item.style}}" class="{{item.class}}"
      hover-class="{{utils.getValue(item.hoverClass,'none')}}"
      hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation,false)}}"
      hover-start-time="{{utils.getValue(item.hoverStartTime,50)}}"
      hover-stay-time="{{utils.getValue(item.hoverStayTime,400)}}"
      animation="{{item.animation}}"
      bindtouchstart="eventHandler"
      bindtouchmove="eventHandler"
      bindtouchend="eventHandler"
      bindtouchcancel="eventHandler"
      bindlongpress="eventHandler"
      bindanimationstart="eventHandler"
      bindanimationiteration="eventHandler"
      bindanimationend="eventHandler"
      bindtransitionend="eventHandler"
      bindtap="eventHandler"
     >
      <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
    </cover-view>
</template>

<template name="tmpl_14_label">
  <label
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    for="{{utils.getValue(item.for, '')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </label>
</template>

<template name="tmpl_14_checkbox">
  <checkbox
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox>
</template>

<template name="tmpl_14_checkbox-group">
  <checkbox-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </checkbox-group>
</template>

<template name="tmpl_14_form">
  <form
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    report-submit="{{utils.getValue(item.reportSubmit, false)}}"
    report-submit-timeout="{{utils.getValue(item.reportSubmitTimeout, 0)}}"
    bindsubmit="eventHandler"
    bindreset="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </form>
</template>

<template name="tmpl_14_radio">
  <radio
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    checked	="{{utils.getValue(item.checked	, false)}}"
    color="{{utils.getValue(item.color, '#09BB07')}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio>
</template>

<template name="tmpl_14_radio-group">
  <radio-group
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </radio-group>
</template>

<template name="tmpl_14_picker">
  <picker
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    header-text="{{utils.getValue(item.header-text, '')}}"
    mode="{{utils.getValue(item.mode, 'selector')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    range="{{utils.getValue(item.range, [])}}"
    range-key="{{utils.getValue(item.rangeKey, '')}}"
    value="{{utils.getValue(item.value, 0)}}"
    start="{{utils.getValue(item.start, '')}}"
    end="{{utils.getValue(item.end, '')}}"
    fields="{{utils.getValue(item.fields, 'day')}}"
    custom-item="{{utils.getValue(item.customItem, '')}}"
    bindchange="eventHandler"
    bindcancel="eventHandler"
    bindcolumnchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker>
</template>

<template name="tmpl_14_picker-view">
  <picker-view
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, [])}}"
    indicator-style="{{utils.getValue(item.indicatorStyle, '')}}"
    indicator-class="{{utils.getValue(item.indicatorClass, '')}}"
    mask-style="{{utils.getValue(item.maskStyle, '')}}"
    mask-class="{{utils.getValue(item.maskClass, '')}}"
    bindchange="eventHandler"
    bindpickstart="eventHandler"
    bindpickend="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view>
</template>

<template name="tmpl_14_picker-view-column">
  <picker-view-column
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </picker-view-column>
</template>

<template name="tmpl_14_slider">
  <slider
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    min="{{utils.getValue(item.min, 0)}}"
    max="{{utils.getValue(item.max, 100)}}"
    step="{{utils.getValue(item.step, 1)}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    value="{{utils.getValue(item.value, 0)}}"
    color="{{utils.getValue(item.color, '#e9e9e9')}}"
    selected-color="{{utils.getValue(item.selectedColor, '#1aad19')}}"
    activeColor="{{utils.getValue(item.activeColor, '#1aad19')}}"
    backgroundColor="{{utils.getValue(item.backgroundColor, '#e9e9e9')}}"
    block-size="{{utils.getValue(item.blockSize, 28)}}"
    block-color="{{utils.getValue(item.blockColor, '#ffffff')}}"
    show-value="{{utils.getValue(item.showValue, false)}}"
    bindchanging="eventHandler"
    bindchange="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </slider>
</template>

<template name="tmpl_14_textarea">
  <textarea
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    value="{{utils.getValue(item.value, '')}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    placeholder-style="{{utils.getValue(item.placeholderStyle, '')}}"
    placeholder-class="{{utils.getValue(item.placeholderClass, 'textarea-placeholder')}}"
    disabled="{{utils.getValue(item.disabled, false)}}"
    maxlength="{{utils.getValue(item.maxlength, 140)}}"
    auto-focus="{{utils.getValue(item.autoFocus, false)}}"
    focus="{{utils.getValue(item.focus, false)}}"
    auto-height="{{utils.getValue(item.autoHeight, false)}}"
    fixed="{{utils.getValue(item.fixed, false)}}"
    cursor-spacing="{{utils.getValue(item.cursorSpacing, 0)}}"
    cursor="{{utils.getValue(item.cursor, -1)}}"
    show-confirm-bar="{{utils.getValue(item.showConfirmBar, true)}}"
    selection-start="{{utils.getValue(item.selectionStart, -1)}}"
    selection-end="{{utils.getValue(item.selectionEnd, -1)}}"
    adjust-position="{{utils.getValue(item.adjustPosition, true)}}"
    hold-keyboard="{{utils.getValue(item.holdKeyboard, false)}}"
    disable-default-padding="{{utils.getValue(item.disableDefaultPadding, false)}}"
    confirm-type="{{utils.getValue(item.confirmType, 'return')}}"
    confirm-hold="{{utils.getValue(item.confirmHold, false)}}"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindlinechange="eventHandler"
    bindinput="eventHandler"
    bindconfirm="eventHandler"
    bindkeyboardheightchange="eventHandler"
  >
  
  </textarea>
</template>

<template name="tmpl_14_editor">
  <editor
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    read-only="{{utils.getValue(item.readOnly, false)}}"
    placeholder="{{utils.getValue(item.placeholder, '')}}"
    show-img-size="{{utils.getValue(item.showImgSize, false)}}"
    show-img-toolbar="{{utils.getValue(item.showImgToolbar, false)}}"
    show-img-resize="{{utils.getValue(item.showImgResize, false)}}"
    bindready="eventHandler"
    bindfocus="eventHandler"
    bindblur="eventHandler"
    bindinput="eventHandler"
    bindstatuschange="eventHandler"
  >
  
  </editor>
</template>

<template name="tmpl_14_navigator">
  <navigator
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    target="{{utils.getValue(item.target, 'self')}}"
    url="{{utils.getValue(item.url, '')}}"
    open-type="{{utils.getValue(item.openType, 'navigate')}}"
    delta="{{utils.getValue(item.delta, 1)}}"
    app-id="{{utils.getValue(item.appId, '')}}"
    path="{{utils.getValue(item.path, '')}}"
    extra-data="{{utils.getValue(item.extraData, '', true)}}"
    version="{{utils.getValue(item.version, 'release')}}"
    redirect="{{utils.getValue(item.redirect, false)}}"
    short-link="{{utils.getValue(item.shortLink, '')}}"
    hover-class="{{utils.getValue(item.hoverClass, 'navigator-hover')}}"
    hover-stop-propagation="{{utils.getValue(item.hoverStopPropagation, false)}}"
    hover-start-time="{{utils.getValue(item.hoverStartTime, 50)}}"
    hover-stay-time="{{utils.getValue(item.hoverStayTime, 600)}}"
    bindsuccess="eventHandler"
    bindfail="eventHandler"
    bindcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </navigator>
</template>

<template name="tmpl_14_video">
  <video
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{item.src}}"
    duration="{{utils.getValue(item.duration, 0)}}"
    controls="{{utils.getValue(item.controls, true)}}"
    danmu-list="{{utils.getValue(item.danmuList, [])}}"
    danmu-btn="{{utils.getValue(item.danmuBtn, false)}}"
    enable-danmu="{{utils.getValue(item.enableDanmu, false)}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    loop="{{utils.getValue(item.loop, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    initial-time="{{utils.getValue(item.initialTime, 0)}}"
    page-gesture="{{utils.getValue(item.pageGesture, false)}}"
    direction="{{utils.getValue(item.direction, 0)}}"
    show-progress="{{utils.getValue(item.showProgress, true)}}"
    show-fullscreen-btn="{{utils.getValue(item.showFullscreenBtn, true)}}"
    show-play-btn="{{utils.getValue(item.showPlayBtn, true)}}"
    show-center-play-btn="{{utils.getValue(item.showCenterPlayBtn, true)}}"
    enable-progress-gesture="{{utils.getValue(item.enableProgressGesture, true)}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    poster="{{utils.getValue(item.poster, '')}}"
    show-mute-btn="{{utils.getValue(item.showMuteBtn, false)}}"
    title="{{utils.getValue(item.title, '')}}"
    play-btn-position="{{utils.getValue(item.playBtnPosition, 'bottom')}}"
    enable-play-gesture="{{utils.getValue(item.enablePlayGesture, false)}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    vslide-gesture="{{utils.getValue(item.vslideGesture, false)}}"
    vslide-gesture-in-fullscreen="{{utils.getValue(item.vslideGestureInFullscreen, true)}}"
    ad-unit-id="{{item.adUnitId}}"
    poster-for-crawler="{{item.posterForCrawler}}"
    show-casting-button="{{utils.getValue(item.showCastingButton, false)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    picture-in-picture-show-progress="{{utils.getValue(item.pictureInPictureShowProgress, false)}}"
    enable-auto-rotation="{{utils.getValue(item.enableAutoRotation, false)}}"
    show-screen-lock-button="{{utils.getValue(item.showScreenLockButton, false)}}"
    show-snapshot-button="{{utils.getValue(item.showSnapshotButton, false)}}"
    show-background-playback-button="{{utils.getValue(item.showBackgroundPlaybackButton, false)}}"
    background-poster="{{utils.getValue(item.backgroundPoster, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    is-drm="{{utils.getValue(item.isDrm, false)}}"
    provision-url="{{utils.getValue(item.provisionUrl, '')}}"
    certificate-url="{{utils.getValue(item.certificateUrl, '')}}"
    license-url="{{utils.getValue(item.licenseUrl, '')}}"
    bindplay="eventHandler"
    bindpause="eventHandler"
    bindended="eventHandler"
    bindtimeupdate="eventHandler"
    bindfullscreenchange="eventHandler"
    bindwaiting="eventHandler"
    binderror="eventHandler"
    bindprogress="eventHandler"
    bindloadedmetadata="eventHandler"
    bindcontrolstoggle="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
    bindseekcomplete="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </video>
</template>

<template name="tmpl_14_camera">
  <camera
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    mode="{{utils.getValue(item.mode, 'normal')}}"
    resolution="{{utils.getValue(item.resolution, 'medium')}}"
    device-position="{{utils.getValue(item.devicePosition, 'back')}}"
    flash="{{utils.getValue(item.flash, 'auto')}}"
    frame-size="{{utils.getValue(item.frameSize, 'medium')}}"
    bindstop="eventHandler"
    binderror="eventHandler"
    bindinitdone="eventHandler"
    bindscancode="eventHandler"
  >
    
  </camera>
</template>

<template name="tmpl_14_live-player">
  <live-player
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    src="{{utils.getValue(item.src, '')}}"
    mode="{{utils.getValue(item.mode, 'live')}}"
    autoplay="{{utils.getValue(item.autoplay, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    object-fit="{{utils.getValue(item.objectFit, 'contain')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    min-cache="{{utils.getValue(item.minCache, 1)}}"
    max-cache="{{utils.getValue(item.maxCache, 3)}}"
    sound-mode="{{utils.getValue(item.soundMode, 'speaker')}}"
    auto-pause-if-navigate="{{utils.getValue(item.autoPauseIfNavigate, true)}}"
    auto-pause-if-open-native="{{utils.getValue(item.autoPauseIfOpenNative, true)}}"
    picture-in-picture-mode="{{utils.getValue(item.pictureInPictureMode, '')}}"
    referrer-policy="{{utils.getValue(item.referrerPolicy, 'no-referrer')}}"
    bindstatechange="eventHandler"
    bindfullscreenchange="eventHandler"
    bindnetstatus="eventHandler"
    bindaudiovolumenotify="eventHandler"
    bindenterpictureinpicture="eventHandler"
    bindleavepictureinpicture="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-player>
</template>

<template name="tmpl_14_live-pusher">
  <live-pusher
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    url="{{utils.getValue(item.url, '')}}"
    mode="{{utils.getValue(item.mode, 'RTC')}}"
    autopush="{{utils.getValue(item.autopush, false)}}"
    muted="{{utils.getValue(item.muted, false)}}"
    enable-camera="{{utils.getValue(item.enableCamera, true)}}"
    auto-focus="{{utils.getValue(item.autoFocus, true)}}"
    orientation="{{utils.getValue(item.orientation, 'vertical')}}"
    beauty="{{utils.getValue(item.beauty, 0)}}"
    whiteness="{{utils.getValue(item.whiteness, 0)}}"
    aspect="{{utils.getValue(item.aspect, '9:16')}}"
    min-bitrate="{{utils.getValue(item.minBitrate, 200)}}"
    max-bitrate="{{utils.getValue(item.maxBitrate, 1000)}}"
    audio-quality="{{utils.getValue(item.audioQuality, 'high')}}"
    waiting-image="{{utils.getValue(item.waitingImage, '')}}"
    waiting-image-hash="{{utils.getValue(item.waitingImageHash, '')}}"
    zoom="{{utils.getValue(item.zoom, false)}}"
    device-position="{{utils.getValue(item.devicePosition, 'front')}}"
    background-mute="{{utils.getValue(item.backgroundMute, false)}}"
    mirror="{{utils.getValue(item.mirror, false)}}"
    remote-mirror="{{utils.getValue(item.remoteMirror, false)}}"
    local-mirror="{{utils.getValue(item.localMirror, 'auto')}}"
    audio-reverb-type="{{utils.getValue(item.audioReverbType, 0)}}"
    enable-mic="{{utils.getValue(item.enableMic, true)}}"
    enable-agc="{{utils.getValue(item.enableAgc, false)}}"
    enable-ans="{{utils.getValue(item.enableAns, false)}}"
    audio-volume-type="{{utils.getValue(item.audioVolumeType, 'auto')}}"
    video-width="{{utils.getValue(item.videoWidth, 360)}}"
    video-height="{{utils.getValue(item.videoHeight, 640)}}"
    beauty-style="{{utils.getValue(item.beautyStyle, 'smooth')}}"
    filter="{{utils.getValue(item.filter, 'standard')}}"
    bindstatechange="eventHandler"
    bindnetstatus="eventHandler"
    binderror="eventHandler"
    bindbgmstart="eventHandler"
    bindbgmprogress="eventHandler"
    bindbgmcomplete="eventHandler"
    bindaudiovolumenotify="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </live-pusher>
</template>

<template name="tmpl_14_canvas">
  <canvas
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
    type="{{utils.getValue(item.type, '')}}"
    canvas-id="{{utils.getValue(item.canvasId, '')}}"
    disable-scroll="{{utils.getValue(item.disableScroll, false)}}"
    bindtouchstart="eventHandler"
    bindtouchmove="eventHandler"
    bindtouchend="eventHandler"
    bindtouchcancel="eventHandler"
    bindlongtap="eventHandler"
    binderror="eventHandler"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </canvas>
</template>

<template name="tmpl_14_keyboard-accessory">
  <keyboard-accessory
    wx:key="{{item.key}}" data-data="{{item.dataSet}}" id="{{item.uid}}"
    style="{{item.style}}" class="{{item.class}}"
  >
    <template is="{{utils.getTemplateByCid(cid+1)}}" data="{{vdoms: item.children, cid: cid+1}}"></template>
  </keyboard-accessory>
</template>

      
  














  